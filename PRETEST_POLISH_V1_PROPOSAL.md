# 外测前三小刀提案（pretest-polish-v1）— v2

> 基线 main `493ce85`。三刀共同点：**让 MVP 外测的信号更干净**，零地图几何、零引擎战斗逻辑、零 prompt 改动。
> v2（Opus 审后修订）：刀1 算术错修正（占3即时胜/丢3即时败把取值夹死在 [0,2]²，原验收 (c) 必挂）+ 验收改为 3×3 全表；刀3 落点改 renderFacilities（原指的占领环循环首行就 continue，旗永远看不见）+ 雾策略裁定；P1 两处实施口径写死。摆位置分歧已消（Opus 撤回原立场，双方同意外测后再议）。

## 0. 这次外测要学什么（所有取舍由它裁）

①陌生玩家能不能用嘴指挥（理解率/挫败点/真实 STT 语料）②参谋可信感（信任时刻 vs 出戏时刻）③戏剧引擎能不能长出"值得讲的一局"④玩家自发想做什么而做不到（免费需求语料）。
一把尺：**改动是否改变我们在这四问上学到的东西**。三刀分别护 ③（刀1）、③+②（刀2）、①（刀3）。大改地图不过尺，不做。

## 1. 三刀

### 刀1 · 超时计分修正（治"龟缩保平是理性解"）

**病灶**（`warPhase.ts:252-260` + `scenario/elAlamein/index.ts:147-159`，双方各自核毕）：超时结算 `score = captured − lost` 等权，draw=0。占 1 丢 1 与龟缩不动同为平局 → 进攻边际收益 ≤ 风险 → 理性玩家不动 → 戏剧引擎哑火。
**取值域（v2 修正的关键事实）**：`requiredCapturedObjectives: 3`（占 3 即时胜）、`maxFriendlyKeypointsLost: 3`（丢 3 即时败）→ 超时路径 captured ∈ [0,2]、lost ∈ [0,2]，共 9 种终局。mapData.ts:611 注释"currently 2"是陈账，以 index.ts 真值为准（实施时顺手改注释）。
**修法**：`score = 2 × captured − lost`（一行）**且** ratingThresholds 抬两档：majorVictory 3→**4**、victory 2→**3**（防倍率通胀顶端——不抬的话占2丢1=3 直接大胜）。语义原则：**进攻的一分买得起防守的一分还有剩；大胜只留给满分卷（占2丢0）**。
**9 格全表（即验收断言，逐格断言 + 负对照旧公式跑同表必 FAIL）**：

| captured\lost | 0 | 1 | 2 |
|---|---|---|---|
| **0** | 0 平局 | −1 小败 | −2 失败 |
| **1** | 2 小胜 | 1 小胜 | 0 平局 |
| **2** | 4 大胜 | 3 胜利 | 2 小胜 |

阶梯语义：大胜=占2且寸土未失；胜利=强推微损；小胜=任何净推进；平局=没动或对冲；败=纯失血（下行三格与旧公式完全一致，防守失败该疼还疼）。
**bench 入口（P1②）**：`endGameWithRating` 模块私有不导出——走已导出的 `checkGameOver` + 把 `state.time` 推过 `timeLimitSec`，断言 `state.gameOverRating`/`gameOverBreakdown`。

### 刀2 · 三山脊性格差异化（治"三个胜利目标复制粘贴"）

**病灶**（`pressureDirector.ts:496-505` buildPressureTargets (A) recapture 分支对所有 objective 统一计分：丢了+100/被占中+70）：打哪个山脊只取决于哪个近哪个空。
**修法**：导出的每目标权重表（v1 默认：北部山脊 ×1.4 死保 / 阿拉曼镇 ×1.2 / 中央山脊 ×1.0 / 南部高地 ×0.6 软肋），乘在 (A) 分支**基础分之后、`historyPenalty` 之前**（P1①：乘在惩罚后会把历史惩罚一起缩放，语义污染；注释写明理由）。只动 (A) recapture 分支，B/C 分支不动（v2 明确）。收益数字零改动，prompt 零改动。
**验收**：(a) 只读探针（照 `chaseAnchorHomeOf` 形状返回拷贝，不直接导出内部函数）：同一状态三山脊同时失守 → 候选分严格按权重表排序；负对照＝权重全 1.0 → 三分相等，新断言必 FAIL。(b) **实施第一步先跑 `ab-capture-stall --sweep` 对照漂移**（Opus 未查证项：主臂占领恒 72.0s/空城 35/36/27s 是真剧本跑的，pressureDirector 在泵帧链里；若漂移则重量重冻基线常量，属机械活但必须做）。(c) 真剧本 sweep 记录敌军对三山脊反扑派兵差异（记录性观测，不设硬门槛）。

### 刀3 · 胜利目标插旗（治"看不出哪 4 个点算赢"）

**病灶**（已核：19 设施中胜利目标 4 个，视觉上与油库无差别；HUD OBJECTIVES=captured/required，总数与规则不可见）。
**修法（v2 修正落点）**：旗画在 **`renderFacilities`（rendererCanvas.ts:240）**——**不是**占领环循环（:385 首行 `if (!fac.capturingTeam || fac.captureProgress <= 0) continue;`，只在占领进行中才进入，旗画在那儿等于平时全隐身）。对 `state.captureObjectives` 4 设施画旗杆+三角旗，颜色**直接读 `fac.team`**（敌红/我蓝/中立灰，不缓存不建第二真相源），易主即换色，v1 无动画。
**雾策略（裁定项②，Opus 建议恒显，Fable 附议）**：占领环有意雾门控（它泄露敌军实时动向）；**旗是静态地图知识**，与设施名字同策略（always-visible）——雾里也看得见旗，否则"第一眼知道往哪打"废一半。旗只随 `fac.team` 变色，不携带任何实时敌情，不泄雾。
**验收**：渲染层 bench 测不到，且 **Opus 浏览器面板跑不了本游戏（已实证 visibilityState=hidden → rAF 冻结）**——验收=实施方出改前/改后截图+用户过目；代码审查两点：旗色无独立状态（恒等 fac.team）、非 objective 设施零绘制改动。顺手：HUD 计数器语义查一遍（真值 required=3、目标共 4），可选加"共 4"一行；mapData:611 陈注释改掉。

## 2. 允许 / 禁改清单

**允许（5 文件）**：`warPhase.ts`（公式一行）· `scenario/elAlamein/index.ts`（ratingThresholds 两个值：4/3）· `pressureDirector.ts`（权重表+乘法+只读探针）· `rendererCanvas.ts`（renderFacilities 内旗绘制）· `App.tsx`（计数器语义，可选一行）；外加 bench 断言文件、mapData:611 注释修正。
**禁改**：地图几何与设施收益数字 · defensiveAI/deployment（P0 反击差异化=v2 选项）· buildPressureTargets 的 B/C 分支 · 一切 core 战斗/占领/移动逻辑 · 一切 prompt · 现有八闸断言。

## 3. 不做什么（明写）

- 不摆位置【分歧已消：Opus 撤回"先纵深"（四问尺服人），Fable 的 bench 漂移论他记为成本非阻碍——结论一致：外测后拿真 transcript 再议】。
- 不加新据点、不改收益数字、不改胜负为领土计量。
- 不治"敌方全知"（combat.ts:134，双方各自核毕逐字属实——敌索敌不吃雾，"偷袭"结构上不存在；大刀另立，外测后按玩家愿望清单排期）。
- 不给 19 设施全部重画图标。

## 4. 裁定记录与流程

**待用户拍板三件（双方联合建议已列）**：①计分收口——建议 2×公式+阈值抬两档（4/3），9 格表为准 ②旗在雾里显不显——双方均建议恒显 ③山脊性格数字——建议 北1.4/镇1.2/中1.0/南0.6 为 v1 默认。三件都可实施后按手感再调；①②不定刀1/刀3 不开工，刀2 可先动。
流程照旧：worktree 另开（`../AI Commander-pretest`，分支 pretest-polish-v1）；一步一测一 commit（①刀1+9格断言 → ②刀2+探针断言+漂移对照 → ③刀3+截图）；每步八闸+typecheck 全绿；三刀独立可单砍。实施/审核分工按上级惯例。

## 5. 三行人话（给用户）

1. 计分改成"占领一分抵丢两分"，且"大胜"只发给满分卷——缩家里保平不再是最优解,敢打才有奖。
2. 三个山脊有了性格：北边敌人死保、南边是软肋——"打哪个"从此是真选择，马克斯有真话可说。
3. 四个胜利目标插旗，红旗=敌占、蓝旗=已夺，雾里也看得见——第一眼就知道该往哪打。
