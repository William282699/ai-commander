# A刀+B刀 审核交接档（2026-08-03）— 给新开的 Fable 5 审核窗口

> 上一轮你是主审，我是联审（联审档 `DIALOGUE_FULL_AUDIT_COAUDIT_20260802.md`）。
> 之后用户批准开工，我实施了 A 刀与 B 刀，四轮真人手测驱动。现在请你审实施。
> **诊断/审核期零实施**：一行代码不许改，发现问题写清楚，改法等用户拍板。

## 0. 现场

- **worktree**：`/Users/yuqiaohuang/MyProjects/AI Commander-approval-v4`
- **分支**：`approval-contract-v4`，**待审 commit = `0ca4e8c`**（其下是 v4 原八 commit）
- **main 未动**（`668fbc2`），**未合并、未打 tag**
- dev：web 3009 / api 3012（`.claude/launch.json` 的 `approval-v4-web` / `approval-v4-api`）
- 台架：`npx tsx scripts/ab-approval-v4.ts --synthetic` / `--negctl`
- 当前状态：typecheck 4 包过；12 个 bench 全绿（116 条）；`--negctl` 25 条 ★ 真 FAIL

## 1. 先读什么（按顺序）

1. `git show 0ca4e8c --stat` 再读 commit message —— 它是结构化的，三段对应三刀
2. 联审档 `DIALOGUE_FULL_AUDIT_COAUDIT_20260802.md` **§9–§10**（施工记录 + 四轮手测实证）
3. 代码：`commanderPresence.ts` / `escalationTicket.ts` / `crisisResponse.ts:assessCrisisEscalation`
   / `ChatPanel.tsx:handleApprove` / `battleBoard.ts` / `intelDigest.ts`
4. 台架新增段：`ab-approval-v4.ts` 的 `runKnifeA` / `runKnifeB1` / `runKnifeB2`

## 2. ★★ 头号审点：我违反了一条你我共同在案的用户裁定

`escalationTicket.ts` 文件头（v4 刀2a 时写入，标注 **user ruling 2026-08-02, B 案**）明确规定：

> **不给板子行铸号**。理由：battleBoard 用 `buildReinforceOptions(state, null)`，升级用
> `(state, front)`，两者对**同名群**算出**不同成员集**（实测：板子 10 units vs 升级 5 units，
> 当簇跨越 front 边界时）。给板子行编号会让陈承诺 5、引擎派 10——静默，且任何只看
> label 的断言都抓不到。

**而 B 刀②给板子群行铸号了**（`intelDigest.ts` 传铸号器给 `boardToDigestLines`）。

- **我的理由**：手测③实证——陈答"附近有空闲部队吗"时念的是**板子**的群行，那面没号 ⇒
  模型只能把 label 写进 fromSquad ⇒ 撞 stale 闸，整条链断死。不给板子号，这条路永远不通。
- **我认为风险已变形而非消失**：现在两面**各铸各的号、各冻各的名单**，"承诺==执行"在
  **单行内部**仍由构造保证。
- **残余风险（请你独立判）**：同一支部队在**同一个信封**里可能拿到两个号、成员集不同。
  模型若引用 A 行的话却写 B 行的号，那条裁定担心的事就回来了，且同样静默。
- **可选缓解**（我没做，等裁）：按 roster 去重——同一轮内成员集完全相同则复用同一个号；
  成员集不同则保留两个号（那本就是两批不同的人，分开编号是诚实的）。

**请你给出裁定建议：维持原裁定（撤回板子铸号，另寻他法）／接受变形（现状）／
加去重缓解。这条我不自己改。**

## 3. 其余请重点挑的七处（我最不确定的）

1. **`commanderPresence.ts:101` 无钟行喂 null 钟** —— 行为零变化。我的论证是"统一走一把尺，
   且**禁止**接不过雾的 `estimateCollapseTime` 秒数，否则雾情经候选名单形状泄露"（TB/TA14 守卫）。
   请判：这是真设计还是自我安慰？零变化的调用点值不值得留？
2. **"番号是地址，不是背书" vs 刀3 的 T4s**（"陈不能提的案绝不存在号"）。我判不冲突，
   统一规则是"**号只给说出口的东西**"：升级面根本不提晚到候选（已过滤）⇒ 无可铸；
   板子面明说"有但赶不到"⇒ 必须有号。请独立验这个统一叙述是否真的自洽。
3. **`retargetIntentForTicket` 的优先级**：精确目的地（设施/tag/坐标）> 票据战斗锚点 >
   front 提示降级为 targetRegion；锚点仅在"目的地即本票据那条线或未给目的地"时生效。
   请挑：有没有反例（比如票据来自板子面 ⇒ `targetFrontId=""`、`anchor=null`，此时全靠降级）。
4. **铸号频次与寿命**：每个真实对话轮都对被说出口的部队铸新号；旧号 TTL 仍 120s 且可解析，
   其 roster 会过期。我判"降级诚实"（`liveMembersOf` 过滤 + 回执报真实数）够用。请挑。
5. **调度权取交集**：番号冻结名单 ∩ 本人格可调池，空交集响亮拒绝。请挑误伤面
   （未编组单位归 combat 角色，跨人格场景下会不会拒错）。
6. **Bucket A 我主动缩了范围没动**（无锚自动执行）。理由：引擎区分不了"新命令没点名"
   （砍卡法要保的"清楚就办"）与"跟进句被乱绑"，两者在"原话里有没有分队名"这个判据下同形；
   硬做会让清楚命令也弹确认，正是用户砍掉的手感。请判这个缩范围对不对。
7. **fix1 的披露文本可能加重复读** —— 我给了陈一句现成的"…都赶不到"，手测④他开始复读
   类似句式。请判披露文本是否需要收敛（但不能退回沉默——沉默会把兵藏起来，那是 fix1 的病因）。

## 4. 手测四轮的事实链（判据用，勿凭印象）

| 轮次 | 现象 | 归因 | 状态 |
|---|---|---|---|
| ① | 荐赶不到的兵；说"是的"派了 Aiden 顶包 | 候选无闸 + 群标签无把手 | A 刀 / B 刀② |
| ② | 问"有没有可支援部队"答"只有 Blake"，6 辆闲坦克被藏 | 闸清空后板子沉默（**A 刀自造**） | fix1 已修 |
| ③ | 陈念出两股空闲部队；说派 → `框选的单位不在可调度范围内`，实派 0 | 板子面无号 + 冻结名单被当过滤器 | B 刀②已修 |
| ④ | 10 个单位出发且是对的那批 ✓；但开进空沙漠 | 落点取战线**几何中心**（差 44 格）—— 刀1 只修了 ETA 那一面 | 已修 |

**未修、且 A/B 两刀结构上碰不到的**（详见联审档 §10.7）：
清楚的祈使被当咨询回掉（**四个数据点**：「调附近空闲军去支援」「全部过去」各一次零执行、
「你觉得应该增援吗」三次逐字复读）。prompt 的 `CONSULTATION vs ORDER` / `判断执照` /
`NEVER repeat yourself` 三条全部未生效。我建议 **G 刀（prompt 债）从 P2 提到 P0，与 E 刀合并**。

## 5. 家法（审核纪律）

判据测效果不测措辞 · **会动兵的断言必须数 `assignedUnitIds`**（TB16 已按此写）·
谁报的数字对方重算才作数 · 台词禁死模板 · 对话是唯一界面 · 禁关键词枚举 ·
先诊断后实施 · 每个结论给 file:line · **本档的自述只是线索，结论从代码重推**。

## 6. 方法论：两条源码扫描不变量（请评估是否推广）

本次把两条"护栏"从注释升级成会 FAIL 的断言，且**施工中各抓到一次真实漂移**：
- `CANDIDATE_FACES`（候选出口枚举表，键＝文件+符号+第几处，**不用行号**——行号会因上方
  加注释而漂，是噪声不是信号）→ 我改字段名时当场炸红。
- `CONFIRM_WORD_SITES`（确认词调用点登记表，政策只允许 fast-path / telemetry / context-only，
  **无 blocker 类**）→ 直接钉死 v4 刀2b 那次"词表被拿去当闸"的复发。

背景：绊索那次事故的根因是 **Codex 立的护栏写成了注释**（"词表只准当捷径；ANY word-list
miss goes to the LLM"），后来的刀把它反过来用而无人察觉。**注释拦不住人，断言可以。**

—— 联审（Opus）2026-08-03。零实施承诺已履行；等你审完与用户共同拍板后再动刀。

---

# 追加（同日，两份审核回来之后）

两位审核已出结论，**均判 Request changes / 不建议合并**。以下是缺陷合并清单、
我的独立复算、以及**用户提出的一个新修法**——请你重点审最后这条，它可能让清单里三条
缺陷连同它们的成因代码一起消失。

## 7. 两份审核合并后的缺陷清单（我已逐条独立重跑，全部复现）

台架计数订正：**实际 PASS=117**（brief/commit 里写的 116 是我引用了加断言之前的旧数）。

| # | 缺陷 | 实测 | 归属 |
|---|---|---|---|
| ① | **板子面票据无锚点 ⇒ 落点仍是几何中心** | 板子号+toFront → 落点 (295,38)，离战斗锚 45 格、离几何中心 1 格 | **本刀新造 + 验收误报覆盖** |
| ② | 板子号 + 无目的地 ⇒ 就地不动，回执仍印"出发了" | `6 个单位就地设防`，位置未变，回执照印 | 本刀新造 |
| ③ | **不存在的战线名被静默改写成票据旧锚点** | `toFront="__不存在战线__"` → `_targetPos=(250,38)`，原字段被删，`isValidTarget` 再也看不见错误 | 本刀新造 |
| ④ | **裸 retreat 被改成撤向原战斗锚** | 6 个单位收到 `retreat → (251,38)`，即敌人所在；违 retreat-semantics-v1 合同 | 本刀新造 |
| ⑤ | 回执不是真实出发数 | roster=6 + `quantity=2` → 实派 2，回执"6个单位出发了" | 旧账，本刀未治 |
| ⑥ | **机器 B 的晚到闸仍量到几何中心** | 同一候选：到战斗点 25s / 到中心 89s / 存活 30s ⇒ 机器 A 留、机器 B 弃 | 旧账，**本刀把它从排序扣分放大成淘汰依据** |
| ⑦ | 披露句把交战中的部队算成"闲着" | `线外2股/10units 闲着`，其中 4 个 task=交战中 | 本刀 fix1 新造 |
| ⑧ | （流程）Bucket A 观察打点收不到目标人群 | 打点条件含 `getActiveEscalation`，而待观察人群正是"无升级的裸确认" | 观察计划缺陷 |

**★ 最该记的一条教训**：①的台架断言（TB17/TB18）用的是**手写的、带锚点的假票据**，
真实板子票据从未进过任何一条断言 ⇒ **台架对它自己要防的那个 bug 发了假绿灯**。
这与本档 §6 那条"登记表锁的是出口数量、不是出口行为"是同一族：
**fixture 的形状必须来自生产路径，不能手捏。**

## 8. ★ 用户新提的修法（请重点审）：目的地按"从具体到笼统"分档

### 8.1 观察起点（用户提出，我复算成立）
同一个 `resolveTarget`，三个字段精度天差地别：
```
tag        {"targetRegion":"tag_1"}        → (301,110)  你标的那个点        ✓
facility   {"targetFacility":"中央前哨"}   → (361,105)  那个设施本身        ✓
front      {"toFront":"中央战线"}          → (264,96)   两块 bbox 的平均值  ✗ 偏 97 格
```
前两个是"某个东西在哪"，第三个是**一个统计量**。而引擎明明算得出那条线上真在打的点
（`battleAnchorFor` = (359,105)，离中央前哨 1 格），只是从没接到这条路上。
`_targetPos` 这个内部坐标通道**早就存在**，注释原文写着它就是为了
"avoid region/front center inaccuracy"——造好了，在最常走的路上没用。

### 8.2 提议的分档（从具体到笼统）
| 玩家怎么说 | 落到哪 | 现状 |
|---|---|---|
| "去标记点1" | 该 tag | ✓ |
| "守中央前哨" | 该设施 | ✓ |
| "固守某战线"·**该线正在打** | **打得最集中的那一处** | ✗ 现给全部交战单位的**平均** |
| "固守某战线"·**没在打但有我方部队** | 我方部队所在（归建/接防） | ✗ 现给几何中心 |
| "固守某战线"·**我方空** | 该线上值得守的东西（我方设施/胜负点） | ✗ 现给几何中心 |
| 荒线什么都没有 | 才退回几何中心 | 保留 |

**第三档不能照搬现成的 `battleAnchorFor`——它取平均。实测：同一条线西头一场、东头一场，
它给出正中间，离两场各 100 格，两边都不挨着。** 必须改成"取最主要的那一处"。
**第五档实测同样必要**：空线时几何中心 (263,96) 是空沙漠，而该线上我方真正拥有的是
中央前哨 (360,105)。

### 8.3 覆盖分析（我算的账，请你复核）
| 缺陷 | 此修法 | 理由 |
|---|---|---|
| ① 板子号落点 | **根治** | 根因就是 front→几何中心；front 解析修对后，板子号无需自带锚点 |
| ③ 假地名静默改写 | **连带消失** | 该段代码（`anchorWins`）本就是为绕开几何中心而写；路修直即可整段删除 |
| ④ 裸 retreat 撤进战场 | **连带消失** | 同上，同一段代码 |
| ② 原地不动 | 半个（"就地设防"本身合法，假话部分归⑤） | |
| ⑤ 回执假数 | ✗ 独立：回执须改为按每个票据 intent 的实际 `assignedUnitIds` 结算 | |
| ⑥ 机器 B 用错点 | ✗ 但**变成顺手**：同一根因的另一个调用点，喂同一个新解析器即可 | |
| ⑦ "闲着"数错 | ✗ 独立，一行（只数 task==="无任务"） | |
| ⑧ 打点盲区 | ✗ 独立 | |

**净效果：3 治 + 1 半 + 1 顺手，且删掉的代码多于新增**（③④ 的成因代码整段作废）。

### 8.4 我看到的取舍与风险（请你独立判）
1. **影响面最广**：改的是**所有**"去/守某条战线"的命令，不只番号派兵那一路。
2. **票据锚点的语义会变**：原本冻锚点是为了"报的 ETA 与实际落点同一个点"。改后落点在
   执行时**现算**，与报 ETA 那一刻可能略有出入。我判可接受（现算更接近真实，ETA 本就是
   估计），但这是本次唯一一处"故意放弃一个已成立的不变量"，请你判。
3. **"最主要的那一处"需要定义**：最大簇？受压最重？两者在多簇场景下会分叉。
4. **验收判据必须量效果**：断言写成"去某条战线的落点距该线上真正在发生事情的地方 < N 格"，
   **不能**写成"调用了新函数"。且 fixture 必须走生产路径（①的教训）。
5. 雾安全：`battleAnchorFor` 只读我方单位（已核），设施是公开元数据，均不泄露敌情。

## 9. 请你交付
1. 复核 §7 八条（我已全部独立重跑，欢迎推翻任何一条）
2. **裁定 §8 这一刀**：分档是否正确？"最主要的那一处"怎么定义？第 2 条取舍接不接受？
3. 与 §2（板子铸号 vs 08-02 裁定）的关系：若 §8 成立，板子票据**不再需要锚点**，
   §2 的争点是否因此缩小？
4. 排序建议：我倾向 §8 先做（它一改，①③④ 连同成因代码一起消失，后面几条的表现形态也变）。

—— 联审（Opus）追加于 2026-08-03。worktree 仍停在 `0ca4e8c`，零实施。
