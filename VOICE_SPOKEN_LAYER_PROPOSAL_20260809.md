# 语音输出分层 · 一页纸提案（spoken 层，2026-08-09）

> **提出人：用户**（手测中提出的设计）。本档由 Opus 5 落成文字并配上实测数据。
> 给 Fable 5 审。**这是超出 §W 开工合同的新增范围**，要 Fable 审 + 用户拍板才施工。
> 现场：worktree `~/MyProjects/AI Commander-voice-input` 分支 `voice-input-v1` @ `c45d1aa`，
> 工作区干净，台架 22 项全绿、negctl 48 条红集合逐条未变。web 3019 / api 3018 在跑。

---

## §1 一句话

现在陈的**一份文本要同时给眼睛和耳朵用**，两件事互相打架；
加一个专给耳朵的 `spoken` 输出（一两句人话），语音回合的 TTS 只念它。

---

## §2 病（用户手测五个症状，是同一个结构问题的五个切面）

用户原话：「要么大段全读跟 Emily 似的当播报员，要么出现 Aiden 这种，
要么逼逼半天，要么重复我说的话，要么就短短一句」。

拆开看，五个症状共用一个根：**同一段正文被两个用途拉扯**。

| 用途 | 要求 | 现状冲突 |
|---|---|---|
| 上屏（扫视） | 要番号 `[临时编队G13]`、精确数字 `23秒/2.48`、地名 | 念出来又长又机械 |
| 出声（听） | 要短、口语、不念番号、不念英文名（中文 TTS 念 `Aiden` 本就别扭） | 写短了屏上就少了定位信息 |

于是：
- **大段全读** ＝ 正文是写给眼睛的，长
- **Aiden 这种** ＝ 屏上该有的英文名，耳朵里很怪
- **短短一句** ＝ 命令类回合模型把话都塞进 JSON，正文只剩一句甚至空
  ⇒ 出声的只剩执行回执那 4 个字「照办，长官。」，而屏上还有三行（**执行摘要行、
  H1 披露句都是哑的**）—— 眼睛和耳朵拿到的信息量严重不对等
- **重复我说的话** ＝ L2（正文 == 转写），本刀已立案

### 现在到底什么会出声（全仓只有两处，已核）

| 出声 | 位置 |
|---|---|
| 陈的正文（流式逐块） | `ChatPanel.tsx:1625 / :1657` + `:1671` flush |
| 执行回执的**前 4 个字** | `:1922` `speak(voiceConfirm, …)` —— **不含**后面的命令描述 |

**哑的**：执行摘要行、H1 披露句、危机升级问句（「北部战线吃紧，约 6 秒，您看怎么处理？」）、
战报播报、其它频道的话。⇒ 陈**主动喊长官**的那句现在一声不出，这是另一笔账。

---

## §3 设计（用户提的分层）

```
Gemini 一次输出三样：
  ① 正文    → 上屏（照旧：番号、数字、地名）
  ② JSON    → 给引擎（options / heard / …）
  ③ spoken  → 新增，只给耳朵：一两句人话

客户端：
  语音回合 → TTS 只念 ③；正文只上屏
  打字回合 → 一个字节不动（照旧念正文）      ← 用户定的保守边界
```

**加一条本地即时应答音**：收到录音的瞬间，本地从现成的 `VOICE_CONFIRMS` 池里播一句
（「嗯。」「照办。」），**零模型延迟**。

---

## §4 延迟实测（推翻了 Opus 自己的估计，数据在此）

Opus 起初报「spoken 放 JSON 里会让首声推到 3-5s」——**错，且错了一个数量级**。
用 5.7KB 真实规模信封（含 FRONT_JUDGMENT / 38 行 SQUADS / FACILITIES / TAGS）实测三跑：

| 跑次 | 首字 | 正文流完 | **JSON 到** | JSON 比首字晚 |
|---|---|---|---|---|
| #0 | 1527ms | 1527ms | 1782ms | **+255ms** |
| #1 | 1451ms | 1809ms | 2803ms | +1352ms |
| #2 | 1754ms | 1754ms | 2041ms | **+287ms** |

**JSON 只比首字晚 0.25~1.35s，中位 ~0.3s。**

附带发现：三跑里**两跑"正文流完 == 首字"**（15 字一次吐完）——
短回复上根本没在流式，"边流边念"的手感有一半是想象的。这进一步压低本方案的代价。

**改动前后的听觉时序**：

```
现在：  说完 ──1.5~2.3s──→ 陈开口（内容是写给眼睛的）
改后：  说完 ──0.3s──→「嗯。」（纯本地）──2~3s──→ 一句写给耳朵的人话
```

⇒ **长官听到第一声反而比现在早**；实质内容晚 0.3~1.0s，换来每句都是人话。

---

## §5 兜底规则（用户在提案时就点出了这个坑）

用户问：「有时候只有文字没有 JSON，对吧？」——**对**，那是降级路径
（模型没吐合法 JSON → `createFallbackResponse()`）。那时 `spoken` 也没有。

> **规则：`spoken` 缺席 ⇒ 退回念正文（即现状行为），绝不静默哑掉。**

一条规则覆盖四种缺席：模型忘写 / schema 白名单吃掉 / JSON 解析失败走兜底 / 通讯中断。

---

## §6 `spoken` 的合同（写不好模型会拿 brief 复制粘贴）

一两句，写给耳朵：
- **不念番号**（`[临时编队G13]`、`G8`）——番号留给眼睛
- **不念英文名**（`Aiden`/`Blake`）——要提就用人话代称
- **不念精确到小数的数字**（`战力比2.48`）——数字留给眼睛
- **不复述长官刚说的话**（heard 已经在别的字段里）
- 说的是**你要对长官说的那一件事**，不是正文的摘要

（写成语义原则，不挂词表——红线二。）

---

## §7 与既有账的关系（这一层吃掉了别的刀的一半理由）

| 账 | 变化 |
|---|---|
| **L2 正文复读转写** | **降级**：TTS 不再念正文 ⇒ 复读即使发生也听不见，只剩"屏上难看" |
| **A-1（改 prompt 措辞治 L2）** | 可缩水或取消——但仍建议保留其中「你听到的不一定是战场命令」那半条，见下 |
| **A-2（流首 heard 事件，40 分钟）** | **不再需要**：spoken 在 JSON 里就够，无需流首解析 |
| **「骂人被转写成中央战线」**（手测新账） | **不受本层影响**，仍需 A-1 那条语义原则单独治 |

---

## §8 判据（全部效果级，可机器判）

`ab-voice-input --live` 增：
1. `spoken` 在场率（语音回合 ≥19/20）
2. `spoken` 长度上限（字数），防它变成正文的复制
3. **不含 `G\d+` / 英文字母 / heard 的整句**（三条硬线，都是正则可判的**结构**特征，
   不是措辞词表）
4. `spoken` 缺席时客户端退回念正文（摘刀负对照：把 spoken 去掉，断言 TTS 收到的是正文）

---

## §9 施工步骤与工期（约 1 小时 15 分）

1. `types.ts` / `schema.ts` 加 `spoken?: string`，**两条 return 路径都带**（heard 同族先例）—— 10 min
2. `ai.ts` 语音回合的【本次强制】补 spoken 合同 + `SPEECH_RULE_SITES` 登记 + 指纹 —— 15 min
3. 客户端：语音回合 TTS 改道念 spoken + 本地即时应答音 + 缺席兜底 —— 25 min
4. 判据四条 + 全家硬线 —— 25 min

---

## §10 待裁

1. **进不进 V1**（demo 时间压力下的优先级）——用户拍板。
2. 打字回合要不要也念 spoken？**Opus 建议不动**（打字路径保持零改动最安全），用户已倾向如此。
3. `spoken` 与正文/单子不一致时以谁为准？**建议：屏上正文与单子为准，spoken 只是"说给你听的那一版"**，
   不得携带正文没有的事实（否则又是一个新的"嘴与账本"缺口，撞 G 刀合同）。
4. §2 末尾那笔独立账——**危机升级问句现在一声不出**（陈主动喊长官时是哑的）。
   要不要顺手让它出声？Opus 建议单独一刀，别塞进本层。

---

## §11 现场状态（给可能的新窗口）

- worktree `~/MyProjects/AI Commander-voice-input`，分支 `voice-input-v1`，HEAD `c45d1aa`，**工作区干净**
- 已收口：步1 `15bc99e` → 步2 `59457ef` → 步2.5 `68e1e3c` → 步3a `d2b7164` →
  步3 `e8a91ac` → 步3b `8b9a185` → 步5 `2b45922` → 刀B `c45d1aa`
- 台架：22 项全绿；`ab-voice-input --synthetic` 61 条；`--negctl` 48 条红集合逐条未变
- 服务：`.claude/start-voice-{api,web}.sh`（3018 / 3019），主仓库 launch.json 已加这两条
- 相关档：`VOICE_INPUT_V1_PROPOSAL_20260808.md`（正文+§V+§W+§X）、
  `VOICE_INPUT_V1_OPUS_REVIEW_20260808.md`、`_R2_20260809.md`、
  `VOICE_INPUT_V1_HANDTEST_20260809.md`（手测+诊断+刀A/刀B plan）
- 未做：步 6 用户完整手测（含延迟 A/B 基线）、A-1、L2 分臂重组

---

**Opus 5 · 2026-08-09 · 设计期零实施**

---

# §12 Fable 审定（2026-08-09；本节与正文冲突以本节为准，连同正文构成施工合同）

**总判：通过，带 R1-R7 修订条款；§10-1 待用户拍板后开工。** 审查四个面的结论：

**① 不是补丁**。五个症状一个根（一份文本同时伺候眼睛和耳朵），分层是治根；反而
A-1/A-2 才是打在单个症状上的。但有一个"长成补丁"的风险要用 R1 钉死。

**② 勾稽关系逐条核过**：schema 两条 return 路径（heard 同族先例）✓；heard fail-closed
判定不受影响✓；brief 回填不受影响✓；CONFIRM_WORD_SITES 无涉✓；打字回合零改动
（用户钉的边界）✓；A-2 死亡、A-1 缩水成归一化原则——与前序裁定衔接一致。
**发现三处提案没写的，进 R2/R3/R4。**

**③ 新输出对既有功能**：additive 字段过白名单、只在语音回合被消费——结构上无溅射。
两个真风险点：义务稀释（语音【本次强制】从一个义务变两个，heard 的 L1 20/20 可能
被拖低）→ R5；缺席兜底的时序差异 → R7。

**④ 红线**：§6 语义原则✓；§8 三条是结构正则（G\d+/ASCII/heard 包含）不是词表，
先例=问号校验✓；本地应答音复用现有 VOICE_CONFIRMS 池=既有 sanctioned 实践
（:1922 今天就在念它），但要 R6 圈住。

**延迟数据采信**：与 Fable 设计期独立探针方向一致（options 紧随 prose、短回复整块到），
不重测；3-5s 错估的自纠处理合规。**刀 B 收口认可**：RED 结构上拿不到（合成假麦无 AEC
管线，尾损仅 ~13ms，夹具表达不出这个病=家法④的反向应用），效果裁决权移交真麦手测
——commit 里如实披露，合规。

## R1-R7 修订条款（施工时逐条落实）

| # | 条款 |
|---|---|
| R1 | **spoken 合同第一条＝从属正文**：不得携带正文没有的事实、不得与正文/单子矛盾，冲突以屏上为准——spoken 是"说给你听的那一版"，不是第二张嘴（防新的「嘴与账本」缺口，撞 G 刀合同） |
| R2 | **听觉序列写死**：一个语音命令回合的完整出声序列=本地应答（「嗯。」）→ spoken → 执行回执 speak（:1922）三段——**回执 speak 保留还是并进 spoken，实施前二选一写明**，不许三声堆叠无设计 |
| R3 | **L2 判据归宿显式登记**（T1j 式）：L2「正文含 heard」从硬线降级为记录行（红了打印全文不 FAIL）；新硬线=「spoken 不含 heard 整句」接任 |
| R4 | **登记表补全**：withVoiceReinforcement 内容变更 → SPEECH_RULE_SITES 该面 note 同步改 + spoken 新指纹 + `REGISTERED_SHARED_SURFACE_RULINGS` 追加条目（Emily 共享面又多一段，性质仍是授权新增） |
| R5 | **义务稀释防线**：spoken 上线后 `--live` 同跑 L1（heard 在场 ≥19/20）与 spoken 在场率，**L1 跌破即停**——不许为了新字段牺牲老字段 |
| R6 | **VOICE_CONFIRMS 池两条铁线**：NEVER EXPAND（池子照抄现有，不加一句）；只出声不上屏（上屏即死模板，违 07-22 家法） |
| R7 | **缺席兜底时序登记**：spoken 缺席 → options 到达后整段念正文（比今天晚 ~0.3s 起念，流式期语音回合不念）——有意差异，登记不隐瞒 |

## §10 四项裁定

1. 进 V1：**Fable 推荐进**（治的正是打断步 6 手测的五个症状；~75 分钟；打字零动）——**用户拍板**。
2. 打字回合不动：**核准**。
3. spoken 从属正文：**核准，升格为 R1**。
4. 升级问句哑巴账：**核准单独立刀**，随收口进 LEDGER（输出侧新账：陈主动喊长官时无声）。
