# UI 动画 Round 2 · 交接文档（老 Fable 窗笔录用户口述，2026-08-15）

> **给谁看**：新 Fable 窗口（出工程 plan）与新 Opus 窗口（实施）。用户已口述
> 全部需求，本档是唯一权威转写；与任何旧计划冲突时**以本档为准**。
> plan 写完先给审核窗（Opus）审，过了给用户三行人话概要过目，然后开工。

## 0. 现场（拿到本档先核这一段）

- **基线＝round 1（UI 简化 V1）八步收口后的 main**：分支 `ui-simplify-v1`
  八 commit（`26a7b5e`→`7ade215`）合 main ＋ tag `ui-simplify-v1-done`。
  ★开工前先核 main 含该 tag；**若无＝round 1 收口未完成，回问用户，
  勿从 worktree 分支上另起炉灶**。
- round 1 随身两笔开账（可与本轮一并处理，plan 里明说怎么办）：
  ①**PTT 真麦克风手测**从未做过（步 5 无头浏览器测不了，一个动作的事）；
  ②步 7 记账：风格条五条的**数字文本**仍同色 cyan，跟不跟各条底色，
  用户一句话裁。
- 实施必须 worktree（家法：勿在主 cwd 动工）；建议复用
  `AI Commander-voice-input` 目录（.env 已配好，ops=deepseek 勿动），
  从收口后的 main 切新分支（建议名 `ui-anim-round2`）。
- dev 服务：`.claude/launch.json` 里 **`frame-web`(3025)/`frame-api`(3024)**
  两条 cd 进 voice-input worktree；★名叫 `web` 的条目＝3003＝主仓库——
  起错了截的图就是没改过的代码，整步验收作废。
- 工装遗产（round 1 淌出来的路，plan 别重蹚）：scratchpad
  `/private/tmp/claude-501/-Users-yuqiaohuang-MyProjects-AI-Commander/7af46070-0c6a-464e-9508-ca31be9f4f61/scratchpad/`
  已装 playwright＋`shoot.mjs` 截图脚本；**弹窗必须从主页面点「弹出面板」**
  （直连 `?mode=panel` 无 opener 会自关）；`__GAME_BRIDGE__` 只在主窗
  （弹窗走 `window.opener.__GAME_BRIDGE__`）；台架＝worktree 根
  `bash scripts/run-benches.sh` 一把 25/25；跨窗口报审**给截图绝对路径**，
  "发图"跨不了会话边界。

## 1. 需求（用户 2026-08-15 口述，两把刀）

### 刀 A · 语音侧（先行）

- **A1 无线电呼叫动画**：按住 🎤 且**真在收音**时，对话流里出现一条
  "电台呼叫中"的临时行（年代感：载波弧线脉动＋莫尔斯点划，手搓 SVG）；
  松手发送 → 动画行消失、正式消息落下。用户原话：「玩家说话的时候，
  对话框里显示无线电拨通的小动画，松开手发送语音的时候，动画消失。」
- **A2 说一半取消（新功能）**：按住说话中途发现命令错了，要能不发出去。
  手感已拍板（用户 08-15）＝**滑开取消**（微信肌肉记忆）：按住不放
  **滑出按钮范围＝进入取消态**（呼叫行＋按钮变红、提示「松手取消」），
  在外面松手＝取消、什么都不发；按住期间 **Esc** 同效。
  ★现状陷阱必须反转：现在滑出按钮的行为是"停止并发送"
  （ChatPanel PTT 按钮 `onPointerLeave` → `stopPTT`）——说到一半手一歪
  错令直接发出去，这正是要治的病。
- **A3 PTT 键加大**（round 1 交接档挂的旧账）：滑开手势需要更大的落点，
  与 A1/A2 一体做。

### 刀 B · 文字侧

- **B1 电报机模拟打字**：输入框旁一台小电报机（发报键＋纸带，手搓 SVG）；
  输入框内容**真变一次**键头敲一下、纸带流出点划。挂 onChange 不挂裸
  keydown：Shift/方向键不响，粘贴一段字响一串。
- **B2 回车＝发报动画**：检测到回车（或点「发送」，同一条路）且**真发送**
  （非空、非 loading）时，演"发报键重击＋一串长划打出去"。空输入回车
  不响、loading 中不响——这两格是天然负对照。
- **B3 来源分流**：马克斯频道语音识别完是**模拟点击发送键**发出去的——
  发送来源须标记（打字/语音），电报机只认打字的发送；语音的收尾归
  电台隐喻（A1 行消失），两套隐喻不许串。

## 2. 铁律（审核按此打回）

1. **动画必须真状态驱动，禁装饰循环**。先例三件：刀C 🔴 真收音才亮；
   C3 灯不许撒谎（`startPTT` 里只有 `arm.press()` 真进 collecting 才亮红，
   设备未到手红灯不许骗人）；步 7 值真变才闪红。A1 挂与红灯同源的状态。
2. **素材零外部**：全部手搓 CSS/SVG，吃现有 HUD 色板变量（含步 7 新增的
   `--hud-accent-purple/yellow`）。用户可选给风格参考图；没有就按现有
   HUD 风出第一版，靠每步截图迭代。
3. **隐喻分区**：语音＝电台（动画进对话流），文字＝电报（仪器在输入旁，
   不进对话流）。
4. **纯 web 层**：引擎/prompt/信封零字节；ChatPanel 巨文件（3400+ 行）
   勿趁机重构——round 1 全程守住了这条，本轮同样。
5. **刀 A / 刀 B 各自单独 commit**（刀 A 内部步序 plan 定）；一步一测一
   commit，每步截图报审，用户过目后走下一步。
6. **A2 取消要分两臂做（plan 须亲核代码）**：录音臂（陈/Emily 频道，
   `channelUsesVoiceCapture`）已有半成品 `arm.cancel()`（`stopPTT` 的
   设备未就绪分支在用），补"取消态松手也走它"；Web Speech 臂（马克斯）
   边听边把识别文字**实时写进输入框**，取消＝`abort()` ＋输入框回滚
   （快照点＝startPTT 按下时），不然半句错令留在框里等着被误发。
7. **判据家法**（round 1 淌出来的）：正反都断言、负对照先行、幅度对齐
   真实场景（先例＝round 1 P0-⑦ 的 0.03 真实幅度格）、绊索自证会响、
   行号必漂锚点 grep、DOM 断言只覆盖渲染出来的（隐藏条件块要源码点名）。

## 3. 明确不在本轮（别顺手做）

- 声音效果（电报滴答、电台底噪）——用户没提，禁自作主张加。
- 停靠态风格条那笔死账（getState 引用不稳定 → 嵌入态 styleSnapshot 恒
  null，LEDGER 在案）——勿顺手修，修了停靠态会凭空多东西。
- 数字跟色（步 7 记账）——除非用户开工前顺手裁定带上。
- 任何引擎侧的语音/发送逻辑改动（取消只动 web 层的两臂收尾路径）。

—— 老 Fable 窗口笔录，2026-08-15。疑义处回问用户，勿猜。
