// ============================================================
// Feature Flags — gradual rollout switches
// ============================================================

/** Marcus acts as chief-of-staff (advisor-only, no execution options) */
export const ENABLE_MARCUS_CONSULT_V2 = true;

/** ops channel uses compressed BattleContextV2 instead of full DigestV1 */
export const ENABLE_BATTLE_CONTEXT_V2 = true;
