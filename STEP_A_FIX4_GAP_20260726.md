# fix4（新鲜度先后）线上复验报告 — 覆盖一支、漏一支

> 测试者：Opus（浏览器自动化 + 截获真实信封）。
> 被测：`commander-presence-v1` @ `2053e38`。环境：worktree 前端 3005 / 后端 3002。
> **结论先行：fix4 方向正确、不是回退；但漏了一支，而漏的这支可能是真实对局里的常态，且失败形态是"把全灭报成苦撑"。**

---

## 0. 先确认没有回退

| 检查 | 结果 |
|---|---|
| 改动面 | `commanderPresence.ts`(+6/-1) + bench(+22)，**零红线** |
| 五闸 + typecheck | **Opus 独立复跑**，全绿 |
| synthetic | 30 条断言全过 |
| 节头文本 | `---FRONT_JUDGMENT--- (CURRENT values for this reply — numbers quoted in earlier questions/escalations are ask-time snapshots, superseded by these; …)` ✓ |

**fix4 之前根本没有任何先后规则**，旧快照无条件赢。所以下文这个失败在 fix4 之前只会一样挂或更糟——**fix4 没有制造它，是没盖到它。**

---

## 1. 测法（可复现）

1. 浏览器自动化，劫持 `requestAnimationFrame` 用假时间戳**泵帧**驱动 sim（可精确控制推进多少游戏秒）；**停止泵帧＝冻结状态**，于是"瞬态"不会在测试期间跑掉。
2. `fetch` 拦截，捕获发往 `/api/command*` 的**请求体**——拿到的是**信封原文**，不是事后推测。
3. 全新开局 → 下令「全线向西推进，跟敌军接触」→ preflight 反问 → 回「照打」。
4. 泵帧直到 Chen 自发抛出**带数字的升级提问**。捕获于 `t=151`：
   > 「长官，中央战线我方单位仅能支撑 **12秒**，敌我战力比 **1:4.5**。东北方向第一未编组群可在 **68秒** 内抵达，是否调动增援？」
5. 继续泵约 **29 游戏秒**（到 `t=180`），让快照彻底过期 → **冻结** → 提问。

---

## 2. 结果：同一冻结状态，两个问法，答案自相矛盾

信封里同时存在（同一封，同一瞬间）：

```
---FRONT_JUDGMENT--- (CURRENT values for this reply — … superseded by these; …)
4. 南部战线: survival≈39s ratio=16.31 best_help=北线前哨附近未编组群(3units 无任务 eta≈48s)
2. 山脊战线: 无我方作战部队（增援须从后方调兵）
3. 中央战线: 无我方作战部队（增援须从后方调兵）      ← 当前真相：中央已无部队
5. 敌军后方: 无我方作战部队（增援须从后方调兵）
```
```
参谋刚问:「长官，中央战线我方单位仅能支撑 1秒，敌我战力比为 1:4。北线前哨有3辆轻坦闲置，预计49秒可抵达，是否调动？」   ← 旧快照
```

| # | 玩家问 | Chen 答 | 判定 |
|---|---|---|---|
| A | 中央那边现在**到底什么情况**？ | 「中央战线我方单位**仅能支撑1秒**，敌我战力比**1:4**。敌军两辆主战坦克正在压上。」 | ❌ **逐字复述旧快照**，把"部队已全灭"报成"还能撑1秒" |
| B | 中央战线**还有我们的人吗**？ | 「中央战线**已无我方作战部队**，长官。」 | ✅ |

**A 和 B 是同一冻结状态、同一封信封。** 所以正确数据**够得着**——模型只是在"要求报现状"的问法下不选它。

**错误性质是分类级的，不是漂移级的**：不是 6 秒说成 10 秒，是**"全灭"说成"苦撑"**。玩家会以为那儿还有人在顶。

---

## 3. 机制推断（未实证，请核）

节头写的是 **"superseded by these"** —— 但无部队行**一个数字都没有**。
**没有可替换的数，模型就退回唯一有数字的来源（快照）。**

对照 bench 的 **R10**：那个 fixture 里当前框有 `3秒/0.33` 的**真实行**，"用这些覆盖"有明确对象 → 过。
**R10 恰好没有覆盖"当前行是无数字的存在性说明"这一支。**

### ⚠️ 同一个洞很可能还波及第二支

`buildFrontJudgmentLines` 现在有**两种不带 survival 的行**：

- `无我方作战部队（增援须从后方调兵）` ← 本次实测中招
- `交战中，我方Nunits hp=X%，敌军实力未明（无法给出存活估计）` ← **同样没有 survival，推断同样压不住旧快照**（未实测）

**建议补完时把两支一起处理，并各配一条断言。**

---

## 4. 为什么这支可能是常态而非边角

我原打算再造一个"当前框有竞争数字"的活样本做正向对照，**造不出来**——而这本身是证据：

**一条战线一旦触发存活升级提问，按定义就是要塌了。** 等玩家读完问题、回一句话，那条线**往往已经没了**。

本局实际分布：唯一有 survival 数字的是 `4. 南部战线 ratio=16.31`（我方碾压，不会触发升级）；而**触发过升级的中央战线——已经空了**。

即："快照说还剩 N 秒 → 玩家开口 → 战线其实已全灭"**恐怕是正常轨迹**。

---

## 5. 证据边界（我没验的部分，不冒充）

1. **fix4 覆盖的那一半（框内有竞争数字 → 用当前值），我没有独立线上复现。** 它依据的是 Fable 5 的 R10 真模型跑；我复跑的是 synthetic 30 条，**不含真模型 fixture**。
2. **失败样本 N=1**（＋1 条对照）。不是 N=3。建议补完前先用 bench 断言把它变成确定性证据。
3. **第 3 节的机制推断未实证。** 另一种解释是"问法诱导复述待答问题"，而非"没有替换数字"。B 组对照说明数据够得着，但没有隔离出成因。

---

## 6. 建议修法（fix4 的补完，同一处、同一条力学）

把"覆盖"从**换数字**扩成**换判断**。语义原则一句，无词表：

> 本框中显示为"无我方作战部队"或"敌军实力未明（无法给出存活估计）"的战线，其在早先提问/升级中出现的存活秒数与战力比**一律作废，不得复述**；该战线的现状以本框这一行为准。

位置沿用 fix4 的力学（规则贴着赢家走）。**一个可讨论的选项**：把作废声明放进**那一行本身**而不是节头——离具体战线更近，可能比节头更"贴着赢家"。

**bench 断言（建议两条）：**

- **R11**：当前框含 `X: 无我方作战部队` + 伪造快照带 X 的 survival/ratio（造成不可能值）→ 复述必须为"无部队"，**快照数字零出现**。
- **R12**：当前框含 `X: 交战中…敌军实力未明` + 同类伪造快照 → 复述必须说"敌情未明/给不出存活估计"，**快照 survival 零出现**。

> R10 的教训沿用：**伪造值要造成不可能值**（Fable 5 第一版 fixture 因 eta 太接近真值无法判定出处），否则断言判不出数字出处。

---

## 7. 给 Fable 5 的三个问题

1. 机制推断对不对——是"无替换数字导致回退"，还是"问法诱导复述待答问题"？（B 组对照说明数据够得着）
2. 作废声明该放节头还是放**那一行本身**？按 fix1/fix3/fix4 换来的力学（纠正落在赢家所在的位置），行内似乎更贴。
3. 雾中激战行（同样无 survival）是否确认同病？要不要同刀处理？

---

## 8. 立场

**这不是推翻 fix4，是把它自己那条力学用到底。** 方向（规则贴着数字走、一处覆盖四通道两路由）我认同；漏的是"当赢家该被替换成的东西本身没有数字"这一支。

**建议补完这一刀再收口 Step A。** 失败形态（全灭报成苦撑）比原先那个数字漂移严重得多。
