# 教学关 V1 · 交接档（2026-09-06 建，**2026-09-10 更新到步 6f**）

> 给新窗口：**本档自含**，不需要上一轮对话。
> **在同一个 worktree 继续开工**（用户裁定 2026-09-06），不要另开分支。

---

## 0. 先读这三样

| 文件 | 是什么 |
|---|---|
| `ROADMAP.md`（**本 worktree 的那份**） | 「🚧 教学关 V1」那一段就是本刀的收口段，已写到步 6f |
| `LEDGER_ALL_KNOWN_ACCOUNTS_20260806.md`（本 worktree） | **§W** 是本刀的账，W1-W12 |
| `~/.claude/projects/-Users-yuqiaohuang-MyProjects-AI-Commander/memory/MEMORY.md` | 家法（feedback_*），违反会被用户当场抓 |

⚠ ROADMAP 与 LEDGER 的改动**只在本分支上**，主仓库那两份没动——合并时一起走，扔掉也一起扔。

---

## 1. 这把刀在干什么

正式战役（el_alamein）之前加一关**独立的新手引导小地图**，手把手把玩家送到
「跟 AI 参谋说话」这件事上，打完问他要不要进正式局。

**为什么值得做**：首次外部试玩三个人**没有一个真正跟参谋说过话**——注意力全被基本
操作吃掉了（LEDGER §U）。这一关的唯一使命是**把人送过门槛**。凡是妨碍这件事的都是 P0。

---

## 2. 环境

```
worktree : /Users/yuqiaohuang/MyProjects/AI Commander-squad-personality
分支     : tutorial-map-v1     （14 commit，未合 main）
基线     : main = 293a14d      （主仓库代码零改动）
```

`node_modules` 与 `apps/server/.env` 都齐，**不用 npm install**。

```bash
npm run typecheck                                     # 四包
bash scripts/run-benches.sh                           # 27/27
npx tsx scripts/probe-tutorial-map.ts --tripwire      # 31/31
npx tsx scripts/probe-tutorial-guide.ts --tripwire    # 67/67
```

**dev server**（★用 `preview_start`，不许用 Bash 起）：`squadp-api`(3026) / `squadp-web`(3027)。
起完**核工作目录**（本项目踩过"端口跑的其实是主仓库"）：
```bash
lsof -nP -iTCP:3027 -sTCP:LISTEN | awk 'NR>1{print $2}' | head -1 | xargs -I{} sh -c 'lsof -a -p {} -d cwd -Fn | grep ^n'
```
⚠ 这两个 server 挂在会话名下，**浏览器窗格一重置就跟着死**（本轮死了六七次）。
用户要长时间试玩时让他自己开终端跑 `.claude/start-squadp-{api,web}.sh`。

**入口**：`?intro=1`（从开场闸走）／`?scenario=tutorial`（直接进）／`/`（正式局）。

---

## 3. 做到哪儿了

**十四个 commit**，引导**十步**：
`squad_1` → `squad_2` → `talk_emily` → `see_arsenal` → `talk_marcus` → `squad_3` →
`merge_squads` → `place_tag` → `take_beacon` → `take_post` → 结束语。

玩家完整体验：开场闸 →（选进教学）→ 编两队 → 跟艾米莉说话 → 看军械 → 问马克斯 →
用她造的兵编第三队 → 编制页拖动合并 → 按 T 插旗 → 占烽火台（迷雾散）→
打下敌军哨站 → 结算屏 +「进入正式战役 ▶」。

**没做**：步 6 手测修单（用户正在玩）。

---

## 4. ★ 七条设计裁定（改之前先读，别推翻错了）

1. **引导是「陈说话」，不是勾选清单**（家法「对话是唯一界面」）。白赚：从第一秒就在教
   「参谋会主动跟你讲话」，那正是本作要验证的核心动作。
2. **引导句写死＝有意的家法豁免**。推翻它的正解**不是**放开自由发挥，而是把指令当
   **事实**递给模型、由它用陈的语气复述。
3. **结束语必须存在**（用户 09-04）：手把手引导会把玩家训练成「等指令」。
   最后一句的职责是**显式解除引导**；有意举例而**不列清单**。
4. **引导提到什么，那个东西就得自己亮**（用户 09-08）。屏边走 CSS 脉冲、地图走 canvas
   呼吸圈，**都是 1.6s、同一个琥珀色**。探针机器版规矩：**每一步至少一个目标，不许光说不指**。
5. **脉冲绑生命周期，不做常驻**；**只有键可点时才亮**；**已经在那个频道/页就不闪它**。
6. **节奏三层闸**（两轮手测逼出来的，别随便拆）：
   · 做完一步**先静一拍**（默认 4s／说话类 12s），期间不说话也什么都不闪
   · **参谋还在回话就继续等**（`advisorBusy`）——猜秒数永远猜错
   · 催促要**等够时间 且 玩家一下都没动过**（30s／行军类 75s），一步只催一次
7. **判据一律判松**（说话＝发过消息就算，看军械＝切过去就算）；**卡的步骤**是
   编队×3、说话×2、看军械、合并、插旗、两次占领。判紧＝在教学关里亲手造门槛。

---

## 5. ★ 本轮踩过的坑（别再踩，全在 LEDGER §W）

1. **`enemyAIMode` 缺席 ≠ 没有敌方 AI**（W1）——两套 AI 由同一字段互斥，闸的方向相反。
2. **占领谓词曾经分家**（W2）——新场景默认掉在"只有步兵能占"那侧，坦克永远打不下目标。
3. **`source: "player"` 被当两个意思用**（W3）——编队日志也是这个标签，害得"跟马克斯
   说过话"那步被自动跳过。判据别嗅代理指标。
4. **渲染顺序吃掉视觉提示**（W4）——引导圈画在设施里被迷雾盖住。
5. **同一个东西有两份渲染**——ChatPanel 里「编队」键、频道键各两份。**改按钮前先 grep 数一遍。**
6. **判据不该跟着数组下标走**（W5）——插一步就全错位，已改成按 id 找。
7. **台词里两次写了 markdown `**`**（W6）、**一次写了查无出处的「全力进攻」**（W7）。
8. **玩家此刻站在哪**要考虑：「编队」键只在陈的频道、「编制/军械」页签跟着频道走——
   引导得先把他叫回去（用户 09-09 连报三条）。

---

## 6. 隔离性（用户反复确认过，回答要准）

- 硬写 `"tutorial"` 只有 **7 处**，全是注册点；教学关专属约 **770 行**。
- **金样逐字节**：el_alamein 初始状态＋三频道信封＋占领谓词输出，18006 vs 18042，
  **只差一行**（显式搬过去的 `victoryLabel`，字符串逐字相同）。
- **删除实验做过**：删专属文件 + 跟着编译器报错删 import ⇒ typecheck 0 错、
  benches 27/27、金样回到 base。
- ⚠ **两处有意改了正式局**：开场入口、阵营圈外晕（共用渲染）。
- **一键扔**：`git branch -D tutorial-map-v1`（从未合 main）。

---

## 7. 接下来

1. **步 6 手测修单**——用户正在玩，把他的**原话**记下来再动手
   （禁脑内枚举同义词，「词汇不通」那笔账）。
2. **合 main 之前**：把 7 个勾稽点收敛成一个 integration 文件（W12），
   那时正好要决定哪些留哪些扔。
3. main 上那笔 ROADMAP 文档 commit `293a14d` **还没 push**。

---

## 8. 家法（最容易被违反的）

- **不在主仓库动工**——`/Users/yuqiaohuang/MyProjects/AI Commander` 只读
- **一步一测一 commit**：typecheck → benches 27/27 → 两个探针 → **实机验** → 报审
- **判据要测效果不测措辞**；**绊索先红**；**不许穷举法**；**台词禁死模板**（本刀有一处
  有意豁免，见 §4.2）；**对话是唯一界面**
- **文案红线**：屏上每句话必须指回一个能派生的数字或真机制（本刀栽过两次）
- **影响手感的变更先给用户三行人话确认**
- **直接读代码，别扇出 agent**（ultracode 与它打架时**用户偏好优先**）
- ★ **台架有盲区**：`processAutoBehavior`／导演／引导 interval 都不在 `tick()` 里，
  27/27 绿**不代表**验过——所以引导判定才被抽成纯函数 `advanceGuide` 以便逐拍重放
- **给用户报卡住的地方要原话**
