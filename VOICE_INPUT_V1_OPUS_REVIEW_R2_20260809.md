# 语音输入替换刀 V1 — Opus 5 收口审核 R2（2026-08-09）

> 被审对象：`VOICE_INPUT_V1_PROPOSAL_20260808.md` 的 **§V 修订 + §W 开工基线**
> （正文与 §V/§W 冲突以后者为准；正文 P6 与 §2-C 兜底已撤回）。
> 上轮审核档：`VOICE_INPUT_V1_OPUS_REVIEW_20260808.md`（3 P0 / 11 P1 / 5 P2 / 4 问 / 2 新账）。
> 本轮只审两件：①§V 对那 19 条的吸收有没有走样或漏项 ②§W 新增件有没有引入新风险。
> 方向已裁死，不重开。基线 main = `cb02c2b` = origin/main，工作区干净。
> **零实施**：本档 + scratchpad 探针副本之外未动任何文件。

---

## §0 结论

**§V 对 19 条的吸收：19/19 全部落地，方向无一走样；两处论证措辞不准（结论对、理由要改）。
§W 六条新增件没有引入新风险；「TTS 零牵连」经代码核实属实。**

**但有 1 个 P0 必须在开工前定掉**——它不是新风险，是**上一轮我自己没说全**造成的验收缺口：
P0-1 的修法本体（「语音回合 heard 缺席 ⇒ 禁入 bucket A」）落在 `ChatPanel.tsx:1576-1577`
那两行 bucket 判定上，而 §V-0/§V-5 采纳的搬家清单是我 R1 给的三个函数
（`canAutoExecute`/`isValidTarget`/`detectStaleSquadRefs`）——**判定本体不在清单里**。
按现在的合同施工，这刀最重要的一条新安全行为**将没有任何机器断言**，只有手测。

| 判决 | 数 |
|---|---|
| **P0（开工前必改）** | **1**（验收落点：bucket 判定要跟着搬家，否则摘刀负对照咬不到修法本体） |
| P1 | 6 |
| P2 | 5 |
| 给 Fable 的问题 | 2 |

**⚠ 因存在 P0，本轮不宣布"可开工"**——P0 是一句话的合同修订（步 2.5 的搬家清单加一项），
Fable 认了、用户点头即可闭环，我随即开 worktree 按 §5+§V-5 施工。

---

## §1 P0 · 摘刀负对照咬得到 ④ 的接线，咬不到 P0-1 的修法本体

### 病

R1 我给的搬家清单是三个函数，Fable 逐字采纳（§V-0 P0-2）。但**决定"进不进 bucket A"的不是
`canAutoExecute`**——它只返回 `{auto, reason, playerNamedSquad}`（`ChatPanel.tsx:271-377`）。
真正的判定在 `processAdvisorData` 里，两行：

```
// ChatPanel.tsx:1576-1577
const bucketA = staleRefs.length === 0 && opt0 != null &&
  (reason === "no_anchor" || (reason === "anchor_mismatch" && !gate.playerNamedSquad));
```

§V-0 的修法「语音回合且 heard 缺席 ⇒ 禁入 bucket A」**必然写在这两行上**（要多一个
`&& !(isVoiceTurn && !heard)` 之类的合取项）。而 :1576 在 `ChatPanel.tsx` 组件闭包内部
（读 `gate`/`opt0`/`data`/`state`），搬不搬家决定它**够不够得到台架**。

后果按两条判据分开看：

- **④（heard→canAutoExecute 接线）**：搬家后台架够得到，摘刀 ⇒ `playerNamedSquad` 由 true 翻 false
  ⇒ 断言真 FAIL。**这条没问题，负对照会咬。**
- **P0-1 的 fail-closed 判定**：断言点在 bucket 结论上，而 bucket 结论留在 ChatPanel
  ⇒ **一条机器断言都写不出来**。它恰恰是本刀对外承诺的那句话（§V-6 第 3 条：
  「没听清时陈会反问而不是照做——这是本刀最重要的行为变化」）。

这正是本项目栽过五次的同一形状（家法：**判据要测效果不测措辞**；第 7 级方法资产：
**负对照必须打在 bench 测得到的层上**——GameCanvas 接线打不到就打 builder 本体）。
LEDGER **H1「GameCanvas 接线无台架可测」** 就是这一族；这次是同族的 ChatPanel 版。

**责任说明**：R1 我写的是"把 `canAutoExecute` 搬家让 ④ 的负对照真咬"，只点了函数没点判定，
Fable 照单全收没走样——**这一条是我上轮的欠账，不是 §V 的吸收问题**。

### 修法（一句话合同修订，约 5 分钟实现）

步 2.5 的搬家清单**加一项**：把 :1576-1577 的 bucket 判定抽成同模块的纯函数，
ChatPanel 只留一行路由（H1 `describeCommittedPull` 的成例：**判断在模块、ChatPanel 只路由一行**）：

```
// apps/web/src/autoExecuteGate.ts
export function decideBucket(input: {
  gate: { auto: boolean; reason?: string; playerNamedSquad?: boolean };
  hasOption: boolean;
  staleRefCount: number;
  voiceTurn: boolean;      // 本轮是否带 audio 发出
  heardPresent: boolean;   // 本轮 heard 是否到位
}): "auto" | "A" | "B" | "C"
```

搬家 commit 仍是**零行为变化**（`voiceTurn=false` 时逐字等价于现在这两行），
13 台架 + typecheck 当负对照；随后 ④ 与 P0-1 两条断言都落在 `decideBucket` 上：

| 断言 | 接线在 | 摘刀（去掉 heard 参数/去掉 voice 合取项） |
|---|---|---|
| ④ 点名不符 | `"B"`（问一句） | `"A"`（静默派 B）→ 真 FAIL |
| ④ 点名相符（正对照） | `"auto"` | `"auto"`（不变，证明闸没拧死） |
| **P0-1 heard 缺席** | **`"B"`** | **`"A"`（照发，含 fallback 那一格）→ 真 FAIL** |

**判据仍是效果级**：`decideBucket` 返回 "A" 时台架跟着跑 `resolveIntent` 数 `assignedUnitIds`
核实际动谁（R1 定的口径不变，只是多了一个够得到的落点）。

> 附带收益：`isValidTarget` 与 `detectStaleSquadRefs` 本来就在搬家清单里，
> `staleRefs.length===0` 这半个条件因此也第一次进了台架射程。

---

## §2 §V 对 19 条的吸收 · 逐条核对

**判法**：对每条问三件事——吸收版=我的原意？有没有被放宽？有没有漏掉承重的半句？

### V-0 三个 P0

| 条 | 吸收 | 判 |
|---|---|---|
| P0-1 | 复算 file:line 与我一致（:334-338 / :368 / :371 / :1576-1581）；结论「真实行为=静默照发、真承重格=bucket B」逐字对；修法=禁入 bucket A | **原意保全**。两处要改：①**论证措辞不准**（见 P1-1）②**修法本体的验收落点漏了**（§1 P0） |
| P0-2 | 摘刀负对照作废、换点名不符格、判据数 `assignedUnitIds`、配正对照、采纳搬家 | **原意保全**，`runKnifeB1` 的 nth 不受影响这点它自己复核了（我 R1 也核过，一致） |
| P0-3 | 登记新面 + policy 分派（D⑤=`context-only` / D⑥=`telemetry`）+ 移动则改 note 不加行 + `isDeclineReply` 主动登记 + 不许放宽断言 | **原意保全且更细**（policy 分派比我给的更明确）。**时点错**，见 P1-2 |

**核心论证复算（本轮重点①）**：「一条判定连 fallback 一起封」——**成立**，但理由要换。
我把三条到达路径全走了一遍：

1. 流式 `options` 事件（`ai.ts:1166-1167`）：payload = `normalizeAdvisorForDay7(validated)`，
   而 `validated` 只能来自 `validateAdvisorResponse`（白名单重建）⇒ heard 要么被登记后透传、要么不存在；
   `normalizeAdvisorForDay7`（:609-632）用的是 `{...data, options}` 展开 ⇒ **不会吃掉 heard**（我特意查了，
   它是第四个可能的丢字段点，结论：安全，提案不必改）。
2. 流式降级（`ai.ts:1171-1176`）与异常（:1183-1188）：`createFallbackResponse()`。
3. 非流（`ai.ts:810-834`）：同上。

⇒ **没有任何未经校验的对象能到客户端**，而 `createFallbackResponse()`（`schema.ts:426-455`）
是**手写字面量**，天生没有 heard。所以语音回合的 fallback 一定 `heard === undefined`，
被同一条判定拦住 ✓。**但 §V-0 的措辞「fallback 经白名单重建后 heard 必为 undefined」是错的**
——fallback **根本不过白名单**（见 P1-1）。

**还要钉死的一句（实现约束，不是新要求）**：判定必须是
`voiceTurn && !heardPresent`，**两个条件都要**。若图省事写成"heard 缺席即禁入 bucket A"，
**每一个打字回合都会被推进 bucket B**（打字回合永远没有 heard）——那是把砍卡法整个推翻。
§V-0 的原文写的是「语音回合且 heard 缺席」，没错；我只是把这个合取项标成承重件。

### V-1 P1 十一条

| # | 吸收 | 判 |
|---|---|---|
| P1-1 延迟对照式 | 收，裁决线 = 中位劣化 ≤ +0.7s，绝对值只作参考，在 demo 拓扑上量 | 原意保全。**但"超线之后怎么办"不成立**，见 P1-3 |
| P1-2 voiceChannels 双条件 | 逐字采纳（白名单 ∩ provider==="gemini"） | ✓ |
| P1-3 fail-closed 挂能力 | 逐字采纳，正文写法作废 | ✓ |
| P1-4 audio/message 互斥 | 逐字采纳 | ✓ |
| P1-5 AEC + 按下即 cancel TTS | 逐字采纳 | ✓（且与 §W-5「TTS 零牵连」不冲突，见 §3-5） |
| P1-6 heard 时点 | 承认正文写错，(a)/(b) 交用户 → §W-2 裁 (a) | ✓，**(a) 的自洽性我另行验过=真**（本轮重点③，见 §3-3） |
| P1-7 占位行进 context | 收，「（语音·未转写）」 | ✓ |
| P1-8 非流+jsonMode+audio | 收且**当场补测** | ✓ 我已复算，见 §4 |
| P1-9 共享面登记 | 收，进 `REGISTERED_SHARED_SURFACE_RULINGS` | ✓ |
| P1-10 --sites 职责 | 承认正文写反并撤回；照刀2 先例主动登记+补指纹 | ✓ |
| P1-11 16kHz 承重 | 收，从"路径"升"承重项" | ✓ |

### V-2 P2 五条 · 全收，无走样（P2-4「禁写断言」的理由它复述对了：同义反复牙）

### V-3/V-4/V-5

- **V-3**（P1-8 补测）：数据我复算过，**定性复现**（§4）。
- **V-4 Q1**：承认没注意到时序，不掩饰——这是对的处理。
- **V-4 Q2**：Emily 一起换耳朵的理由（demo 有她戏份 + 只上陈反而要加 channel 分支）**站得住**；
  §3 补第 7 条差异。
- **V-4 Q3**：**它更正了我的前提，而且它是对的**——我 R1 说"把批准对象从模型手里收回引擎"
  隐含了"对象在模型手里"，实际 v4 立场是 authorize 逐字执行**登记时钉死的捕获案**
  （top 候选 + unitIds 快照），`pendingDecision` 只决定批不批。既然对象本就在引擎手里，
  词表 backstop 的净增益只剩"漏批救援"（摩擦问题），代价是给最娇贵的合同消费路径加第二个法官。
  **裁 v1 单法官 + 漏批 ≥1 次才启用旋钮 —— 我接受这个裁定，撤回 R1 的 Q3 建议。**
- **V-4 Q4**：时间预算收进 §7 第二启用条件 ✓。
- **V-5**：施工顺序全采纳我的 §5 表 + 新增步 2.5 + 步 3 打包安全态 ✓。
  **两处要补**：CONFIRM_WORD_SITES 的时点（P1-2）、步 3 打包漏了 ②⑤（P1-4）。

---

## §3 §W 六条新增件 · 有没有引入新风险

| # | 件 | 核 |
|---|---|---|
| W-1 | 范围维持（陈+Emily 换耳朵，ops/群聊不动） | 无新风险 |
| W-2 | **P1-6 裁 (a)** | **无新风险，自洽性已验**（本轮重点③，下详） |
| W-3 | 保守案降级=用户喊停制、无死日期 | 无新风险；**但"步 1-3 成果复用"略夸大**，见 P2-3 |
| W-4 | **V1.5 转写中继立项（不施工）** | **接口没有被锁死**，但有"复制第二份"的风险，见 P1-5（本轮重点②） |
| W-5 | **TTS 三人画像钉死、与本刀零牵连** | **属实**，见下 |
| W-6 | 施工照 §5+§V-5 | 见 P1-2 / P1-4 |

### 3-3 · 本轮重点③：「(a)/(b) 只影响气泡不影响安全」——**这句话为真**

代码核实：`processAdvisorData` 的唯一两个入口都在**回复结束时点**——
流式的 `event.type === "options"`（`ChatPanel.tsx:1744-1752`，含 EOF 兜底 :1778-1783）
与非流兜底 `/api/command` 的 `processAdvisorData(data)`（:1812）。
而 `canAutoExecute` 的唯一调用点在 `processAdvisorData` 内部（:1531），bucket 判定在 :1576。

⇒ **④ 接线与 P0-1 fail-closed 判定在 (a) 和 (b) 下是同一个时点**（options 事件），
(b) 只是让 heard **更早**到达、可以更早填气泡。安全面逐字不变。✓

追加核了一个"(a) 期间玩家能不能插进来"的窗口：不能——`loading` 为真时发送按钮禁用
（:2927 / :2776-2777）、`startPTT` 直接 return（:686）。所以"陈说完之前气泡还是 🎤"这段时间里
不存在第二个回合。**(a) 的代价纯粹是观感**，与安全无关，§W-2 裁得对。

### 3-5 · 「TTS 零牵连」核实：属实

- 本刀不改 `apps/web/src/tts/*` 任何字节；
- P1-5 要的「按下 🎤 即 cancel() TTS」调用的是 ChatPanel **已经导入并在用**的那三个函数之一
  （`cancel()` 现已用于 :1716「reset TTS for new stream」，注释 :688-691 明写 ChatPanel 只碰 speak/flush/cancel）
  ⇒ 是 ChatPanel 内一行调用，不是 tts 模块变更；
- 嗓子替换线（Alnilam/Orus/Callirrhoe，规格档已读）动的是 voice 配置与上场引擎选型，
  与本刀的输入侧无共享文件。**两条线可以并行，互不阻塞。** ✓

---

## §4 探针复算（家法⑥：Fable 补测的这组我还没算过）

**做法**：把 `probe-nonstream.mjs`（无信封）与 `probe-nonstream2.mjs`（带信封，同款 ~5KB）
原样复制到 scratchpad，同一 `cmd1.wav`，各 2 跑，请求形状一字未改。

| 臂 | Fable 报 | 我重跑 |
|---|---|---|
| 无信封 | 「**占领附件**的闲置部队, 是**东原**南线前哨」×2 | run0「**占领附件**的闲置部队, 是**东源**南线前哨」/ run1「**在附件**的闲置部队, 是增援**南线前哨**」 |
| 带信封 | 「**战狼点**附近在闲置部队,去增援**南线前哨**」×2 | run0「**战狼点**附近在闲置部队,去增援**南线前哨**」/ run1「**战狼点附近的闲置部队,去增援南线前哨**」（逐字全对） |

**定性结论复现 ✓**：同一段音频，**信封的有无翻转地名对错**。
逐项复算（原句=「战狼点附近的闲置部队，去增援南线前哨」）：

- **「战狼点」（玩家自己插的标记名）**：无信封 **0/4**（Fable 2 + 我 2，全错）→ 带信封 **4/4 全对**。
- 「南线前哨」（设施名）：无信封 **1/4**（我 run1 蒙对一次）→ 带信封 **4/4**。

**我给这条证据加一句 Fable 没说、但更有说服力的话**：红利**最大的正是「战狼点」这种
任何通用语言模型先验里都不存在的名字**——它只存在于这一局的 `---TAGS---` 里。
这恰好是 Web Speech 结构上永远拿不到的那一格（I2 的账根），也是第 8 级刀4「标记名闭环」
手测刚刚证到的那条链（玩家插旗 → 说回去 → 真派兵）。消融实验证的不是"上下文有点用"，
是"**这条链没有信封就断**"。

**次要观察（与 §V-3 第 ③ 条一致，把 n 从 2 抬到 4）**：jsonMode 臂的虚词精度——
带信封 4 跑里 **1 跑完全逐字**、3 跑有同一种滑（的→在 / 去→是）。§V-3 用「疑似」是准确的措辞，
不下结论、由听错清单覆盖 ✓。这条同时是 **N2（脏音频下 heard 是复述不是转写）** 的旁证：
**滑的永远是虚词，实词（地名）在信封在场时是稳的**——对 ④ 的锚点（番号/领队名属实词）是好消息。

**绝对毫秒**：无信封 2036/1494ms、带信封 1763/1709ms（Fable：2248/1874、1948/1597）。
±0.3s 漂移照旧 ⇒ **仍然只引用定性结论与差值，不引用绝对值**（§V-1 P1-1 已经这么写了 ✓）。

原始输出留在 `…/scratchpad/probe/`（连同 R1 那次的 `rerun-stdout.txt` / `rerun-results.json`）。

---

## §5 新账查重（LEDGER A-I 三十八笔逐区核过）

| 新账 | 查重结论 |
|---|---|
| **N1 通讯中断的兜底方案被自动执行** | **无重复**。B 区（执行链补漏）讲的是作用域/番号/走回头路，机制不同；H 区是台架结构账。LEDGER 里没有任何一笔涉及 `createFallbackResponse` 或"LLM 失败时自动下单"。建议立案时写明**打字路径 main 今天就成立、非本刀引入**（§V-0 已这么写 ✓） |
| **N2 脏音频下 heard 是复述不是转写** | **无重复，但要挂两条链**：与 **I2**（Web Speech 听不懂英文地名）是**不同引擎的不同病**——I2 是听不出来，N2 是听不清就自己顺一句；与 **D4**（目的地不抄原话）**同精神不同层**——D4 在 intent 字段层、N2 在转写层。立案时建议在 N2 行里显式写 `见 I2/D4，非同一笔` |

---

## §6 判决清单（P1/P2 一句话）

**P1**

| # | 一句话 |
|---|---|
| P1-1 | §V-0 的论证措辞要改：`createFallbackResponse()`（schema.ts:426）是**手写字面量、根本不过白名单**——"经白名单重建后 heard 必为 undefined"这个理由是错的（结论对）；正确表述＝**能设 heard 的地方全仓只有一处**（`validateAdvisorResponse` 读模型 JSON），fallback 不经过它所以天生没有；不改的话，将来谁给 fallback 加字段会误以为有白名单在保护。 |
| P1-2 | **`CONFIRM_WORD_SITES` 不能在步 2 登记**——TB2 断言「登记表里没有已消失的调用点」，登记一个还不存在的 nth 会让**步 2 当场红**；正解＝**与新增调用同一个 commit**（步 3/4），步 2 只登记 `SPEECH_RULE_SITES`+指纹与 `REGISTERED_SHARED_SURFACE_RULINGS`（这两个在步 2 是安全的，因为音频说明那一面步 2 就存在）。 |
| P1-3 | **延迟裁决线失败后的出路不成立**：超 +0.7s 就退保守案——可保守案是**两跳、更慢**（实测带信封非流音频调用 1.5-1.8s，再加命令跳），退过去只会更差；延迟失败的正解是缩短回复 / 接受并登记 / 不合并退回现状，**不是 §7**。
〔**本条post-裁定自纠（Opus，§X 落定后）**：原文把 (b) 流首事件也列进了延迟补救，那是错的——
(b) 只让 heard 早到、改的是**气泡**时序，而判据量的是「松手→陈第一个字」，(b) 一毫秒都不动它。
§X-2 裁的「超线 ⇒ V1 不合并、退回现状 Web Speech」比我给的候选更干净，采纳。〕§7 的启用理由是**风险与工期**（§W-3 的喊停制是对的），把延迟也挂上去是把两件事混了——请 Fable 拆开写。 |
| P1-4 | **步 3 打包漏了 ②（pushContext user）与 ⑤（isCancel/isDecline→clearEscalation）**，而这两件**都直接喂 F2（账本头号 demo 账）**：缺 ② ⇒ 语音回合完全不进对话上下文（下一轮只剩单边 assistant）；缺 ⑤ ⇒ 语音说「不用」清不掉升级问句、悬挂 120s 串进下一条命令。两件加起来约 4 行，**并进步 3**；否则步 3 手测会长出一批"假 F2 新账"。 |
| P1-5 | **V1.5 复用要靠命名，不靠运气**（本轮重点②）：接口**没有**被锁死成"只能进 chat 补全"（`provider.chat(messages, opts)` 是通用的，转写中继自己拼 messages 即可），但步 1 若把三个缝写死在调用点里，V1.5 就会复制第二份——请在步 1 就把它们**命名导出**：①`Part`/`AudioAttachment` 类型（不许内联匿名对象类型）②`channelAcceptsAudio(channel)` 能力谓词（不许把 `provider==="gemini"` 抄进路由 handler）③客户端录音成独立模块（不许写在 PTT 事件处理器里）。三处都是"放哪儿"不是"多做"，零工期成本，却是 `placeNameAt` 第二份实现 / `getFrontCenterPos` 重复副本那一族的预防针。 |
| P1-6 | **「没听清，您刚才说的是？」是一句新的引擎罐头台词**，撞 07-22「台词禁死模板」+「对话是唯一界面」两条家法（`ChatPanel.tsx:1563-1570` 那段注释就是上一句罐头被判退的墓志铭）；而且它在语义上自相矛盾——heard 缺席时模型**照样答了话**，`buildGateQuestion` 会拼成「陈的原话 —— 没听清，您刚才说的是？」。**零成本替代**：heard 缺席时直接走**现有** bucket B 文案（`anchor_mismatch` 的「…请确认是哪支部队」或 `default` 的「…请确认或重述」），一个新模板都不加。 |

**P2**

| # | 一句话 |
|---|---|
| P2-1 | §3 有意差异表现在是 **7 条**不是 6 条（正文 5 + V-0 的 fail-closed 第 6 + V-4 Q2 的 Emily prompt 第 7），commit message 登记时别丢第 7 条。 |
| P2-2 | §V-3 第 ③ 条的 n 由 2 抬到 4（我复算 2 跑：1 跑完全逐字、1 跑同款虚词滑），结论与措辞都不用改，只是证据更实。 |
| P2-3 | §W-3「步 1-3 成果按 §7 转保守案上场」略夸大：保守案**用不到** heard 合同（步 2）与步 3 里 heard 那一半；真正通用的是**步 1 + 步 3 的录音件**——喊停决策要按这个真实沉没成本做。 |
| P2-4 | §W-4 给用户的 V1.5 延迟账「净增约 +0.5~1.5s」偏乐观：带信封非流音频调用四跑实测 1.49-2.25s（中位 ~1.74s），转写 prompt 更小会快一些但难到 0.5s；V1.5 自己的小提案里按实测重报一次。 |
| P2-5 | V1.5 的 `/api/transcribe` 将来要挂第三条 4mb 路由（P2-2 只说了两条命令路由），一并记着。 |

---

## §7 给 Fable 的两个问题（未覆盖的取舍，先问原因）

1. **延迟超线的出路是什么？**（P1-3）我看不出"退保守案"怎么能治延迟——保守案更慢。
   是不是本来就想写成"风险/工期出口"（§W-3 已经这么改了），只是 §V-1 P1-1 那格没跟着改？
   如果是，把 §4 里那句"超线退 §7"删掉即可，不必新写条款。
2. **heard 缺席时那句话，是有意要新罐头，还是没注意到已有 bucket B 文案够用？**（P1-6）
   若是有意（想让"没听清"与"不确定是哪支部队"在措辞上分开），我不反对——但那要按
   台词法走一遍（引擎只给事实、话让模型说，或明确登记为例外）；若只是顺手写的一句话，
   直接复用现有文案最省事也最安全。

---

## §8 我核过、没问题、不必再审的

- `normalizeAdvisorForDay7`（ai.ts:609-632）用 `{...data, options}` 展开 ⇒ **不吃 heard**（第四个潜在丢字段点，安全）。
- 三条到达路径全部经 `validateAdvisorResponse` 或 `createFallbackResponse()`，**没有未校验对象逃到客户端**。
- `runKnifeB1`（ab-approval-v4.ts:1199-1240）读 ChatPanel 源码文本数 nth：待搬家的三个函数体内**不含**
  `isConfirmReply(`/`isCancelReply(` ⇒ 搬家不动 nth（§V-0 的复核与我一致）。
- (a) 方案下不存在"气泡未回填期间玩家插入第二回合"的窗口（loading 双闸）。
- §W-5「TTS 零牵连」属实（§3-5）。
- 上轮的基线台架仍绿（R1 亲跑：typecheck 四包 / `--synthetic` 195-0 / `--negctl` 48 条红集合原样 / `--sites` 全绿），本轮未动代码，无需复跑。

---

**审核人：Opus 5 · 2026-08-09 · R2 · 零实施**
**状态：1 个 P0 待闭环 → 不宣布可开工；P0 认下 + 用户点头后即开 worktree 施工。**
