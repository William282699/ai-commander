# 批准合同 v4 提案（approval-contract-v4）— 2026-08-01

> 基线 = main `9499b1b`（pretest-polish-v1 已合）。诊断档 = `ESCALATION_ACK_V4_DIAGNOSIS_20260801.md`（Opus，行号已核）。
> 判退史 = 保护分支 `approval-contract-v3.2-failed-handtest`（v1-v3.2 共 17 实现 commit，四 P0 原文在 `HANDTEST_FAILURE_20260722.md`）。
> 资产源 = 分支 `approval-contract-v1` @ `f83f032`（判退书未批评引擎侧执行器，可捡）。
> **用户三条硬约束（2026-08-01 拍板）：写清楚 / 最小范围 / 不破坏原有结构。**

## 0. 一句话

陈提议增援的那一刻，引擎把「哪十个兵、去哪」拍成快照钉在案上；玩家光杆一句「可以」照单发兵，
插话追问不作废、聊完再点头照样算数；LLM 全程负责说话与踩刹车，**永远碰不到油门**。

## 1. 立场（用户设计哲学，2026-08-01 定调）

**播音员是没有执行力的陈；我们要造的是判断能落地、但公章锁在抽屉里的参谋长。**

- LLM 保留并将来只增不减的真决策权：**填单权**（模糊命令派谁/几个/落点，Bucket A 原样保留）、
  **提案权**（何时开口、推荐谁）、**顶撞权**（判断执照）。养成路线（第 12 级）就长在这些决策上：
  style 参数调制 Bucket A 的保守/激进、提案时机、preflight 顾虑排序——每个玩家的陈手感不同。
- 收归机器的只有一格：**核章**——「你刚那句话算不算点头」。这不是决策是文书，三判退全死在这一格，
  且这一格里没有人格可言。
- 今天的陈才是讲话 NPC：提议永远变不成行动。合同是给他的判断装上执行力，不是剥权。

## 2. 机制设计

### 2a. 快照合同（核心数据结构，引擎侧唯一新增）

登记刻 = 陈的提议消息上屏同一 tick（原子，不存在"后台旧合同"物种）。内容：

```
{ gNumber: "G7",             // 临时番号，同局单调递增永不复用（防"旧G7新G7"= P0-1 换皮）
  unitIds: [...],            // 铸号即冻结的名单快照 —— 全案唯一真相源
  targetFrontId, anchor,     // 沿用 f83f032 的 CapturedReinforcementProposal 形状
  mintedAt, lastActivityAt } // 滑动钟用
```

### 2b. 生命周期（用户 2026-08-01 裁定：一次性 + 反对死 TTL）

| # | 注销触发 | 裁决方 |
|---|---|---|
| 1 | 执行成功即烧号（一次性） | 引擎 |
| 2 | 新提案顶号（单槽显式替代，陈台词言明） | 引擎 |
| 3 | 滑动冷场钟：玩家每发一句话归零，连续 ~90s 静默过期；硬上限 300s（两数字皆手感旋钮） | 引擎 |
| 4 | 消费时名单核空：死亡/已被玩家后续命令另派的剔除，余部出发+真实回执；空则作废+陈一句人话 | 引擎 |
| 5 | 陈回话时标记 withdrawn（长官否了/话题走了）→ 撤案 | **LLM（唯一参与点，判错方向安全=落空重说）** |

**hold 是默认态**：追问、咨询、闲聊合同纹丝不动——「提案→追问→拍板」是最自然指挥流，不得误杀
（v4 设计过程中已否决过"玩家插话即作废"方案，此为其墓碑）。

**实现形状（防做复杂，用户 2026-08-01「五触发器会不会打架」审查后钉死）**：单槽一个状态变量 +
三个检查点，**零定时器零后台任务**——#2 挂在提案上屏、#5 挂在陈回话落地、#3/#4/#1 是光杆确认
到达时同一动作的三道工序（看表→核名单→执行烧号），全部惰性求值。UI 单线程按序处理，无竞态；
#2/#5 都是清槽，幂等；唯一时序细节=点头与顶号同秒到达（单槽固有，非触发器数量问题），兜底=
执行回执必复述。五条触发器是给 bench 写的可枚举规格（每条一对正/负对照），不是五个机器；
300s 硬上限是唯一纯保险项，审程可砍。

**续盘（压落空成本，用户 2026-08-01「重说一遍也烦」）**：光杆确认落空且合同系 30s 内刚过期/
刚被撤时，陈不给干巴巴引导句，而是续盘再问一次（「增援案刚过期——还派东北那群去中央吗？」，
数据取自刚烧的合同，仅供说话）；点头即按当下活人**重新登记一张新合同**再消费——续盘不复活旧号，
不破一次性。重说成本从"重组一句完整命令"塌缩为"再点一次头"。
**落空率是验收指标非形容词**：真模型探针加"落空率"专项（提案→各干扰路径→光杆确认的消费成功率），
门槛定在手测前由取证局实测基线后拍板（诚实：v4 设计阶段没有实测数字，只有结构论证——冷场钟是
唯一"哑巴落空"窄缝，其余触发器要么可见要么正确）。

### 2c. 裁决权分立（三判退的直接解药）

| 转移 | 裁决方 | 触发 |
|---|---|---|
| → execute | **只有结构门，LLM 无权** | ① 光杆确认（复用 preflight 现成 `isConfirmReply` 判定）消费在案合同 ② 玩家点名走正常命令闸 |
| → cancel | 结构为主 + LLM 安全方向 | 生命周期表 #1-#5 |
| → hold | 默认 | 其余一切输入 |

### 2d. 四 P0 逐一对账

- **P0-1 答A执行B**：绑定零 LLM 判断——光杆确认只消费"当前在案且陈刚复述过的那一单"；单槽+
  番号不复用+消费时复述回执（「G7 十个单位即刻出发驰援中央战线」，即使万一绑错也当场可见）。
- **P0-2 二次执行/权限旁路**：**光杆确认词永不进入普通命令链**——有合同消费合同，无合同得一句
  引导（「您说『派谁去哪』我才能动」），零执行。Bucket A 对真命令（有动词有目标）原样保留。
- **P0-3 目标类型缺失**：**围栏**——设施类危机（"夺回中央前哨"）本刀不登记合同，退回现行行为，
  且 bench 断言钉死围栏（设施类危机 → 合同表必为空）。注：capture 级（7-30 收口）已把
  attack_move+targetFacilityId+到岗驻防的引擎地基建好，拆围栏=v4.1 传字段小刀，本刀不做。
- **P0-4 合同劫持咨询**：hold 默认态下咨询走正常对话，合同不占任何回合；LLM 撤案标记只在
  陈自己回话时顺带产出，不抢答。

### 2e. 番号点名入口（刀B，合同过手测后开闸）

board 群行加后缀 `东北方向第一未编组群(G7)` + prompt 一行（fromSquad 接受 G 番号照抄）+
`isValidTarget` 认活跃番号 + resolver 走快照名单不经全局池。LLM 只抄 2-3 个字符，与现有
squad 把手同族；失败模式=闸响亮拒单，非错派兵。**对人隐藏对机器公开**：陈与玩家全程说人话，
番号只活在 prompt 与 JSON 里，零 UI（对话是唯一界面家法）。

## 3. 改动清单（最小范围，不破坏原有结构）

**捡现成（判退书未批评，照搬）**：
- `packages/core/src/capturedReinforcement.ts`（f83f032 +249 行）：登记快照、执行只遍历捕获 ids、
  *no resolveIntent no fromSquad no backfill*、走 applyOrders 唯一入口、回执报真实出发数
- `frontEscalationPayload.ts` +6 行 memberIds（§2c 缺的把手）
- `ab-approval-contract.ts` 743 行 bench 骨架（判据按 §5 重写）

**重写（四 P0 全在此，~80 行目标）**：`ChatPanel.tsx` 绑定层——登记（消息上屏原子）、
生命周期五触发器、光杆确认消费、引导句。**不捡** v1 的 246 行绑定与 GameCanvas+messageStore
共 203 行 UI 小条（撞「对话是唯一界面」，判退 P1 的多真相源病灶）。

**禁改**：resolveIntent/tacticalPlanner/applyOrders 本体 · Bucket A 判定 · preflight 合同
（`pendingContractRef` 一直好用，两套合同并存互不相认，消费判定各自独立）· 一切 capture/
战斗/移动语义 · 现有八闸+ab-pretest-polish 断言。

## 4. 取证先行（硬前置，不取证不动刀）

信封截获（`reference_pumped_frame_testing` 手法）抓两发 intent JSON 作 RED 基线：
① 02:14 形状——`invalid_intent_fields` 到底毙在哪个字段（诊断档 §2a 唯一未落地推断）；
② 02:32 形状——无 fromSquad + quantity 瞎猜。取证结果决定 prompt 侧 *Copy VERBATIM* 行
是否需要同步措辞（若毙点不在 fromSquad，§2a 推断作废，回炉再诊）。

## 5. 验收（家法：判据测效果不测措辞）

- **bench**：`assignedUnitIds` 逐一 == 登记快照（数人头不读台词）· 落点坐标 == anchor ·
  负对照=关掉合同重跑，消费断言必真 FAIL · 围栏断言（设施类危机零登记）· 生命周期五触发器
  各一条正/负对照（含：追问三句后点头必执行、冷场过期后点头必落空得引导句、另派重叠部队后
  消费只出剩余人数）· 光杆确认无合同时零执行
- **真模型探针**：升级问句 → "可以" ×N 方向正确率；追问两句再"可以"的消费率
- **手测定生死（判据提前写死）**：①提案→光杆"可以"→数实际出发 ids==快照 ②提案→追问两句→
  "可以"→照常执行 ③提案→另下无关命令→合同不劫持、无关命令正常执行 ④提案→冷场 2 分钟→
  "可以"→落空+引导句 ⑤设施类危机→零合同 ⑥全程台词自然（判退 P1：无调试式回执刷屏）
- **退路（预先定死）**：手测不过当天回滚，5 分钟止血=prompt 一行让陈在收到光杆肯定词时
  说人话引导；带病外测但话术诚实。

## 6. 流程

worktree 另开（不污染 main，家法）；一步一测一 commit：①取证+RED → ②引擎侧捡装+bench GREEN →
③ChatPanel 绑定重写+生命周期 → ④真模型探针+手测。Opus 交叉审（重算数字+亲手负对照）。
规模预估：core 捡现成 + 绑定 ~80 行 + bench 判据改，总敞口两三天——比 v3（17 commit 三版本）
小一个数量级。手测 PASS 才合 main；刀B 番号随后单独开闸单独手测。

## 6b. 审后修订（2026-08-01 Opus+用户联审，Fable 复算全部属实后认账）

**两个数字是错的（先于一切机制问题）**：①68s ETA 量的是 frontCenterPos 几何中心
（`frontEscalationPayload.ts:346`，差一个数量级；修法 `battleAnchorFor` 在雪藏分支 `5a1f195`
躺着，71 行，当年只打进执行器没打进 V1b payload——且 commit 自证旧危机卡路线本来就故意用
交战守军质心，捡它=恢复语义非新设计）②"撑 7 秒"=我方一枪不放的全灭倒计时
（`crisisResponse.ts:250`，注释自认故意悲观）。两数字量纲都不同却并排喂给司令决策。

**重排序（Opus 提，Fable 第一性原理认账：五 check 的全部存在理由是"可以"无内容；
番号一上题面降维，合同从主刀降为"给核章上锁"）**：

| 序 | 内容 | 性质 |
|---|---|---|
| 1 | ETA 锚点：捡 `5a1f195` battleAnchorFor **打进 V1b payload**（board 群行同源自动受益） | 纯 bug 修复，零设计争论 |
| 2 | 临时番号（原刀B，§2e）+ 一次性/惰性过期（原五触发器塌缩为消费时一次检查）+ 绊索：光杆确认无在案升级→引导句永不进 Bucket A | 直对主因 |
| 3 | 诚实闸：eta > tCollapse 不提该案；tCollapse 台词用法待用户拍（Fable 一票：内部阈值照旧、台词去裸秒数——"数字须指回可辩护字段"铁律） | 几行判定 |
| 4 | 刀A 合同+五 check：**雪藏待咬**——光杆确认执行全埋日志，外测误绑率超阈值才复活 | 上锁，等证据 |

原 §2-§3 设计全文保留：刀A 若复活照此实施；番号生命周期规则（一次性/过期/续盘）自 §2b 继承。

## 6c. 二轮联审落账（2026-08-01，Opus 事实更正 + 用户拍板 + Fable 复核补挖）

### 6c-1. 事实更正与全消费图（A1 属实且不全，Fable 复核又补三处）

坏数爆炸半径的**完整**消费图（bench 覆盖以此为准）：

| 消费点 | 数 | 性质 |
|---|---|---|
| 升级问句 payload `etaSec`（"68秒能到"） | ETA | 说话 |
| `commanderPresence.ts:103/:122` `best_help=…(eta≈Ns)` | ETA | 说话 |
| `director.ts:379` 报警闸 + `:381` urgency 节奏 | tCollapse | **内部（不动）** |
| `director.ts:382` beat tSec（"预计还能坚持7秒"源头）★补挖 | tCollapse | 说话 |
| `director.ts:708` `estimatedCollapseSeconds` 事实包 | tCollapse | 说话 |
| `commanderPresence.ts:111` `survival≈Ns` + `:212` "约N秒内承压加剧"★补挖 | tCollapse | 说话 |
| `decisionReview.ts:260/410/483` 复盘基线/结局判定/跨线行 | tCollapse | 说话（经 7e 复盘语音） |

★另一处补挖的结构事实：`frontCollapseSeconds` wrapper 在 `decisionReview.ts:149` 与
`commanderPresence.ts:149` 是**同名复制的两份**（公式真相源在 crisisResponse 唯一，wrapper 已分叉）
——刀3 实施时新数放进 `assessCrisisEscalation` 返回结构，两份 wrapper 各改一行读新字段，勿再复制。

**A2 归因更正 + 术语钉死**："1 兵案"（02:32）的输入是**带尾确认**（"可以，派他们去"），
`isConfirmReply`（normalize 后整句精确匹配）按定义不命中——绊索管不到它，其解法=番号（刀2，
LLM 写 `fromSquad="G7"` 过闸走冻结名单）。绊索管的是**裸确认**（整句命中确认词表）无在案升级
的情形。两词从此为正式术语：**裸确认**走绊索/合同消费，**带尾确认**走正常命令链靠番号消歧。
bench 必须两条路各测（带尾确认那条正是用户真实说过的话）。

### 6c-2. 用户拍板（B）：tCollapse 保留裸秒数，把它算对

否决"降定性档位"（两据：Step B fix1 判例——形容词=prompt 替引擎下结论；态势板立论=传候选+
代价不传结论，数字是代价形容词更接近结论）。采纳"两个数两个用途"，**口径原则一句话：
对内触发用悲观钟，对人说话用互射钟**。

实现形状（C，Opus 查证 Fable 复核）：`crisisResponse.ts:226-251` 循环已有 defenderHP/
defenderDPS/enemyDPS，补 enemyHP 一行 + 互射推演十来行；`tCollapse` 零字节动继续喂报警闸。
新数前提为真（双方对射、四输入皆指回真实字段）；**诚实披露仍不算**：地形防御加成/兵种克制/
射程内外/增援途中/玩家撤退——是"没算进去"非"算反了"，台词措辞不得宣称精确（"约"字级）。
**★ 追加披露（实施方 2026-08-02 报告，用户裁定 Lanchester 不做）**：敌方 DPS 不随其伤亡衰减，
故**输面念出的秒数恒等于旧悲观秒数**（两者都是 defenderHP/enemyDPS）——我方输出只改变**判定**
（守不守得住），不改变**时长**。刀3 的实际收益＝**赢的时候不再谎报倒计时**，而非"输的时候数字更准"。
辩护：真输掉对耗时我方火力未能及时削减对方，HP/敌DPS 即合理死亡时间；前提从"我们一枪不放"（假）
换成"我们输了这场对耗"（真）。若将来要输面也变准＝加敌 DPS 衰减推演（Lanchester 型），另立一刀。

### 6c-3. 行为变化预期（D，写死防手测误判回归）

算准后会出现旧公式结构上不可能的态："报警闸响（悲观钟<阈值）但互射钟说守得住"。诚实闸
合并升级为：**互射钟说守得住 → 根本不提升级案**。预期后果=升级频率下降（用户记录：敌压
强度已足缺的是花样——方向正确），手测与 bench 一律按"行为会变"写：正对照=互射钟判输→
照常升级且念新数；负对照=互射钟判赢→零升级零提案。

### 6c-3b. M9/M10 裁定（2026-08-02，实施方报告冲突后用户拍板 A 案）

刀3 落地时 `ab-commander-presence` 68/0 → 66/2，唯一被撼动的闸。实施方挖出算术：
构造局 `boundaryMood([40,40,40])` ＝ 我方 3×40HP/DPS12 vs 敌方 1×60HP/DPS4 —— 悲观钟
120/4=30s，互射钟 60/12=**5s**。**三个步兵打一个步兵，敌人 5 秒死光，旧公式播报"还能撑30秒"**；
M9/M10 把这句话钉成了验收标准，即刀3 要删的那个谎本身。

与 §3 禁改清单（"现有八闸断言禁改"）冲突。**裁定＝A 案：改写 M9/M10**，三条件：
(a) 边界测试职责用**输面**构造局保留（1v3 型：我方 1×360HP/DPS4 vs 敌 3×60HP/DPS12 →
tWeDie=30s、tEnemyDies=45s、holds=false、spoken=30，恰在 CRITICAL_COLLAPSE_SEC 边界）——
不许只删；(b) 赢面 3打1 必须**不**出倒计时，进负对照（新增 M9b）；(c) commit 引 §6c-3+§6c-3b。
禁改清单的立法意图是防**静默回归**；本次是有档可查的口径切换，故豁免。

### 6c-4. 复盘口径（E，Fable 意见：同刀切换，不后议）

三个理由：①复盘不是哑巴——`:483` 的 collapseSecondsNow 与 `:410` 的结局分类经 7e 复盘
语音说给玩家；刀3 只换台词不换复盘，上线当天同一时刻两个"撑几秒"（决策时互射钟、复盘引悲观
基线），口径分裂立刻咬不是将来咬。②白捡一个盲区修复：旧谓词"接触即有钟"使 `:483` 的
`continue` 把"本来压着打、后来开始输"的战线永远滤掉（基线非 null 即跳过）；换互射钟后
"压着打"=null，翻盘才非 null——**"北线本来赢着打现在输了"第一次变得可报**。③零额外成本：
新字段进 assessment 返回结构，两份 wrapper 各一行。复盘行为变化同入 D 的预期（eased 更稀
更真、pressure_emerged 新增翻盘报告），bench 正负对照覆盖。

### 6c-3b. 刀3 实施期两裁定（2026-08-02，Opus 撞规则冲突停机请裁，Fable 拍）

**① M9/M10 断言改写＝A 案批准（§3 禁改清单的例外，有档可查）**：ab-commander-presence
M9/M10 钉的是旧口径的谎（实测构造局：3 步兵 120HP/12DPS 打 1 步兵 60HP/4DPS，敌 5 秒团灭，
旧公式播"撑 30 秒"）——§3"现有闸断言禁改"防的是静默回归，此处是 §6c-3 预告过的行为变化，
后者更晚更具体，胜。三个条件：(a) M9/M10 原职责（mood critical 边界测试）必须用**输面构造局**
保留，不许只删；(b) 补负对照：赢面（3打1）必须不出倒计时；(c) commit message 引 §6c-3+本节，
§3 视为已加脚注（行为面断言按 6c-3 豁免须逐条附裁定出处）。

**② Lanchester 衰减推演＝不做，简单钟收口**：Opus 诚实报告简单模型下输面口播秒数==旧悲观秒数
（刀3 实际收益=**赢时不再谎报倒计时**，非"输时数字更准"）——此性质陈述批准入档，它是 §6c-3
"升级频率下降"预期的孪生表述。输面前提从"我们一枪不放"（假）换成"输掉对耗时敌 DPS 不衰减"
（偏悲观但可辩护），**加进 §6c-2 诚实披露清单**：敌方 DPS 不随伤亡衰减（输面秒数偏悲观）。
Lanchester 记 P2 挂账，凭外测手感裁决，本刀不做。

### 6c-3c. 刀2b 活体冒烟 P0 与裁定（2026-08-02 深夜）

**P0**：番号在闸层不被承认——`detectStaleSquadRefs`(:214，调用 :1530) 与 `isValidTarget`(:136)
两道上游闸只认 squad/commander，`fromSquad="G1"` 判 stale/invalid，永远走不到 :1817 翻译层。
失败方向安全（响亮拒绝零执行）但功能不通。**活体冒烟同时实证三刀核心全部工作**：ETA 15-16s
非 68s、19s 存活 vs 15s ETA 提案首次可执行、G1 铸号/注入正确、**LLM 照抄 G1 成功**（全链唯一
靠模型自觉的一环）、绊索正确。

**裁定（Fable）**：抽 core 纯函数（Opus 倾向的 b 案），一条细则：**闸层对番号形引用
（isTicketRef 命中）一律放行不判生死**——票据生命周期的裁决权独占于翻译层
（unknown/expired/burned 的人话拒绝已在 resolveTicketReference，闸层再判=票据有效性的
第二真相源，正是本 P0 的病型）。幻觉号 G99 同理放行后被翻译层响亮拒绝，安全不变。

**台架盲区入档（家族第三例）**：bench 测了孤立件与执行路径，唯独没把带 G 号的 intent
从闸层推到底——同族先例=capture-stall 账⑦（reportEvents 排水/GameCanvas 接线）。
方法论：**把判断下沉到台架够得着的层，是把盲区变成断言领土的唯一解**。谓词入 core 后
补端到端断言：活跃 G1 → 不 stale 不 invalid 且 assignedUnitIds ⊆ 快照；burned/expired →
闸放行+翻译拒绝零派兵；未铸 G99 → 同前。

### 6c-5. bench 覆盖矩阵（F4 复核结论：A1 把复盘归刀1 是口径归错）

复盘消费的是 tCollapse 不是 ETA，归**刀3** 覆盖面。矩阵：
- **刀1（ETA）两个断言面**：升级 payload `etaSec` 锚到战斗质心（probe 逐点：交战守军质心≠
  frontCenterPos 的构造局）+ 态势板 `best_help` eta 同值——同源 buildReinforceOptions
  仍双断言，防将来分叉；负对照=battleAnchorFor 摘除必真 FAIL。
- **刀3（tCollapse）六个说话面 + 两个内部不变面**：说话面=director:382/:708、presence:111/
  :212、review:260/410/483 全换新数各一断言；不变面=director:379 报警闸与 :381 urgency
  仍读悲观钟（负对照：改错面必被抓）；另加 D/E 的行为正负对照与 6c-1 的两条确认路径。

## 8. 实施清单（给实施方的改法，按刀独立可单砍；步骤零=取证）

**刀0 取证 ✅（2026-08-02 Fable 完成，全档=`APPROVAL_CONTRACT_V4_RED_EVIDENCE.md`）**：
泵帧复现升级问句（68s 几何中心 ETA 当场再证）+ 信封截获两发。**三大发现**：
① digest 明令「group labels are NOT valid fromSquad」——LLM 服从禁令后即兴发明错法，
  三局三样（用户局=整单被毙；探针A 裸确认=fromFront=toFront 自我循环单，过闸就地挪 6+1 兵；
  探针B 带尾确认=零来源单，全局池 6+1 兵），台词三次都宣称「调动东北群」。
  §2a 推断（fromSquad 被毙）修正为形状之一非恒定路径。
② **RED 判据效果级**（字段形状是骰子）：东北群快照 ∩ assignedUnitIds = ∅ 而台词宣称调群。
③ `escalateId` 已随请求进服务端（实证）——刀2 登记线索现成；刀2 的 prompt 改动本质=
  把那行禁令改写为「G# IS valid」，病灶与修法同行对齐。

**刀1 ETA 锚点**（纯捡装）：
1. `git show 5a1f195` 取 `battleAnchorFor`（交战守军质心→战线内守军质心→frontCenterPos
   三级兜底，71 行）——**新家放 `crisisResponse.ts`**，与 frontCenterPos 同文件成唯一锚点真相源
2. `frontEscalationPayload.ts:346` 一行：`anchor = battleAnchorFor(state, front)`（etaSec 随
   anchor 自动变准；battleBoard groups 与 presence best_help 同源受益）
3. bench：构造局逐点（交战点 (360,105) vs 几何中心 (263,96) 型），断言 payload etaSec 与
   presence best_help 双面；负对照=换回 frontCenterPos 必真 FAIL

**刀2 番号**（核心=翻译不改闸；**Fable 裁定 2026-08-02：走 B 案——番号铸在升级候选快照上，
不挂板子行**。Opus 实测抓获 §8 原文（A 案形状）的静默错派坑：battleBoard 用
`buildReinforceOptions(state, null)` 不排线内单位、升级候选用 `(state, front)` 排除——
同标签成员可差一倍（跨界构造局 10 vs 5），A 案=承诺 5 执行 10 且只验标签的 bench 抓不到。
B 案使 memberIds 恰为陈口播那批（同一次 builder 调用），承诺==执行 by construction；
另一独立理由：板子行是逐帧重算的临时聚类，铸稳定号需跨帧簇身份追踪——A 案的隐藏子问题，
B 案天然回避。代价入账：主动"派G7"仅在提案在场时可用，板子行把手留 v4.1/Preflight V2。
**双处禁令（Opus 补漏，digest.ts:309 + battleContext.ts:192）在 B 案下零字节动且仍为真**
——群标签依旧非法，番号不经这两节，只随 ACTIVE_ESCALATION 携带；v4.1 若做板子把手须两墙齐拆）：
1. 铸号：**升级候选快照构建时** mint `G{n}`（同局单调递增），冻结该候选 memberIds
   （捡 f83f032 +6 行），挂 mintedAt；一次性+惰性过期管生命周期
2. prompt：番号随 `---ACTIVE_ESCALATION---` 注入（RED 档实证此路可达），附一行明确许可
   「本案编号 G7，fromSquad="G7" 即调陈所述那批」；digest/battleContext 两处禁令不动
3. `ChatPanel.tsx` 翻译层（唯一新逻辑，~30 行）：intent.fromSquad 命中活跃番号 → 惰性检查
   （过期/已烧/核空→落空+续盘）→ 翻成 `selectedUnitIds=快照活人` 调 resolveIntent——
   **已核：`tacticalPlanner.ts:168` 自带 selectedUnitIds 硬约束参数（Day 10.5 框选），
   resolver 零字节动，禁改清单成立**；执行成功烧号
4. 绊索：命令链入口 `isConfirmReply(input) && 无在案升级` → 引导句直接返回不发 LLM；
   裸确认+有在案升级 → 照常走 LLM（其执行埋日志记误绑率，供刀A 复活裁决）
5. bench：裸/带尾两路各测；番号过期/烧号/核空三态；翻译后 assignedUnitIds ⊆ 快照；
   **★跨界构造局必测**（Opus 抓坑转化的判据）：5 线内+5 线外同簇，断言执行恰=候选快照 5 人
   （≠板子 10 人），负对照=故意从板子行取名单必真 FAIL

**刀3 互射钟**（一处公式多处换读）：
1. `crisisResponse.ts:226-251` 循环补 enemyHP 一行；互射推演 ~10 行（连续 DPS 对耗：
   tWeDie=defenderHP/enemyDPS，tEnemyDies=enemyHP/defenderDPS，tEnemyDies<tWeDie=守得住）；
   新字段进 `assessCrisisEscalation` 返回结构；`tCollapse` 零字节动
2. 换读六个说话面：`director.ts:382`（beat tSec）/`:708`（estimatedCollapseSeconds）、
   两份 wrapper（`decisionReview.ts:149`/`commanderPresence.ts:149`）各一行读新字段
   （presence:111/:212 与 review:260/410/483 随 wrapper 自动换）；台词带"约"
3. 诚实闸并入升级构建：互射钟守得住→不提案；eta>互射钟→该候选不列
4. bench 照 §6c-5 矩阵：六说话面+两内部不变面（:379/:381 仍悲观钟）+D/E 行为正负对照

**刀4 合同**：雪藏，凭刀2 日志的误绑率数据裁决复活，图纸=§2。

## 7. 三行人话

1. 陈提议的那刻，机器把"哪十个人、去哪"写在案上；你说"可以"就照单发兵，问几句再点头也算数。
2. 你不点头兵永远不动：插话不误杀、冷场自动过期、同一单绝不执行两遍、执行时陈原话复述让你看见。
3. 陈照样自己挑兵、自己提案、敢顶嘴——越用越像你的参谋长；只有"你到底签没签字"这一件事归机器管。
