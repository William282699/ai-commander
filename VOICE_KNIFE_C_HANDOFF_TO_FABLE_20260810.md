# 刀 C 交接单 · 给 Fable（2026-08-10 晚）

> 你上次的裁定（提案 §10）之后发生的全部事情，一页看完。
> 现场：worktree `~/MyProjects/AI Commander-voice-input` @ `e26165c`，工作区干净，
> 全家硬线 **22/22 全绿**，main 零字节。三个 commit 堆着等你审。

---

## 1. 你批的，我做了什么

| commit | 内容 |
|---|---|
| `09cade2` | C1+C2+C3：麦克风开局握住整局不放、空闲 `track.enabled=false` 数字静音、🔴 只跟 `press()` 真进 collecting 才亮 |
| `3324c63` | 兜底文案：**纯删除**「以下为默认方案。」（砍卡法后无方案可展示＝假话），下文由 bucket B 现成问句机器接，不新造罐头 |
| `e26165c` | 设备开启耗时回传服务端，与 `voice_heard` 落同一行 |

**你的两个附加条件都落了**：①负对照开关 `?novoicewarm`（**只改 arm 的时点**，采集件
一个字节不变）②`arm()` 自记设备开启耗时。

**台架 +18**：C1-C16（采集状态机结构断言）+ N64/N65（兜底句）。最要紧的三条＝
C2「没按下→帧一律丢弃且计数不涨」、C3「空转 200 帧后仍一个样本没留」、
C7「松手后 armed 仍为 true」。**结构断言，不冒充效果断言**（照你 §6 的分账）。

---

## 2. 两臂真麦实测（用户同一次坐下跑完，日志自带 `warmup:true/false` 标臂）

### ★①【不依赖任何人的记忆】§4 那个 X 填上了

摘刀臂每次按下重开设备，六次全记（同 origin、权限已授予、**无授权框污染**）：

| | 六次 | 中位 |
|---|---|---|
| `gumMs`（申请到设备＝G1） | 340/277/283/250/247/295 | **280ms** |
| `firstFrameMs`（到第一帧真流起来） | 487/433/435/378/446/441 | **438ms** |

⇒ **旧行为每按一次，最前面约 0.44 秒物理上不存在。**
中文 4-5 音节/秒 ⇒ **438ms ≈ 2 音节**，与三个修前坏样本丢的量（「中央」2 字、
「Aiden」2 音、「南线前哨守军」前几音）**在音节数上对齐**。机制、数字、现象第一次凑齐。

> 预热臂那个 `gumMs 4257` **不可作盲区读**（新 origin 首次弹授权框，含用户点"允许"
> 的反应时间）。它的意义只在于：这笔钱付在**开局那一下**，不在按下那一刻。

### ★②【不依赖记忆】最脆的音活没活

| 臂 | 回合 | 句首带语气词/改口（嗯、呃、啊、"北线啊…北线前哨"） |
|---|---|---|
| 预热（修复开） | 8 | **3/8 有**，且完整录下 |
| 摘刀（修复关） | 6 | **0/6** |

句首语气词是最先被吃的那批声音，有无与"记不记得说过什么"无关。
**单句最强对照**：修前「南线前哨守军全部出动」→「**全套的收军**全部出动」；
修后同一句 →「(嗯陈)南线前哨的守军全部出动去中央前哨」**逐字全对**。

### ③【依赖记忆，如实降级——请你裁这一条】

你 §10-④ 定的「逐句手读、只数掉头音」，**这次只能部分执行**：用户自然玩、没照稿念，
事后回忆不起原话（原话：「应该是，我忘记了，但差不多那个意思」）。所以这两条我
**只报作"与盲区一致"，不作已证**：

| 疑似掉头（摘刀臂） | 预热臂同类句 |
|---|---|
| 「**前哨**是否需要」（疑似丢「中央」） | 「中央前哨是不是需要支援呢」完整 |
| 「全员出动去走中央沙漠小路…」（疑似丢句首「Aiden」） | 「**Aiden**，呃，走，中央沙漠小路…」完整 |
| 「我看不纠正吧」（整句对不上原话） | — |

**方法教训（建议进方法资产）**：自然玩生态效度更高，但**牺牲逐句基准真值**；
要两者兼得只能在采集端记下"长官打算说什么"——那不存在。
**下次同类验收：要么念固定稿，要么认下这半判据做不了，别事后靠回忆补。**
本次仍站得住，是因为①②压根不依赖记忆。

---

## 3. 我自己要报的三处偏离/新增（请你逐条裁）

1. **时序偏离**：我没让用户先跑"修前 10 句基线"就开工了。理由＝**修前的红你已裁定
   由昨日三样本充当**，而"摘掉修法"的负对照与"修前基线"**是同一次测量**；
   两臂背靠背（同一天/同一支麦/同一副嗓子）比"今天测旧的、明天测新的"对照更干净。
   我判这是**加强证据不是放宽**，但它确实动了 §6 的顺序，报你。
2. **超出你条件②的范围**：你说的是"console 一行"。我把它**扩到回传服务端**
   （请求体加 `voiceDiag`，纯观测、不进任何判定、形状不对就当没有；
   `rejectCommandBody` 一个字没动，400 矩阵 N15-N28 全绿未变）。
   理由＝用户找不到 DevTools 那一行，而"要长官去控制台捞证据"正是 §8 那笔账的形状。
3. **手测服务必须起在会话自己的 preview 名下**（我这次另起 3022/3023）——
   否则 `preview_logs` 够不到，§8 那笔账会第二次发生。这条建议写进工装规矩。

---

## 4. 挂在你那儿等裁的（按轻重）

1. **L6b 仍红**（上一轮就问了、你这次没答）：spoken 含英文字母 8/21。根因在信封不在
   措辞——那支部队唯一身份是 `I1(Aiden)`，中文名不存在（对照：El Alamein 被译成
   「阿拉曼」＝**能译的它译了，译不了的它照念**）。两条路：(a) 判据改口径，放行信封
   专名、只禁番号与能译的英文；(b) 引擎在信封里给部队中文代称（真解，动信封，另一级）。
2. **§7-② 陈那两笔台词账现在可以重审了**：当时的理由是"输入是脏的时候不审嘴"。
   刀 C 之后**输入干净了**，那两笔（拒办口吻／屏上复读 n+1）该不该开刀，条件已具备。
3. **新账**：「战狼点」→「狼店」（预热臂 #5，句**中**不在句首 ⇒ 不是设备盲区，
   是转写本身错的；台架 --live 20/20 全对，真麦此次 1 次未中）。
4. 14 个语音回合 **heard 无一缺席**，fail-closed 一次没触发（顺带的好消息）。

---

## 5. 全文在哪

- 提案＋你的裁定＋两臂结果：`VOICE_KNIFE_C_HEAD_BLINDSPOT_PROPOSAL_20260810.md`
  （§10 是你的裁定，**§11 是这次实测**）
- spoken 层那份完整手测（延迟 A/B、听错清单、四幕重走）**还没做**——
  那一份才是决定 V1 能不能合 main 的，排在你审完之后。

**Opus 5 · 2026-08-10 · 22/22 全绿 · main 零字节 · 等审**
