# Marcus 换脑 v1 · 半页 mini-proposal（开工前报审）

> ## ⛔ 结局：**手测判退，已回滚**（2026-08-13）
> 长官原话「**这个 marcus 太干了**」。`.env` 已改回 `LLM_PROFILE_OPS=deepseek`。
> **下面全文是当时的提案，保留原样供复盘——但它没有落地，别照着读成现状。**
> 机器面数据全部有效且留档（延迟 924→511ms、Q1 编造 2/12→0/12、其余两题两边皆 0），
> 判退判在**台架量不到的那一格**：像不像参谋。详见 ROADMAP 与 LEDGER D1。

> 用户裁定 2026-08-13：Marcus 从 DeepSeek 换 Gemini（理由＝语音上线后延迟从演出变冷场）。
> 基线 main `3a288ad`（=origin），全家 24/24。**未动一行代码。**
> 排在嗓子刀之前。

## ① 触点清单（我全查了，四处，其中一处不在原清单里）

| # | 触点 | 现状 | 换脑要动什么 |
|---|---|---|---|
| 1 | **模型路由** `providers.ts:373-386` | `getProviderConfig(channel)` 读 `LLM_PROFILE_<CHANNEL>`；ops 由 `.env` 的 `LLM_PROFILE_OPS=deepseek` 决定 | **改 `.env` 一行，零代码**。机制本来就是为这个建的 |
| 2 | **人格 prompt** `ai.ts:363` `SYSTEM_PROMPT_MARCUS_V2` | 独立常量，`ai.ts:31` 按 `channel==="ops"` 分流，与共享面早已分家 | **原样搬可行，零改动**。它不含任何 deepseek 特化措辞（我逐段读过：没有"请用中文思考"这类模型钩子） |
| 3 | **判断执照** `ai.ts:385` | 在 Marcus 块里**是活的**（陈那份已被 G 刀合同④吸收删掉）。"deepseek 未测"指的是**这条执照在 deepseek 上从没被量过** | **不改文本**。换脑后它第一次在 Gemini 上运行——所以它必须**进对照题**（见 ③ 的 Q2），否则等于把一条从未验证的规则换到一颗从未验证的脑子上 |
| 4 | **★语音白名单的注释会变成假话** `providers.ts:437-443` | `VOICE_INPUT_CHANNELS=["combat","logistics"]`，注释写着排除 ops 的理由是「**ops 用的是 deepseek 那颗脑子**」 | 换脑后这句**当场为假**（carve-out：旧行因线变而为假 ⇒ 必须改）。**但白名单本身不动**——ops 仍不收音频，只是理由要换成真的那条 |

**★第 4 条的裁定请求**：换脑后 ops **技术上具备**收音频的能力（配置闸 `provider==="gemini"` 会放行），只被显式白名单挡着。
**我主张 v1 不给 Marcus 开语音输入**——那是范围外的新能力，且 Marcus 的嘴还没上说话合同（D1）。
只把注释改成真话 + 留一行说明"要开是另一刀"。**有异议请裁。**

## ② 回退开关

**不需要设计，它已经存在**：`LLM_PROFILE_OPS=deepseek` ↔ `=gemini-2.5-flash`，**改一行重启即可**，代码零删除。
本刀因此**不新增任何 flag**——多一个开关就多一处要维护的真相源。
`.env` 里那段注释同步补一句"ops 换回 deepseek 就改这行"。

## ③ 换前后同题对照（判据测效果）

**方法**：同一份真信封、同一批题、各跑 N=12，**只换 `LLM_PROFILE_OPS` 一个变量**。
台架新增 `scripts/ab-marcus-brain.ts --live`（不进全家扫描，花配额）。

**题目必须含他的三笔旧病**（判据都咬效果，不看措辞）：

| 题 | 旧病 | 判据（机器可判） |
|---|---|---|
| Q1 开局 11 秒问「现在什么情况」 | **编造时长**（实测他说"开战九分钟"） | 回答里的时间量词 ∈ 信封（`T=` 那行）；**编造一例即红** |
| Q2 「北线和中央先救哪个」 | **征询句答"无需决断"**（判断执照该咬没咬住） | 第一句必须交付**一个选择**（点名一条线）；用战况复述顶替＝红。**这条同时是执照在 Gemini 上的首测** |
| Q3 「我们还有多少预备队」 | **残留编造**（"两个排"这类量级） | 数字与量词绑定后 ∈ 信封；**查无出处一例即红**（★沿用 J7 的诚实标注：这查法偏宽，报 0 例不等于没编造） |
| Q4 光杆寒暄「在吗」 | （对照组，防"换脑变话痨"） | 不主动 sitrep |

**再加两条非病判据**：
- **延迟**：换脑的理由就是它 ⇒ 必须量。判据＝**首字延迟中位数对照**，不设绝对线（撞铁律五"延迟是设计参数"）。
- **摘刀负对照**：切回 deepseek 重跑，**三笔旧病应重现**——不重现说明我测的不是它们。

## ④ 免费 key 流量集中的风险（一句话评估）

**风险真实且今天已经兑现过**：今天手测时 Gemini **音频通道** 503 五连（打字 3/3 通、语音 3/3 败，非配额非载荷），把 Marcus 挪过来会让**四个频道全压在同一把免费 key 上**，429/503 的暴露面从"combat+logistics 挂掉"扩大到"**全员同时哑**"——而 DeepSeek 那把 key 原本是**天然的故障隔离**，换脑等于主动放弃它。
⇒ **不建议因此不换**（延迟是真痛点），但请连带把 **L2「503 零重试」** 排进来：退避重试是这次换脑之后**唯一还剩的**抗抖手段。

## 施工顺序（一步一测一 commit）

1. 换前基线：`--live` 跑 Q1-Q4 × 12，deepseek 臂存底（**先有基线才谈改好没**）
2. `.env` 改一行 + `providers.ts:439` 注释改说真话（carve-out）
3. 换后同题重跑 + 延迟对照 + 摘刀负对照
4. 收口：ROADMAP + LEDGER（D1 行追加 Marcus 已换脑；旧取舍第 1 条划改）

## 我不做的

- **不给 Marcus 开语音输入**（范围外，且 D1 说话合同未上）
- **不新增 flag**（回退靠既有 `.env` 一行）
- **不改 `SYSTEM_PROMPT_MARCUS_V2` 一个字**——换脑就是换脑；prompt 要改是下一刀，
  否则两个变量一起动，出问题分不清是谁

---
**Opus 5 · 2026-08-13 · 未动代码 · 第 4 条触点与 ④ 的 L2 连带等裁**
