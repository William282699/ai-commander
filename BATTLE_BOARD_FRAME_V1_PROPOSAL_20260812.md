# 刀② 一页纸 **v2**：战场板参照系（命名侧）

> v1 经 Fable 审定「可修订后拍板」，**七条必改全部照收**（我逐条在代码里复核过，
> 无一条我不同意）。基线＝`facility-target-downgrade-v1` @ `49d2d68`，全家 23/23。
> **未拍板不动工。** 取证：`npx tsx scripts/probe-naming-frames.ts`（纯读，不花配额）。
>
> **两处我要反过来更正审定意见的数**（都往更保守的方向）：
> ① 池差是 **11** 不是 12（1 commander + **10** elite_guard；`manualOverride` 开局为 0，
>    生产靠 `type==="commander"` + `isDispatchablePlayerUnit` 剔，实测 85→74）；
> ② 封顶 36 **余量为零**——本图最远那一群正好 36 格，`<=` 才刚好收住。
>    它不是"恰收"，是**拟合到这一张图这一个开局**的常数。见 §5 洞一。

---

## §1 病：同一句话里两个参照系（确定性，不吃抽样运气）

长官问「我军**兵营附近**那几个坦克」，陈报「**东北方向**第一未编组群」——
那支部队在兵营的**西北**。两个方位词都没算错，**它们量的是两个原点**：
`compassOctant`（`frontEscalationPayload.ts:313`）以**地图中心**为原点，长官以兵营为原点。

**数字按生产标签重报**（v1 的 10/12 把"罗盘词从不印出的群"也算进去了，虚高）。
生产路径 `buildReinforceOptions(state, null).options` 实测 **11 个群**：

| 事实 | 数 |
|---|---|
| 罗盘命名（拿不到地名） | **6/11** |
| 其中方位与"从它自己最近地标看过去"**不一致**＝**用户可见病面** | **4/6** |
| 已带地名（`X附近未编组群`，罗盘词根本不印） | 5/11 |

用户那一支逐字复现，在表里：

```
9人 东北方向第三未编组群   最近地标=我军兵营 17 格
                          以地图中心为参照=东北 / 以兵营为参照=西北   ★不一致
```

**第二个病同源**：`NAME_RADIUS_TILES = 12`（`:51`）。兵营旁那两支在 **17 / 20 格**，
超半径 ⇒ 拿不到「兵营」这个词 ⇒ **「兵营附近」在引擎词汇表里不存在**，
长官说什么都点不中它。

**第三个（工程，v1 说错了、已改口径）**：`commandPreflight:91` 与 escalation 各有一份
「地名或罗盘」的组合。**preflight 不是"缺去重"**——它按名聚合计数
（`placeCounts`，同名合并求和，语义是"从兵营附近走了几个人"），合并对它是**对的**。
两处真正重复的只是**内核**：「这个点该叫什么方位词」。

---

## §2 命名侧 / 寻址侧（边界，先划清）

| | 命名侧（**本刀**） | 寻址侧（**第 9 级，本刀不碰**） |
|---|---|---|
| 做什么 | 引擎**印出去**的名字叫什么 | 长官说的话**匹配到**哪支部队 |
| 手段 | 唯一命名内核、玩家参照系 | 来源字段 / provenance |
| 明令禁止 | — | **零匹配逻辑，不许模糊匹配绕** |

**本刀 prompt 面零字节**（红线重申；若施工中发现非改不可，停下报审，不夹带 F2 措辞路）。

---

## §3 修法两条（v1 的③已砍，见下）

**① 唯一命名内核**（纯重构，单独 commit，零行为变化）
把「地名或罗盘方位词」的内核收敛成 `frontEscalationPayload` 一个导出函数，
`commandPreflight:91` 改调它。**聚合与序数去重各留原地**——它们语义不同，
合并才是 bug。判据＝所有现有台架**逐字节不变**。

**② 玩家参照系**：方位以**这支部队最近的那个地名**为原点，不以地图中心。
`兵营西北未编组群` 取代 `东北方向未编组群`。
理由不是"更准"，是**长官只会用他看得见的东西当原点**；地图中心不是战场上的东西。
（同一条语义刀4「标记优先」已立过：精度是长官自己标出来的，引擎无权盖过。）

**③（原"数据驱动半径"）——砍掉，不是缓办。** 分位数半径**非局部**：
插一面旗会挪动全图分位数、翻转远处群的措辞。②落地后半径悬崖已不挡命名，
③只剩新病。**半径留常数。** 我再加一条同向理由：非局部还会让
**同一支部队跨轮改名**，正撞 B3（模型抄旧名 → 绊索拦下白费一轮）。

---

## §4 判据（v1 判据 1 是重言式，已作废换三件）

> v1 写「引擎印的方位＝从地标算的方位」——修后方位**就是**从地标算的，
> by construction 恒过。**它测不到任何东西**，撞家法「判据要测效果」。

1. **罗盘兜底率**：**6/11 → ≤1/11**，在**生产标签**上量。
2. **★跨面同名一致性**：同一群在 escalation label / preflight source / 板子行
   拿到**同一个名字**。这才是 ①+② 的真效果（今天两条路可能给两个名字）。
3. **摘刀负对照**：走唯一命名内核摘刀，**红集合精确重现**；
   摘刀后**先确认台架还活着再读红绿**（不然"全绿"可能是台架没跑）。
4. **零溢出**：①后现有台架逐字节不变；②后
   `ab-battle-board`/`ab-commander-presence`/`ab-approval-v4` 断言按新名逐条改，
   **★改断言与改实现分 commit**，否则台架跟着实现走就不再是判据。
5. **观察账（顺手量，不设线）**：原点变近 ⇒ 名字**跨 tick 翻转率**会变高，
   与 B3 互动。验收时量同群跨 tick 翻转率，记数不判红。
6. **不做的判据**：不测「长官说兵营附近能不能点中」——寻址侧，第 9 级的判据。

**验收台架从生产路径读标签**（`buildReinforceOptions(...).options` 与 preflight 生产路径）。
`probe-naming-frames.ts` **降级为取证附件**，不作验收——它与生产差三处，
实测：池 85 vs 74（差 11）、群 12 vs 11、标签自己拼的（丢「附近」、
跳 `locationPhraseFor` 的行进中分支、滤单人群）。

---

## §5 两个洞（必答，摆给长官拍）

**洞一 · 远地标封顶**：100 格外印「兵营西北」是误导（P1-1 家族）。两案都写全：

| 案 | 长相 | 代价 |
|---|---|---|
| (a) 名字带距离 | `兵营西北 17 格未编组群` | 自披露、**无新常数**；名字长，信封变胖，陈说起来不像人话 |
| (b) 不带距离 + 封顶 | `兵营西北未编组群`，超 N 格退回罗盘 | 名字短；**多一个常数**，且 N 拟合于本图 |

★ 若选 (b)：`3×NAME_RADIUS=36` **余量为零**（本图最远群正好 36 格）。
它盖住本图 6 群是**拟合不是验证**，换张图就得重量。**这条我不替长官选。**

**洞二 · 方向原点集合**（v1 写「玩家地标」，与 `nearestPlaceWithin` 不是同一个集合
——后者是标记优先＋**全部**设施（含敌/中立）＋战线中心。两集合不一致＝**第三个参照系**，
正是本刀要消灭的那类病）。
**我同意 Fable：改成同一个集合**，理由不只是"一致"——第 8 级改名刀（`6013d9a`）
专为「说得出口」改的就是敌方设施名（驼峰山脊/烽火台），把它们排除在原点之外
等于把那一刀的收益扔掉。
★ 实测这两个集合在本图开局**对这 6 群给出同一个最近地标**（全是玩家设施），
所以洞一洞二在**这一张图上**不互相牵连——但那是"这 6 群都在我方后方"的巧合，
西边的群就不成立。**别把这个巧合当成通例。**

---

## §6 爆炸半径（v1 §6-3 已删：`escalationTicket` **零字节**）

已核：票据在 `mintOne:162` 就把 `label: opt.label` **冻进自己身上**，
`glued:421` 比的是**那张票自己的冻结 label**，且登记簿 `tickets.clear()` 每局 reset
——**不存在"改名当轮旧票失配"这个时刻**。v1 那半段是我凭空担心的，删掉。

开工前记账三笔：

1. **`glued` 补真牙**：按新式样补用例——**新式样粘连必须解析、旧式样必须拒**。
2. **`ab-approval-v4` label 断言逐条清点**哪些是硬编码
   （`:952` 那类"两面相等式"会自动存活，硬编码的不会）。
3. **F2 夹具不受影响但不许跨刀直比**：`ab-facility-target:315/317` 是**冻结输入**，
   改名动不了它；但刀① 那个 **25%** 是在旧信封上量的，
   要跨刀比必须**用新生产信封重造夹具**再量。
4. （旧账仍在）**硬编码显示名全仓 grep** —— 第 8 级改名刀在 `ab-pretest-polish`
   栽过一次：它会安静印旧名。

---

## §7 请拍板两件

1. **洞一选 (a) 带距离 还是 (b) 封顶**（§5 表）——影响信封长度与陈的人话手感。
2. **次序确认**：① 纯重构单独 commit → ② 参照系（含洞二"同一集合"）。
   ③ 已砍。拍板后开工，一步一测一 commit，每步停下报审。

---

**Opus 5 · 2026-08-12 · v2 · 七条必改全收 + 两处数字反向更正 · 等拍板**
