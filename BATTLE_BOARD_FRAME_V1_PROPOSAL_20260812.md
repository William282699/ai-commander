# 刀② 一页纸：战场板参照系（命名侧）

> 给用户 + Fable 裁。基线＝分支 `facility-target-downgrade-v1` @ `1ba7361`
> （自 main `c23be5c`），全家 **23/23**。取证脚本可重跑：
> `npx tsx scripts/probe-naming-frames.ts`（纯读，不调模型、不花配额）。

---

## §1 病：同一句话里两个参照系（确定性，不吃抽样运气）

长官问「我军**兵营附近**那几个坦克」，陈报「**东北方向**第一未编组群」——
而那支部队在兵营的**西北**。两个方位词都没算错，**它们量的是两个原点**：

- `compassOctant`（`frontEscalationPayload.ts:313`）以**地图中心**为原点
- 长官的那句话以**兵营**为原点

真现场（`el_alamein` 开局，12 个未编组空间群）逐群实测：

| 事实 | 数 |
|---|---|
| 罗盘词与"从它自己最近的玩家地标看过去"的方位**不一致** | **10/12** |
| 拿不到任何地标名、只能报罗盘方向 | **6/12** |

用户那一支就在表里，逐字复现：

```
9人 @(400,61)  引擎叫「东北方向未编组群」
               最近玩家地标=我军兵营 17 格
               以地图中心为参照=东北 / 以兵营为参照=西北   ★不一致
```

**第二个病同源**：`NAME_RADIUS_TILES = 12`（`:51`）。兵营旁那两支在 **17 格 / 20 格**，
超出半径 ⇒ 拿不到「兵营」这个词 ⇒ **「兵营附近」在引擎的词汇表里根本不存在**，
长官说什么都点不中它。半径不是"精度阈值"，它是**地标密度的函数**，
而这张图的玩家地标只有 7 个、平均间距远大于 12。

**第三个（工程）**：「地名或罗盘」这套组合逻辑有**两份**——
`frontEscalationPayload.ts:419-444`（带 第一/第二 去重）与
`commandPreflight.ts:91`（`nearestPlaceWithin(...) ?? \`${compassOctant(...)}方向\``，
**没有**去重）。同一支部队在两条路上可能拿到两个名字。

---

## §2 命名侧 / 寻址侧（本刀的边界，先划清再谈修法）

| | 命名侧（**本刀**） | 寻址侧（**第 9 级 provenance，本刀不碰**） |
|---|---|---|
| 做什么 | 引擎**印出去**的名字叫什么 | 长官说的话**匹配到**哪支部队 |
| 例 | 把那 9 个人印成「兵营西北未编组群」 | 「兵营附近那几个坦克」→ 解析成 G4 |
| 手段 | 唯一命名函数、玩家参照系、数据驱动 | 来源字段 / provenance |
| 明令禁止 | — | **不许加模糊匹配绕过去** |

本刀**一行匹配逻辑都不写**。它只让引擎印出的名字落在长官的参照系里；
"听懂"仍然是第 9 级的活。两者的因果顺序也对：名字里有了长官真会用的地标，
第 9 级面对的是一个更小的问题（刀① 收口段已记：这会缩小 F2 的触发面）。

---

## §3 修法三条（都在命名侧）

**① 唯一命名函数**（工程，零行为变化优先落地）
把「地名或罗盘」的组合收敛成 `frontEscalationPayload` 里一个导出函数，
`commandPreflight:91` 改调它。**先做这条、且单独一个 commit**——它是纯重构，
判据＝所有现有台架逐字节不变（含 `ab-battle-board` / `ab-commander-presence` /
`ab-approval-v4` 的名字断言）。有了唯一入口，②③ 才只需改一处。

**② 玩家参照系**：方位以**离这支部队最近的玩家地标**为原点，不以地图中心。
`兵营西北未编组群` 取代 `东北方向未编组群`。
理由不是"更准"，是**长官只会用他看得见的东西当原点**——地图中心不是战场上的东西。
（同一条语义已在刀4「标记优先」立过：精度是长官自己标出来的，引擎无权盖过。）

**③ 数据驱动半径**：`NAME_RADIUS_TILES` 的 12 是个手挑的常数，而它该回答的问题是
「这支部队离那个地标，近到可以拿它命名吗」——那取决于**这张图的地标密度**。
改成由地标间距推出来（例如最近邻间距的分位数），常数留作下界。
★ 与②合起来还有个副作用值得单独说：有了"地标+方位"，**半径悬崖本身可以消失**
——每支部队都能拿到「某地标某方向」，罗盘兜底只在真的荒无人烟时才用。

---

## §4 判据（效果级；名字是给人读的，所以判据分两层）

1. **参照系一致性（机器，硬判据）**：对每个群，引擎印的方位必须等于
   "从它自己被命名的那个地标看过去"的方位。今天 10/12 不一致，**修后 0 不一致**。
   这条是纯几何，不吃模型、不吃抽样——**RED 先行照跑**（`probe-naming-frames.ts`
   已经是红的）。
2. **可点名率（机器）**：拿不到地标名的群 6/12 → 目标 ≤1/12。
3. **零溢出（机器）**：①落地后所有现有台架逐字节不变；②③落地后
   `ab-battle-board`/`ab-commander-presence`/`ab-approval-v4` 的断言按新名**逐条改过**
   ——★改断言必须与改实现**分开 commit**，否则"台架跟着实现走"就不再是判据。
4. **不做的判据**：不测"长官说兵营附近能不能点中"——那是寻址侧，第 9 级的判据。

---

## §5 爆炸半径（如实报，这是本刀最该被审的一段）

改名字会波及**三处已经按名字工作的机器**：

- **G# 番号登记簿**：`forceHandleTagFor` 的 label 逐字匹配（`glued()` 要求
  "剥掉尾号后的前缀逐字等于票据 label"）。名字一变，**跨轮次的旧引用会失配**。
  → 本刀必须同时回答：改名当轮已发出去的票怎么办（倾向：票据存自己的 label 快照，
  不回头重算——与 B3「不覆写冻结字段」同一条家法）。
- **对话史**：上一轮陈说过「东北方向第一未编组群」，改名后模型抄旧名 ⇒ 撞 B3
  已登记的那条（号一直换 → 模型抄旧号 → 绊索拦下白费一轮）。
- **硬编码显示名**：改名刀（第 8 级 `6013d9a`）栽过一次——`ab-pretest-polish`
  里有一份硬编码名表会安静印旧名。本刀开工先 grep 全仓。

---

## §6 请裁三件

1. **三条修法的次序与取舍**：我主张 ①（纯重构）单独先落，②③ 一起；
   ③ 若嫌复杂可只做②，半径留常数记账——**②才是治用户那一枪的**。
2. **新名字的形状**：`兵营西北未编组群` / `兵营西北 17 格未编组群` / 其它？
   带不带距离数字，影响信封长度与"陈说人话"的手感，**这条我不替长官定**。
3. **爆炸半径 §5 三条的处置**，尤其票据 label 快照那条——它决定本刀要不要
   碰 `escalationTicket`（碰了就不再是纯命名侧，得重新划线）。

---

**Opus 5 · 2026-08-12 · 23/23 · 取证脚本 `probe-naming-frames.ts` 可复跑 · 等裁**
