# 刀 C · 头部盲区（一页纸提案，2026-08-10）

> 给 Fable 5 审。**诊断期零实施**——本档写完时 worktree `voice-input-v1` @ `8f0c0b7`
> 工作区干净、22/22 全绿，一个字节没动。
> 病源＝用户真麦手测（三个样本，跨两个频道）。刀 B 治了尾，这次是头。

---

## §1 一句话

按下 🎤 到麦克风**真的开始收音**之间有一段盲区，长官在这段里说的话物理上不存在；
危机中按下就喊必中，平静中按下有停顿则躲过——**坏的永远是第一个词**。

---

## §2 病象（用户手测，三个样本同形）

| 频道 | 长官说的 | heard 交回的 | 形状 |
|---|---|---|---|
| 陈（危机中） | **南线前哨守军** 全部出动 去支援中央前哨 中央前哨很重要不能丢 | **全套的收军** 全部出动 去 支援中央前哨 中央前哨很重要不能丢 | 句首 4 字全坏，**后半句逐字全对** |
| 陈（短句） | （短句） | 「中央前哨八」/「我们处理你」 | 头/尾残缺 |
| **Emily（平静）** | **Emily** 你现在怎么样 | **Amaly** 你现在怎么样 | **第一个词坏，其余逐字全对** |

Emily 那条是**对照组里最要紧的一条**：它跨了频道，说明这不是陈的 prompt 问题；
而且它平静、按下有停顿，所以只坏了一个词——**同一个病的轻症**。

### 两个已证事实把别的嫌疑排掉了

1. **信封大小不影响转写质量**（步 5 消融已证）⇒ 不是信封稀释。
2. **代码层两条频道的采集路径逐字相同**（`channelUsesVoiceCapture` 只决定"这个频道
   走不走录音"，走了之后是同一个 `startVoiceRecording`）⇒ 频道差异只可能来自
   **长官按键习惯**，这恰好与"危机中按下就喊必中 / 平静中有停顿则躲过"吻合。

---

## §3 机制（结构上已确证的部分，不需要等实测）

`ChatPanel.tsx:493-511` `startPTT`：

```
按下 🎤
  ├─ cancel()                     // 掐掉陈的 TTS
  ├─ setPttStatus("listening")    // ★🔴 在这一行就亮了
  └─ startVoiceRecording()        // ← 之后才开始开设备（await getUserMedia）
        ├─ G1  getUserMedia 解析中……      ← 麦克风还没开，说什么都不存在
        ├─ G2  new AudioContext / 接图      ← 亚毫秒
        └─ G3  AEC/AGC 收敛中               ← 收得到，但被压
```

**🔴 指示灯在设备开之前就亮了**——这一条不用量，读代码即知：`setPttStatus("listening")`
是同步执行的，排在 `startVoiceRecording()` 之前。长官看见红灯就开口，而红灯说的是假话。

### 一个必须先排除的误判：4096 帧那个缓冲**不是**盲区

`createScriptProcessor(4096,1,1)` 在 48kHz 下正好 85.3ms，很容易被当成"丢了 85ms"。
**它是延迟不是缺口**：第一次回调携带的正是这 85ms 期间采到的样本，一个不少。
真正的缺口只可能在**帧还没开始流之前**，也就是 G1。

---

## §4 实测（我能量的那一半，与我量不到的那一半）

### 我量到的：图启动（**不含设备开启**，所以是下界）

在真浏览器里逐字镜像 `voiceRecorder.ts` 的图，喂合成流跑 5 次：

| 跑次 | AudioContext 创建 | **第一帧到达** |
|---|---|---|
| #1（冷） | 0ms | **228ms** |
| #2-#5 | 0-3ms | **83 / 87 / 87 / 85 ms** |

稳态 85ms ＝ 4096 帧 @48kHz 的一个缓冲周期，与理论值分毫不差 ⇒ 这一段是**图固有延迟**，
冷启那次多出的 ~143ms 是音频基础设施第一次起来的代价。

### 我量不到的：G1 设备开启 —— 也就是**唯一真正会丢字的那一段**

这台浏览器**没有音频输入设备**（`getUserMedia` 直接 `NotFoundError: Requested device
not found`，4/4 跑全是它）。所以：

> **★ 家法披露**：合成假麦**没有设备开启这一段**——它连"病"这个东西的载体都没有。
> 这一格与刀 B 的 RED 同形（假麦无 AEC 管线，尾损只有 ~13ms，夹具表达不出那个病）：
> **不是我没量，是这套夹具结构上量不到。** 真数只能由长官在真浏览器上量。

**给长官贴的探针**（DevTools Console，10 秒出数；它只算时间，不录音、不发送）：

```js
(async () => {
  const one = async () => {
    const t0 = performance.now();
    let stream; try { stream = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation:true, noiseSuppression:true, autoGainControl:true, channelCount:1 } }); }
    catch (e) { return { error: e.name }; }
    const gum = performance.now() - t0;
    const ctx = new AudioContext(); const src = ctx.createMediaStreamSource(stream);
    const proc = ctx.createScriptProcessor(4096,1,1); const mute = ctx.createGain(); mute.gain.value = 0;
    let f=null, nz=null, ab=null, n=0;
    await new Promise(res => { proc.onaudioprocess = e => { const now = performance.now()-t0, d = e.inputBuffer.getChannelData(0); n++;
      if (f===null) f=now;
      if (nz===null) { for (let i=0;i<d.length;i++) if (d[i]!==0) { nz=now; break; } }
      if (ab===null) { let a=0; for (let i=0;i<d.length;i++) a+=d[i]*d[i]; if (Math.sqrt(a/d.length)>0.0005) ab=now; }
      if (n>=30) res(); }; src.connect(proc); proc.connect(mute); mute.connect(ctx.destination); setTimeout(res,3000); });
    proc.onaudioprocess=null; stream.getTracks().forEach(t=>t.stop()); void ctx.close();
    return { gum_ms:Math.round(gum), first_frame_ms:f&&Math.round(f), first_nonzero_ms:nz&&Math.round(nz), first_above_floor_ms:ab&&Math.round(ab) };
  };
  const out = [];
  for (let i=0;i<5;i++) { out.push(await one()); await new Promise(r=>setTimeout(r,800)); }
  console.table(out); copy(JSON.stringify(out)); return '已拷贝到剪贴板';
})()
```

**两档要分开报**（用户提的，我同意，两个数可能差一个量级）：
① **首次授权后第一次**（权限已给但设备是冷的）＝第 1 行；
② **后续按下**＝第 2-5 行。
`gum_ms` 就是 G1；`first_above_floor_ms − first_nonzero_ms` 是 AEC 爬坡（G3）的粗测。

---

## §5 候选修法（三条，可叠加；用户列的三条我逐条评了代价）

| # | 修法 | 治哪一段 | 代价（**必须先说清**） |
|---|---|---|---|
| **C1** | **常驻 stream**：首次授权后不再 `track.stop()`，按下＝立刻开始收，松手＝停止收集但**不拆设备** | **G1 归零**（第二次起） | ★**麦克风常开**：macOS 菜单栏/橙点指示器整局亮着。这是长官的决定不是我的——一个语音指挥游戏里大概率可接受，但**必须他点头**，不许我替他默认 |
| **C2** | **开局预热**：借第一个用户手势（点"开始"）就把设备开起来 | G1 的**首次**那一档 | 同 C1（开了就一直开）；单独用不解决问题，是 C1 的"什么时候开始"变体 |
| **C3** | **🔴 只在真开录后才亮**（指示灯不许撒谎） | 一段都不治 | 零风险，但**它只是把等待转嫁给长官**；且危机中长官眼睛在战场上、不在按钮上，未必看得见 |

**Opus 建议：C1 + C3 一起上。** C1 拿掉盲区本体，C3 让第一次（还有 C1 失败降级时）
不再骗人。C2 并进 C1 的实现里（预热时点＝首次授权那一次）。

**一条我加的候选，留给你裁**：**C4 开录提示音**——真开始收音时播一声极短的 earcon。
理由：危机中长官在看战场不看按钮，视觉指示灯到不了他。代价：这一刀刚花了整整一层
把耳朵里的声音减到两声，现在又加一声。**只在 C1 被否、必须靠"等一下再说"时才值得上。**

---

## §6 RED 先行判据（修前必须先跑出红）

**修前（真麦，长官量）**
1. `gum_ms` 中位 ≥ **X ms**（X 由 §4 探针填；我这边只有不含设备的下界 85/228ms）；
2. **首词丢失可复现**：念 10 句**以战场专名开头**的话（如「南线前哨守军全部出动」），
   按下即说不留停顿，heard 首词坏 ≥ 3/10。
   ——判据数的是**首词**对错，不是整句字错率（家法：判据要测效果不测措辞）。

**修后**
3. 按下→第一个**被收进 chunks 的样本** < **Y ms**（建议 Y = 30ms，即"按下即收"）；
4. 同 10 句重跑，**首词全对**（10/10）；
5. **★负对照（必做）**：把常驻 stream 摘掉、回到每次新开设备，
   **首词丢失必须回来**——回不来就说明修的不是这个病，那组数作废。

**哪一半台架够得到（如实分账）**
- **够不到**：G1/G3 的毫秒数与首词丢失本身。合成假麦没有设备开启这一段，
  **结构上表达不出这个病**（刀 B 先例）。这一格的裁决权在长官的真麦。
- **够得到**：把"按下就开始收"这条不变量做成可测的形状——
  录音件拆成 `arm()`（拿设备）/ `collect()`（开始收）两态之后，
  node 台架可以断言「collect 之前不产生任何样本」「arm 已完成时 collect 的启动开销为 0」，
  以及常驻 stream 的生命周期（松手不 stop、切频道/卸载才 stop）。
  **这是结构断言，不冒充效果断言。**

---

## §7 同刀顺手两笔小账（**只登记，不动 prompt**）

> 家法：**输入是脏的时候不审嘴**。下面②的两条都是在句首已经被吃掉的输入上产生的，
> 现在去改陈的说话合同，改的是一个没犯错的人。刀 C 落地、输入干净之后再看还在不在。

### ① 语音回合的兜底文案（要改，但不许新造模板句）

`packages/shared/src/schema.ts:449`：

> 「通讯干扰，无法解析参谋建议。**以下为默认方案。**」

三个问题，一个比一个重：
1. **没有方案**：砍卡法之后这条路根本不展示 A/B/C 选项，「以下为默认方案」指着一个
   屏上不存在的东西；
2. **吓人**：语音回合本就是长官在危机中喊话，回一句"通讯干扰"像是游戏坏了；
3. **★而且现在它会被念出来**——这是我这一刀刚欠下的新账：spoken 缺席时按 R7 退回念正文，
   而兜底回执天生没有 spoken ⇒ 分层之前这句话在非流兜底路上是**哑的**，
   分层之后**耳朵能听见了**。登记为 spoken 层的一处有意行为差异的副作用。

**修法方向（不新造罐头）**：这条路的语义就是「没听清/没接上，请再说一遍」，
而 bucket B 已经有一台现成的问句机器（§X-6 裁过：fail-closed 只做路由、台词走
现有 bucket B 问句）。**优先复用/裁剪它，而不是写第二句罐头**。

### ② 陈的两笔台词账（登记在案，本刀不动）

| 账 | 现场 | 撞哪条 |
|---|---|---|
| **拒办口吻** | 「中央战线已失守，无法再增援中央前哨」 | 说话合同② —— **战线失守 ≠ 派不了兵**；"值不值得"是长官的决定，不是陈的。陈可以说代价，不能替他判死 |
| **屏上复读 n+1** | 陈把长官刚说的话原样印在屏上 | 就是 L2 那笔。分层后耳朵听不见了，**屏上这一半仍在**（台架实测 6/21）；这次是真机实例，记一笔计数 |

---

## §8 我没能做到的一件事（如实报，且它本身是一笔账）

**这一局的 `[EVENT] voice_heard` 原始日志，我捞不到。**

- voice-api(3018) 是**另一个会话**起的进程，不在本会话的 preview 名下 ⇒ `preview_logs` 够不到；
- 它的 stdout/stderr 是 unix socket（`lsof` 已核，fd 1u/2u），**没有落盘**；
- 全库搜别的会话记录，只搜到设计窗口里的**转述**，没有原始行。

代码注释里写得很清楚（`index.ts:94`）：*"现状只到 console / `fly logs`：JSONL 落盘属 O6，
随 A5 记录仪缓办"*。**那笔缓办的账，今天第一次收利息**：一次真麦手测的原始证据没了，
只剩长官的口述。所以本档的对账是拿口述做的（三个样本 §2 那张表），**没有逐行日志佐证**。

> 顺带澄清一个词：用户口述里的「通讯感染」＝他在念屏上的「通讯**干扰**」那句兜底文案
> （`schema.ts:449`），**不是第二种听错形状**。查到出处才敢这么说，见 §7-①。

**建议**（不在本刀，登记）：`voice_heard` 落一份 JSONL 到磁盘。手测的价值一半在事后对账，
而现在每次关掉窗口就清零。

---

## §9 给 Fable 的四个问

1. **C1 的代价（麦克风整局常开）值不值**——我判"值，但必须长官点头"，你同意吗？
2. **C3 单独上算不算修**？我判它是"诚实"不是"修"，不能作为唯一修法交差。
3. **C4（开录提示音）** 与 spoken 层刚做的"耳朵只两声"冲突，我判**默认不上**，只在 C1 被否时上。
4. §6 的 Y=30ms 与"首词 10/10"两条线，够不够狠？（我的顾虑：10 句 n 太小，
   但真麦手测的成本在长官身上，不敢开 20 句。）

---

**Opus 5 · 2026-08-10 · 诊断期零实施（worktree 干净 @ `8f0c0b7`）**

---

# §10 Fable 审定（2026-08-10；本节与正文冲突以本节为准，连同正文构成施工合同）

**总判：通过。** 两处代码断言 Fable 亲自复核过（🔴 确在开麦之前亮；「通讯干扰」确在
`schema.ts:449`）；85ms 缓冲"是延迟不是缺口"的排除论证成立；逻辑链无洞。

## 四问裁定

| # | 裁定 |
|---|---|
| ① | **C1 值得做，等用户点头**（麦克风常开的代价是长官的决定）——同意 |
| ② | **C3 单独上不算修，只算诚实**——同意，不得作为唯一修法交差 |
| ③ | **C4 提示音默认不上**——同意（刚把耳朵减到两声，别又加回去） |
| ④ | **§6 判据第 4 条改口径**（见下） |

## ④ 判据口径修订（替换 §6-4）

原文「同 10 句重跑，首词全对（10/10）」**过严且测错了东西**。改为：

> **首词失败逐条手读，按「盲区形状」判**——只有**掉头音**（第一个字/音节物理缺失，
> 如「南线前哨守军」→「全套的收军」）才算病没治好；**普通谐音听错**
> （头音在、只是选错同音字）**不算**。
>
> 理由：这一刀治的是「头有没有被吃掉」，不是「转写零错」。拿转写准确率当判据，
> 会把一个已经修好的盲区判成没修好——也会反过来把"碰巧转写对了"当成修好了。

**10 句维持不加**（真麦手测的成本在长官身上）。

**Fable 5 · 2026-08-10 · 提案通过，等长官两件事：探针实测数 + C1 点头**

---

# §11 两臂实测结果（2026-08-10 用户真麦，Opus 读服务端日志对账）

施工＝`09cade2`（C1+C2+C3）+ `3324c63`（兜底文案）+ `e26165c`（耗时回传）。
两臂在**同一次坐下、同一支麦、同一副嗓子**上跑完，靠 `?novoicewarm` 切换——
它只改 arm 的时点，采集件一个字节不变。日志自带 `warmup:true/false` 标臂，
不靠人记。

## ①【memory-independent】盲区的真数——§4 那个 X 填上了

摘刀臂每次按下都重开设备，于是每一句都记一行。六次（同一 origin，权限已授予，
**无授权框污染**）：

| | 六次实测 | 中位 |
|---|---|---|
| `gumMs`（申请到设备＝G1） | 340 / 277 / 283 / 250 / 247 / 295 | **280ms** |
| `firstFrameMs`（到第一帧真流起来） | 487 / 433 / 435 / 378 / 446 / 441 | **438ms** |

⇒ **旧行为每按一次，最前面约 0.44 秒的声音物理上不存在。**

中文正常语速 4-5 音节/秒 ⇒ **0.44s ≈ 2 个音节**。与三个修前坏样本丢失的量
（「中央」2 字、「Aiden」2 音、「南线前哨守军」前几音）**在数量级与音节数上对齐**。
机制、数字、现象三样第一次凑齐。

> 预热臂的 `gumMs 4257` **不可作盲区读**：新 origin 首次弹授权框，含用户点
> 「允许」的反应时间。它的意义只在于**这笔钱付在开局那一下，不在按下那一刻**。

## ②【memory-independent】最脆的音活没活下来

| 臂 | 回合 | 句首带语气词/改口（嗯/呃/啊、"北线啊…北线前哨"） |
|---|---|---|
| 预热（修复开） | 8 | **3/8 有**，且全部完整录下 |
| 摘刀（修复关） | 6 | **0/6** |

句首语气词是**最先被吃的那批声音**，它们的有无不依赖任何人记得说过什么。
预热臂里它们活着（连"嗯陈"、"算了先吧"这种改口都在），摘刀臂里集体消失。

★ 单句最强对照（预热臂 #1）：用户说的几乎就是修前那句坏掉的话——
修前「南线前哨守军全部出动」→「**全套的收军**全部出动」；
修后「(嗯陈)南线前哨的守军全部出动去中央前哨」→ **逐字全对**。

## ③【memory-dependent，如实降级】逐句首词判定做不完整

Fable §10-④ 定的口径是「首词失败逐条手读、只数掉头音」。**这一条这次只能部分执行**：
用户自然玩、没照稿念，事后回忆不起原话（"应该是，我忘记了，但差不多那个意思"）。
所以下面两条只能报作**"与盲区一致"，不作已证**：

| 疑似掉头（摘刀臂） | 预热臂同类句 |
|---|---|
| 「**前哨**是否需要」（疑似丢「中央」，尾也秃） | 「中央前哨是不是需要支援呢」完整 |
| 「全员出动去走中央沙漠小路…」（疑似丢句首「Aiden」） | 「**Aiden**，呃，走，中央沙漠小路去占领…」完整 |
| 「我看不纠正吧」（整句对不上原话） | — |

**方法教训（进方法资产）**：自然玩的生态效度更高，但**牺牲了逐句基准真值**。
要两者兼得，只能在采集端记下"长官打算说什么"——而那不存在。
**下次同类验收：要么念固定稿，要么就认下这一半判据做不了，别事后靠回忆补。**
本次之所以仍然站得住，是因为①②两条压根不依赖记忆。

## ④ 顺带记两笔

- **「战狼点」→「狼店」**（预热臂 #5，句**中**不在句首）：不是设备盲区，是转写
  本身错的（台架 --live 20/20 全对，真麦此次 1 次未中）。单独记账，不算刀 C 未愈。
- 8 + 6 = 14 个语音回合，**heard 无一缺席**（fail-closed 一次没触发）。

## ⑤ 结论

- **修法有效**：负对照臂病回来了（①②两条互相独立地指向同一结论）；
- **量化对齐**：测出的盲区 438ms ≈ 2 音节 ＝ 修前样本丢掉的量；
- **一半判据未完成**：逐句首词判定因缺基准真值降级为"与盲区一致"，已如实标注。

**Opus 5 · 2026-08-10 · 待 Fable 复核（三个 commit 堆着等审）**
