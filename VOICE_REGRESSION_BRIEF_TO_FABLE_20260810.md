# 请复核：spoken 层引入的回归（「通讯干扰」）+ 我自己的两处失职

> 给 Fable 5。**用户手测中发火：「一直都好使，怎么突然又坏了」——他是对的。**
> 这是我这一刀捅出来的回归。修法已进 `51d0115`，但**未经真模型验证**。
> 现场：worktree `voice-input-v1` @ `51d0115`，22/22 全绿，main 零字节。

---

## 1. 回归本体（日志实证，机器复现）

```
spoken 层上线
 → 模型把正文流在 ---JSON--- 之前、又在 JSON 里写了 spoken
 → 它认为正文已经交过，【不再写 brief】
 → validateAdvisorResponse 第一行：typeof obj.brief !== "string" → return null
 → 整份合法答卷判废 → 服务端换 createFallbackResponse()
 → 屏上「通讯干扰，无法解析参谋建议」
 → 【heard 与 spoken 一起陪葬】
 → 客户端 fail-closed 进 bucket B、耳朵退回「正文 + 回执」两段
 → 用户报的"墨迹"
```

**发生率：一局 15 个语音回合中 4 次（~27%）。**
其中 2 次的 JSON **完整、合法、spoken 写得好好的**，唯一的毛病是没有 brief。

**机器复现（台架 A1/A2，日志原文逐字照抄，不是我造的例子）**：
原样 → `null`；把已流给长官看的正文补进 brief → 复活，且 heard 与 spoken 都在。

---

## 2. 修法（`51d0115`，请重点查这一条）

`backfillBriefFromPrelude()` 导出纯函数，在**两条命令路**判废之前各插一次：
JSON 没写 brief ⇒ 把 `---JSON---` 之前那段**已经显示在屏上的正文**补进去再验一次。
不覆盖模型自己写的 brief；前言为空则不编、照旧判废。

**这条补救原先只对 `marcus_consult` 开**（"Marcus V2 向后兼容"），陈的 execute
模式一直没有。**A5 断言第一次跑就咬到非流式路上漏网的第二份副本**——而那条正是
流式判废后的兜底路，只补一条等于没补。

台架 +6：A1-A6（含两条源码级：两条路都装、门禁已拆）。

**★我要你重点查的**：这条"复活术"会不会救活**本来就该死**的答卷？
我的论证是不会——`validateAdvisorResponse` 对 options/intent 的校验一个没松，
补的只是一个**内容已经在屏上、客户端本来也要补**的字段（`accumulatedText &&
!data.brief`）。但这是我自己的论证，请你独立判。

---

## 3. ★我的两处失职（比回归本身更该查）

### 失职一：昨天就见过这个症状，我只治了标

**用户昨天的手测里「通讯干扰，无法解析参谋建议。以下为默认方案。」就出现过。**
我当时做了什么？——把文案里那半句假话删掉（`3324c63`），登记成"小账①"。

**我没有问一句：这条兜底为什么会触发。**

那正是家法 `feedback_treat_root_cause`「改值前先找根因」的反面。一个每四次
就烧一次的兜底路，被我当成文案问题处理了一整轮。

### 失职二：台架的信封是个玩具，所以它测不到真局的失败模式

`--live` 的 `LIVE_DIGEST` 只有十来行；真信封是 FRONT_JUDGMENT + 38 行 SQUADS
+ FACILITIES + TAGS + PLAYER_VIEW 一大堆。**小信封下模型的回复短、老老实实写
brief；真信封下它话多、就开始省字段。**

我甚至**自己写下过这个风险**（分臂那次 commit：「spoken/正文长度比 0.96 说明
台架小信封里两层几乎没分化，分化度的裁决权在真信封手测」），
**却没有想到同一条理由也意味着"台架测不到真局的判废率"**。
22/22 全绿与 27% 的真局失败率同时成立，就是这么来的。

**建议（请你裁）**：`--live` 换成**真实规模的信封**再重跑一遍 spoken 层的全部
判据（L1/L4/L7 那一组数可能全部要重测）。这不是补丁，是修判据的取样。

---

## 4. 还没做、需要你裁的

1. **修法未经真模型验证**：A1-A6 证明的是"补上就能复活"，
   **没有证明"真局里判废率降到 0"**。要一次真麦手测或一次真信封 `--live` 才算完。
2. **根因还在**：复活术是**防守**，它没有阻止模型少写 brief。是否同时在
   【本次强制】里点明「spoken 不替代 brief，brief 照写」？
   我倾向做（防守+去因各一条），但那是 prompt 改动、按 G 刀止损线只有一轮，
   且要先有真信封基线才量得出效果。**请你裁先后。**
3. **spoken 层要不要继续**：用户此刻的原话是「卧槽了真的是服了」。
   如果你判这一层的收益压不住它引入的风险，**回滚它是干净的**——
   三个 commit（`08a32b0` `bd8a971` `9806ad8`）加后续，都在独立分支上，main 零字节。

---

## 5. 顺带：本轮手测的其它已核事实（不必你查，备查）

- 本地即时应答音**已按用户判退砍掉**（`54548fa`）。顺带兑现了我在步 1 挂的那笔账：
  长官问「有没有空闲部队」，池子抽中「动手。」——**承诺早于理解**当场实锤。
- 刀 C 稳住：本轮 11 个语音回合**首词全活**，语气词/改口全部录到。
- 「51 个单位」是真数（开局 85 全未编组）；
  截图里那句是**长官自己的回填气泡**，不是陈复读——但**用户当场读成了复读**，
  ⇒ 回填与复读在屏上分不出来，记 UX 账。
- **G17 那笔（执行链，不在本刀）**：`02:01` 实走 3 个且去的是「中央战线」而非
  长官说的「中央前哨」；`02:19` 陈又报同一张 G17、同样 8 个单位（票已花）；
  `02:25` 长官点名 G17 → 引擎「已经派出去了，不重复下令」。
  **嘴报的是过期名单，照着点名被拒。** 属票据/名单同步问题，进 LEDGER。

---

**Opus 5 · 2026-08-10 · 回归已修未验，等你复核**
