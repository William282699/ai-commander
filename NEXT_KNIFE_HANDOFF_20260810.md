# 新窗口交接：语音已收口，下一刀＝执行链两笔（2026-08-10）

> 新窗口开工先读：主仓库根 `ROADMAP.md`（语音收口段在「归档与资产」之前）
> + 本档 + `FACILITY_TARGET_DOWNGRADE_BRIEF_20260810.md`（含 Fable 裁定＝施工合同）。

---

## §0 现场（照抄给新窗口）

- **main = `fb0aa8a`**，tag `voice-input-v1-done`，**工作区干净**，全家硬线 **22/22**
- 语音分支 `voice-input-v1` 已 ff 合入 main（24 commit），worktree
  `~/MyProjects/AI Commander-voice-input` 与 main 同点，**下一刀继续在这个 worktree 上做**
  （用户指定；Fable 原裁"从新 main 开新 worktree"，同点即等价）
- 服务：`knifec-api` 3022 / `knifec-web` 3023（本会话 preview 名下，
  `[EVENT] voice_heard` 与 `prevSpeech` 都落在这儿；**手测服务必须起在自己会话名下，
  否则 preview_logs 够不到**——这是语音刀踩过的账）
- 跑台架：`bash scripts/run-benches.sh`（22 项，含 typecheck）。
  `--live` 花配额，按需手动：`npx tsx scripts/ab-voice-input.ts --live 20`
- **未 push**（用户没要求；要推得先问）

---

## §1 下一刀：据点被降格成战线（Fable 已批，一页纸合同已就位）

**病**：长官说「支援**南线前哨**」，兵去了「**南部战线**」。**7 个实例**（本次手测占 4 个）、
**2 份历史记录**（撤退语义粒度账 ROADMAP:55 / 批准合同 v3.2 结构病③ ROADMAP:40）、
**零次修**——v3.2 那次被判退雪藏，把这条一起带走了。

**根因＝合同缺口，不是模型能力**（同分钟反证：`capture` 在合同里写清楚了，
「夺回中央前哨」「占领烽火台」精准打到设施；`defend` 没写，它就自己找了个最像的填）。
引擎侧早就通了（`resolveTarget` 设施优先、`tacticalPlanner:178` 会把写错格的设施名搬回来），
**信息是在到达引擎之前丢的**——模型直接写了合法的战线 id。

**修法（Fable 批准 + 两笔小修）**：
1. `ai.ts:240` DEFEND WITH DESTINATION 补上 `targetFacility`（事实更正，引擎本就支持）
2. 一条语义原则替代 `ai.ts:302` 的动词枚举：**长官点名的是哪一级的地方，单子就填哪一级**
   ——★Fable 小修：**删掉括号里的类别例词，纯锚 `---FACILITIES---` 节区**
3. `ai.ts:301` 不动（核准）

**判据**（RED 先行、效果级、数落点坐标不看台词）：
- 修前 N=10「支援/守住 <某前哨>」→ `resolveIntent` → 量 order 落点与该前哨的距离，跑出红
- 修后落点在前哨；**★Fable 小修：N=10 里要含两句真点名战线的句子，修后必须仍走战线**
  （防原则反向溢出）
- 摘刀负对照：撤掉那条原则，降格必须回来
- 零溢出守卫：`capture`/`sabotage` 那些本来就对的格逐条不许变
- **prompt 一轮止损**（G 刀先例）

---

## §2 同族第二笔：兵调不动 / 说动了实际没动（本次手测新挖，未立提案）

用户手测三张截图，同一个根：

| 现象 | 证据 |
|---|---|
| 「我军兵营附近那几个坦克全部过去」→ 派的是**我军机场附近未编组群**，且 5 个只走 2 个 | 截图2 |
| 「兵营附近那个坦克为什么不去啊」→ **一个都没动**，回「请重新描述指令」 | 截图5 |
| 陈报「**东北方向**第一未编组群」而部队在兵营**西北** | 截图1 |

**已查到的机制**（`frontEscalationPayload.ts`）：
- `compassOctant:314` 的方位是**相对地图中心**算的，而长官那句问的是「兵营附近」
  ⇒ **同一句话两个参照系**，几何上没算错也一定读着是错的
- `nearestPlaceWithin` 只在 **`NAME_RADIUS_TILES = 12`** 格内找地名（标记优先），
  超出就退回方位词 ⇒ 那几支离兵营 >12 格，拿不到"兵营附近"这个名字
- 于是**群名里没有长官用的那个地标**，他点不中；模糊匹配就派了别的群

**这一笔还没写提案**，属战场板命名/参照系族（与据点刀相邻但不同刀）。
建议：据点刀落地后单独一页纸，别合并——两把刀都动"长官说的地方"，混在一起分不清谁的功劳。

---

## §3 语音刀留下的、还开着的账（全在 `LEDGER_ALL_KNOWN_ACCOUNTS_20260806.md` J 节）

| # | 一句话 |
|---|---|
| J1 | G17 过期名单：嘴报已花掉的票，长官照着点名被拒 |
| J2 | 回填气泡与复读在屏上分不清（用户当场把自己的气泡读成"陈又复读我"） |
| J3 | 本地应答音已判退；教训＝**凡在理解之前出声的，都会替嘴许下脑子还没做的承诺** |
| J5 | spoken 数量声明超出正文（真信封 5/20） |
| J6 | 意译复读：引擎闸只判整句照抄，换个说法说回来判不出 |
| J7 | R1 数量查法偏宽：报 1 例必真，报 0 例不等于没有 |
| J8 | **生产单次上限 10 静默截断**——Emily 说造 12、引擎造 10，诚实拒绝缺了第五种 |
| J9 | 派兵数量被截断无交代（3→2 没有一句解释） |
| — | **延迟基线**：用户豁免，`?webspeech` 开关在库里，想补随时可跑 |

---

## §4 新窗口第一句该做的三件

1. `cd "~/MyProjects/AI Commander-voice-input" && bash scripts/run-benches.sh` 存底（22/22）
2. 读 `FACILITY_TARGET_DOWNGRADE_BRIEF_20260810.md`（正文＋Fable 裁定＝施工合同）
3. **修前 RED 先行**：先把"落点距离"那条判据写出来跑红，再动 prompt

规矩照旧：一步一测一 commit、每步停下报 Fable 审、四红线（不打补丁／不穷举／
不套模板／不破坏现有逻辑）、有意行为变化 T1j 登记、**判据测效果不测措辞**。

---

**Opus 5 · 2026-08-10 · main `fb0aa8a` · tag `voice-input-v1-done` · 22/22**
