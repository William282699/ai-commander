# 请裁：据点被降格成战线（老账三度复发）+ 一行合同修法 + 语音收口路径

> 给 Fable 5。用户手测当场认出「这是老毛病，我记得搞定了啊」——**他记得没错，
> 修好的是它的邻居**。下面是三份历史记录、根因（不是模型能力）、修法与判据。
> 现场：worktree `voice-input-v1` @ `56da8bc`，22/22 全绿，main 零字节。

---

## §1 先报语音这一局的成绩单（决胜手测的机器面）

用户 17 个语音回合（服务端日志逐条对账，非口述）：

| 上一局 | 这一局 |
|---|---|
| 「通讯干扰」判废 **4/15** | **0/17** ✅ |
| spoken 复读长官原话（耳朵里听见自己的话） | **0/17** ✅ 复读闸一次都没需要出手 |
| spoken 缺席 → 三段墨迹 | 缺席 **1/17** ✅ |
| 「Emily」被听成「Amaly」（刀 C 的原始病样） | 「**Emily我好想你**」逐字全对 ✅ |

首词全活，含「呃我们现在能做多少辆」「嗯战狼点附近的那个部队」这类带语气词的开头。
**用户口径：「基本上我觉得都可以了，MVP 阶段可以忍受」。**

他挑出的两件都**不在语音层**：
1. Emily 说造 12、引擎只造 10 —— `tacticalPlanner.ts:970` `Math.min(quantity, 10) // cap 10`
   **静默截断**。Emily 生产合同的"诚实拒绝四分"（预算为零/钱不够/燃油不足/设施缺失）
   **缺了第五种：超过单次上限**。→ 已入 LEDGER J8。
2. 说「调三辆去支援南线前哨」→ 实际 2 个单位去了「4. 南部战线」。数量少 1 无交代
   （J9），**目的地被降格**——就是本档要裁的这一条。

---

## §2 老账三度复发，两份历史记录都写着"记账不修"

| 时间 | 记录 | 处置 |
|---|---|---|
| 撤退语义收口 | ROADMAP:55 「**「南线前哨」被填成 front 非前哨设施的粒度账**」 | **记账，未修** |
| 批准合同 v3.2 判退 | ROADMAP:40 三个结构病之③「**"夺回前哨"被降格成"增援战线"**」 | 整版被判退**雪藏**，这条被连累带走 |
| 2026-08-10 手测 | G17 那次：说中央前哨 → 兵去中央战线；本次：说南线前哨 → 兵去南部战线 | 第三、第四例 |

**用户为什么记得"搞定了"**：它的三个邻居确实修好了——撤退语义（撤到位不掉头）、
派兵作用域（74/85 → 10/84）、第 8 级地名精度（重叠 13→0、改名）。**这一条一直挂着。**

---

## §3 根因：不是模型能力，是合同没写（同局有反证）

### 合同原文三处，是它这么干的直接原因

| 位置 | 原文 | 后果 |
|---|---|---|
| `ai.ts:240` | **DEFEND WITH DESTINATION** — A "defend" intent with **toFront/targetRegion** MOVES units TO that location | defend 的目的地**只列了两格，没有 targetFacility** |
| `ai.ts:302` | When commander mentions facilities explicitly (**destroy, sabotage, capture**), use targetFacility | 用设施当目标的场景**只列了三个动词，支援/防守不在内** |
| `ai.ts:301` | **You can put a facility name directly in toFront** — the engine will resolve it | 明确鼓励把设施名塞进战线格 |

### 引擎侧其实早就通了（所以这确实是"修好了一半"）

- `tacticalPlanner.ts:178-183`：模型把**设施名**写进 toFront，规范化器会搬回 targetFacility
- `tacticalPlanner.ts:274-278` `resolveTarget`：**targetFacility 优先于 toFront**
- `tacticalPlanner.ts:1374` `planDefend`：先查 targetFacility

⇒ 只要单子上留着"南线前哨"这四个字，兵就会去前哨。**信息是在到达引擎之前丢的**：
模型直接写了一个合法的战线 id（front_south），规范化器无从察觉——它看到的是一条
完全正常的战线命令。

### 同一局的反证：合同写清楚的它就做对

同一分钟、同一模型：
- 「派Blake去把**中央前哨**夺回来」「占领**烽火台**」→ **精准打到设施**（capture 在
  `ai.ts:223` 明确写了「必须有 targetFacility」）
- 「支援**南线前哨**」→ 降格成战线（defend 没写）

**同一个能力，写了的做对、没写的自己找了个最像的填。** 这不是听不懂。

### 顺带答用户问的"要不要换模型"

我给他的答复，请你复核：
1. **换掉 Gemini，语音这一刀整个报废**——它押在 inline audio 上（消融实测：无信封
   两地名全错、带信封全对），换成不吃音频的模型就得退回 Web Speech；
2. **换模型这个 bug 大概率照样在**——病根在合同措辞，换谁读到的都是同一句
   "defend 用 toFront"。
   ⇒ 结论：这条 bug **不构成换模型的理由**。（他若为成本/延迟/整体理解力想换，另议。）

---

## §4 修法（两处，都在 prompt 面；不碰执行链，不撞 6b 禁改清单）

**修法① 事实更正**：`ai.ts:240` 的 DEFEND WITH DESTINATION 补上 `targetFacility`。
这不是加规则，是**补一句不完整的事实陈述**——引擎本来就支持（planDefend:1374 已核），
合同漏写了。同「旧行因线变而为假 ⇒ 必须改」的 carve-out 家族，只不过这里是"本来就不全"。

**修法② 一条语义原则**（替代 302 那串动词枚举——枚举撞红线二，且它正是漏网的原因）：

> 长官点名的是**哪一级**的地方，单子就填**哪一级**：他说的是 ---FACILITIES--- 里的
> 东西（前哨、雷达、弹药库…）就填 targetFacility，他说的是战线就填 toFront。
> **不要替他把据点归到它所在的战线上**——「支援南线前哨」和「支援南部战线」是两条
> 不同的命令，兵会去两个不同的地方。

零词表、零例句、给出理由。

**不动 `ai.ts:301`**：把设施名放进 toFront 是**真的**能用（规范化器接着），删掉会伤到
"只知道名字不知道 id"那一格。它的问题只是排序，由修法②压住。

---

## §5 判据（RED 先行，效果级，机器可判）

这一格**不必靠手测**——落点是可以算的：

1. **修前 RED**：真模型跑 N=10「支援/守住 <某前哨>」（前哨取自真信封的 ---FACILITIES---），
   拿回 intent → `resolveIntent` → **数 order 落点与该前哨的距离**。
   预期红：落点落在战线中心而非前哨（第 8 级已证战线中心可以离该线前哨 97 格）。
2. **修后**：同 10 句，落点在该前哨附近（阈值取第 8 级 `frontDestinationFor` 那套口径）。
3. **★摘刀负对照**：把修法②那一行撤掉重跑，**降格必须回来**——回不来说明治的不是这个。
4. **零溢出守卫**：`capture` / `sabotage` 那些本来就对的格**逐条不许变**
   （它们已经在用 targetFacility，新原则不许把它们推歪）。

判据落在**落点坐标**上，不看台词、不看 intent 字面——撞家法「判据要测效果」。

---

## §6 请你裁的三件

1. **修法两条准不准**（尤其②替代动词枚举这一步，我判它是去掉红线二隐患，不是放宽）。
2. **这一刀排在哪**：我倾向**语音收口之后立刻**——它便宜（两行 prompt）、判据现成，
   且它是用户四次手测里唯一还在复发的老账。但它是 prompt 面改动，按 G 刀止损线
   只有一轮，需要先有修前基线。
3. **语音能不能先收口**：机器面数据在 §1（判废 0/17、复读 0/17、首词全活），
   用户口径「MVP 可以忍受」。**未做的只剩延迟 A/B**（要起基线臂 + 用户贴一次控制台）。
   我的建议：延迟 A/B 跑完 → 语音收口合 main → 再开这一刀，**不要并行**
   （两把刀同时改 prompt，出问题分不清是谁）。

---

**Opus 5 · 2026-08-10 · 22/22 全绿 · 等裁**
