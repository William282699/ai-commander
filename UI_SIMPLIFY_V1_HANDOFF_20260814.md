# UI 简化 V1 · 交接文档（老 Fable 窗口笔录用户口述，2026-08-14）

> **给谁看**：新 Fable 窗口（出开工 plan＋实施）与新 Opus 窗口（审核 plan＋逐步报审）。
> 用户已亲自口述全部需求（含五张截图标注），本档是唯一权威转写；
> 与任何旧计划冲突时**以本档为准**。plan 写完先给 Opus 审，过了给用户三行
> 人话概要过目，然后开工。

## 0. 现场（老 Fable 已核）

- main = origin = `9e4f96e`，工作区净，全家 **25/25**（18 台架+typecheck）。
- 新分支建议 `ui-simplify-v1`，从 `9e4f96e` 切；worktree 复用
  `AI Commander-voice-input` 目录（.env 已配好，ops=deepseek 勿动）。
- dev 服务：3025(web)/3024(api)，`.claude/launch.json` 已有配置。
- 部署押后等本刀（侦察清单见记忆档 `project_deploy_prep`，不归本刀）。

## 1. 本轮范围（用户 2026-08-14 口述，七条全是表现层）

**主 UI（停靠态）三条：**

1. **编制页只留陈**：现在编制 tab 三列（CHEN/MARCUS/EMILY），马克斯与 Emily
   旗下永远是 `(empty)`（编制功能只给陈做了）——**藏掉他俩的列**，陈独占。
2. **底栏「编队」键只在陈的频道出现**：当前频道是 Marcus/Emily 时隐藏该键
   （用户截图①圈的就是 Emily 频道下的编队键）。
3. **砍掉 `+兵$80` 与 `+坦$200` 两个快捷购买键**（截图③圈处）。
   ★边界：**只砍按钮，不砍能力**——造兵走 Emily 对话（emily-production 合同，
   对话是唯一界面的正统路）。plan 里须 grep 确认按钮背后的处理函数没有
   别的调用方，砍键不断别的路。

**大弹窗（`?mode=panel`）四条：**

4. **BATTLEGROUP ORG TREE 陈独占整页**：藏 Marcus/Emily 两列，陈的树占满宽度
   （属下名字要看得清楚）。
5. **UNIT POOL 加一行 Total Units**：显示全体单位总数（现在只有分类行）。
6. **编队键与 +兵/+坦 的处置与主 UI 同**（第 2、3 条在弹窗内同样生效）。
7. **中栏对话栏与风格栏对掉**：麦克风＋输入框从页面最底部挪到**对话流正下方**；
   「风格」五条滑杆（冒险/集火/目标/惜兵/侦察）挪到**最下面、横排一行五个**。

## 2. 明确不在本轮的（别顺手做）

- **三个动效**（PTT 加大、按住无线电"正在发报"、打字电报机）——用户原话
  "做好了我再想…怎么做放哪里"，等他看完本轮再设计，**round 2**。
- 步 0 玩家功能清单（昨令）仍排队，不挡本轮、不并入本轮。
- 任何引擎/prompt/信封字节；任何 ChatPanel 逻辑重构（它 2500+ 行是已知债，
  **勿趁机拆**——那是验证后的活，且曾因此 revert 过一次）。

## 3. 约束（家法，审核按此打回）

1. **纯前端表现层**：引擎/prompt/信封零字节；台架 25 项照跑，红了＝溢出信号。
2. **隐藏≠删除**：Marcus/Emily 编制列与编队键用条件渲染藏，将来一开就回。
3. **一步一测一 commit**，每步截图报审（手感刀，验收＝用户眼睛；
   停靠态与弹窗态各截一张）。
4. 影响手感的变更先三行人话确认——plan 过审后给用户的概要即此。
5. 布局对掉（第 7 条）只动布局不动逻辑；输入框/麦克风的现有行为
  （PTT、回填、发送）一个字节不许变。

## 4. 建议步序（新 Fable 可改，改了在 plan 里说明理由）

步 1 砍两个快捷键（最小、先热身）→ 步 2 编队键按频道隐藏 → 步 3 编制页/org
tree 陈独占 → 步 4 Unit Pool 总数行 → 步 5 中栏对话/风格对掉（最大的一步，
单独 commit）。每步截图给用户过目后再走下一步。

## 5. 触点提示（线索非结论，plan 须自己核实并列全）

- 主 UI 底栏/编制 tab/频道状态：`apps/web/src/ChatPanel.tsx`。
- 弹窗态：`?mode=panel` 路由，Battle Board 面板组件（org tree / unit pool /
  中栏布局所在文件 plan 自查）。
- `+兵$80`/`+坦$200` 的 onClick 处理链与其它调用方（grep 证据进 plan）。

—— 老 Fable 窗口笔录，2026-08-14。疑义处回问用户，勿猜。
