# 第 8 级 用户手测记录 + 诊断 + 修法建议（2026-08-08）

> 用户实机手测四幕，Opus 5 整理与诊断。**待 Fable 裁定后再动代码**——本档写完时
> worktree 停在 `2d37e20`，未合 main、未打 tag。
> 手测版本：worktree `AI Commander-envelope-precision` @ `2d37e20`（刀3/刀4/fixA-B/
> 改名刀/刀2/刀1 六个 commit 全在），web 3017 / api 3016。

---

## 0. 一句话

**四幕的设计目标全部在实机里达成了**（含台架证不了的那一半：刀4 的标记名闭环、
刀1 的前哨危机改喊陈、刀2 的号贴部队名）。

**四笔账**，按处理优先级（**前两笔都是刀2 捅的**）：

| # | 账 | 性质 | 建议 |
|---|---|---|---|
| **H1** | 「群名+号」粘成一串写进 fromSquad → 解析不了 → 零执行 | ★ **刀2 的自造回归** | **本级修掉再合** |
| **H4** | 编制分队被信封当面称作「临时编队」+「N股…闲着」，陈照念成「空闲部队」，玩家编的队名没了 | ★ **「临时编队」这半是刀2/R6 捅的**；归类正确、措辞错 | **与 H1 一并修，需改 R6** |
| H2 | 同批兵每问一次换一个号（G32→G39、G45→G53） | 已登记 B3，本次拿到**执行代价**证据 | 维持排队，但证据升级 |
| H3 | 战力比方向不定：同一局同一个数被读成两个相反的意思 | 已登记 C2，本次**定位到具体缺口** | 一行节头可缓解，请裁 |

另有一笔观察（陈的"附近"措辞），见 §4。

---

## 1. 手测原始记录（逐幕转录）

### 幕一 · 开局环视

玩家问：**「当前战线什么情况?」**

> **陈军士 00:08**：长官，北部战线有7步兵3轻坦，中央战线4主坦4步兵，南部战线
> 7轻坦6步兵1主坦。各战线目前没有交火。

屏上可见地名（截图逐个核过）：沿海公路 / 阿拉曼镇 / 沿海雷达 / 北部山脊 /
**驼峰山脊** / 敌军德军营房 / 敌军总部 / 敌军机场 / 敌军意军营房 / 中央沙漠小路 /
3. 中央战线 / **烽火台** / 前线油库 / 沙漠弹药库 / 北线前哨 / 中央前哨 / 南线前哨 /
我军总部。

**判定：过。** 两个新名字在屏上；**没有出现**「中央沙漠西段 / 南缘 / 魔鬼花园雷区
北段 / 北部沿海东段」任何一个（fix A 生效）；陈报三条战线无交火，与目视一致。

### 幕二 · 中央战线危机 → 增援（刀1 + 刀2 主场）

> `01:12` 中央战线正在接战，敌军一辆轻型坦克试探性进攻，我方战力比4.50，局面稳定。
> **陈 01:21**：长官，中央战线我方单位仅能支撑18秒，敌方火力是我们的4.5倍。
> 东北方向第一未编组群可于17秒内抵达，是否立即调动增援？
> `01:34` 中央前哨 正在被夺取！(1%)
> **[玩家] 01:35**：可以
> **陈 01:37**：执行: 10 个单位前往3. 中央战线设防
> **陈 01:37**：明白。 调动东北方向第一未编组群**[临时编队G8]**增援中央战线
> **陈 01:37**：东北方向第一未编组群 10个单位出发了，按估算约 17 秒到位。
> `01:42` 3. 中央战线 遇袭，正在接战。
> `01:42` 长官，中央战线正遭敌军4辆主战坦克、2步兵和1辆轻坦攻击，我方中央前哨
> 已失守，该线部队预计24秒内将无法支撑。
> **[玩家] 02:02**：中央前哨附近还有可以支援的部队吗？
> **陈 02:03**：中央战线我方有9辆主战坦克和2个步兵班，敌军3辆主战坦克。
> 野战修理厂附近未编组群**[临时编队G15]**有8个单位，24秒能到中央战线。

**用户原话：「应该都是对的」。**

### 幕三 · 撤退（幕三的拓展）

> `03:00` 1. 北部战线 遭敌军1辆轻坦和1个步兵班袭击，我方部队仅能支撑5秒。
> `03:04` 1. 北部战线 快顶不住了——战力比 21%，正承受重火力。
> **陈 03:05**：长官，北部战线我方突击部队生存时间仅剩5秒，以目前敌我战力比 4.0
> 来看，是否继续维持攻势？
> **[玩家] 03:41**：北部战线的部队暂时先撤退到中央前哨
> **陈 03:42**：执行: 命令 10 个单位撤退至中央前哨
> **陈 03:42**：这就办。 北部战线部队撤退中央前哨
> **陈 03:44**：长官，两分钟前您固守中央战线的决定，我方阵地已稳定，但折损了两名弟兄。

**用户原话：「我觉得也可以，不错」。**

### 幕四 · 前哨危机 + 标记点调兵（刀1 R1/R10 + 刀4）

**4a｜前哨危机改喊陈（截图4）**

> `04:32` 北线前哨 正在被夺取！(1%)
> **陈 04:33**：长官，北线前哨（我方关键点）正被敌方8人夺取1%，我方无兵力。
> 是否调动闲置增援？
> **[玩家] 04:44**：我军兵营附近有没有空闲部队？
> **陈 04:45**：长官，我军兵营附近有我军机场附近未编组群**[临时编队G32]**，
> 共5个单位，包括2个步兵班、2辆轻型坦克和1辆主战坦克。
> **[玩家] 05:16**：我军兵营附近有没有空闲部队？（同一句再问一次）
> **陈 05:17**：长官，我军兵营附近有我军机场附近未编组群**[临时编队G39]**，
> 共5个单位，包括2个步兵班、2辆轻型坦克和1辆主战坦克。

用户标注：圈中我军兵营旁那一坨兵，「那个是 Aiden」，怀疑「临时部队编号和 Aiden
我给的编号有冲突」。

**4b｜标记点调兵（截图5）**

> **[玩家] 07:15**：战狼点附近的部队，去支援南线前哨
> **陈 07:16**：战狼点附近未编组群G45去支援南线前哨。
> **⚠ 这条引用的 战狼点附近未编组群G45 已不在编 —— 确认要继续，还是另指部队？**
> `07:31` 4. 南部战线 遇袭，正在接战。
> `07:31` 南部战线遭敌军4辆重甲和1步兵突袭，我部撑不过11秒，急需增援。
> `07:38` 4. 南部战线 快顶不住了——战力比 29%，正承受重火力。
> **陈 07:38**：长官，南部战线我方攻势受阻，前锋预计还能坚持8秒。我方火力比为
> 0.71，当前无增援可用。是否需要调整战术？
> **[玩家] 07:42**：战狼点附近的闲置部队，去增援南线前哨！
> `07:42` 南线前哨 正在被夺取！(1%)
> **陈 07:44**：执行: 8 个单位前往南线前哨设防
> **陈 07:44**：是，长官。 派遣战狼点附近未编组群G53增援南线前哨
> **陈 07:44**：战狼点附近未编组群 8个单位出发了。

**用户原话：「可以的，都通过了，里面唯一觉得不对劲的是」那句 ⚠ 警告。**

---

## 2. 达成确认：哪一刀在哪一幕被证到

| 刀 | 实机证据 |
|---|---|
| 刀3 战线消重叠 | 幕一三条战线的兵力报数与目视一致；全程无"一场仗两边都报" |
| fix A 屏上地名 | 幕一：四个新切子块**一个标签都没上屏** |
| 改名刀 | 幕一：**驼峰山脊 / 烽火台** 在屏上；无旧名残留 |
| 刀2 号贴部队名 | 幕二 `东北方向第一未编组群[临时编队G8]`、`[临时编队G15]`；幕四 `[临时编队G32/G39]`——号紧跟部队名，**未见行尾 handle=**，也未见号被绑给战线 |
| **刀1 设施票据** | 幕四 4a：北线前哨危机**出现在陈的频道**（旧行为在马克斯的 ops，那里一个兵都没有）、点明"我方关键点"、直接问"是否调动闲置增援"——R1/R10 判定式生效 |
| **刀4 标记进地名** | 幕四 4b：**`战狼点`（玩家插的标记）被当地名印进候选 label**，玩家把这名字说回去 → 8 个单位实际开赴南线前哨。**这是台架证不到的那一半：闭环在真局里跑通了** |

---

## 3. 四笔账

### ★ H1 —— 「群名+号」粘成一串 → 解析不了 → 零执行（**刀2 的自造回归**）

**现象**（幕四 4b，07:16）：玩家说「战狼点附近的部队，去支援南线前哨」，陈复述
`战狼点附近未编组群G45`，引擎当场回一句「⚠ 这条引用的 战狼点附近未编组群G45
已不在编」，**一个兵没动**。玩家 26 秒后重说一遍，才正常执行（8 个单位出发）。

**诊断（逐层落到字符串）**：

1. `detectStaleSquadRefs`（ChatPanel:222-252）打印的 `staleRefs` **就是
   `intent.fromSquad` 的原值**——不是从陈的台词里抠的。所以模型往 `fromSquad`
   里写的确实是整串 `战狼点附近未编组群G45`。
2. 解析实测：
   ```
   isTicketRef("G45")                   = true
   isTicketRef("临时编队G45")            = true
   isTicketRef("[临时编队G45]")          = true
   isTicketRef("战狼点附近未编组群G45")   = false   ← 就是它
   ```
3. 走向：`isKnownForceRef` 返回 false → 落进「这实体根本不存在」分支 →
   `stale.add(fromSquad)` → 确认闸问句 → **零执行**。
   **它连票据登记簿都没查**——G45 那张票很可能是活的。那句「已不在编」在这里
   也是误导：真相是"我读不懂这个引用"，不是"这批人没了"。

**根因是刀2。** 旧印法 `…未编组群 handle=G45`，名字与号在行里是**两样分开的东西**；
新印法 `…未编组群[临时编队G45]` 把它们**粘在一起**，模型抄"这支部队的引用"时就
整段抄走。不能证明旧印法下绝不发生，但**这个形状是刀2 请出来的，概率是刀2 抬高的**。

**我自己台架的缺口**（一并认）：K2-3「印认互逆」测了 `G7` / `临时编队G7` /
`[临时编队G7]` 三种，**唯独没测信封实际印出来的那一种**：`名[临时编队G#]`。
测了号在真空里的样子，没测它在行里的样子——与刀4 立的那条「引擎自己印出去的
名字必须认得回来」是同一条，刀2 只落实了一半。

**修法建议（闭环 + fail-closed）**：

`normalizeForceRef` 增一档——**剥掉尾部的号之后，剩下的前缀必须逐字等于那张票
自己的 `label`，才认**。

- 为什么这样切：它只认「我们自己印出去的那个组合」，不是模糊匹配，不是同义词表
  （红线二不违）。一个恰好以 G 数字结尾的真分队名不会被误吞——因为前缀对不上
  任何一张票的 label。
- 代价：`normalizeForceRef` 现在是纯字符串函数，要能看到票据登记簿。同文件内，
  不跨模块。
- 覆盖两种写法：`群名[临时编队G45]`（信封原样）与 `群名G45`（模型省掉方括号，
  **本次实测就是这种**）。
- 断言：①两种粘连形与裸号派同一批兵 ②`某某群G9`（G9 存在但 label 对不上）
  **拒绝** ③摘刀负对照。
- 工作量：一处函数 + 三条断言，约半小时。

**建议本级修掉再合**：demo 场景里它表现为"我下了命令，陈复述了，但一个兵没动"，
是最难看的一类；而它是刀2 捅的，不该留给下一级。

---

### H2 —— 同批兵每问一次换一个号（**已登记 B3，证据升级**）

**现象**：幕四 4a 同一句问两遍，同一批 5 个单位（2 步兵班+2 轻坦+1 主战坦克），
号从 **G32 变成 G39**；4b 里同一坨兵从 **G45 变成 G53**。

**核过的机制**：`mintSpokenForce`（escalationTicket.ts:198-215）无条件调 `mintOne`，
**没有任何复用查找**；`seq` 单调递增。每建一次板子/判读行就铸一批新号。

**这不是新账**——LEDGER **B3「同批兵换轮换号」**（G38→G42，修法已裁：活票复用 /
名单逐字节同 / 不覆写冻结字段 / 称呼沿用「临时编队」，排在第 9 级）。

**但本次证据把它升级了**：原来 B3 记的是"编号不稳定、难看"。这一局显示它有
**执行代价**——号一直变 → 对话历史里堆满死号 → 模型抄到一个旧的 → 绊索拦下 →
白费一轮。H1 与 H2 是**同一条链上的两截**：H2 制造死号，H1 让引用读不出来。
两条都修才彻底；只修 H1 的话，模型抄到真正过期的旧号时仍会被拦（那时候拦是**对的**）。

---

### H3 —— 战力比方向不定（**已登记 C2，本次定位到具体缺口**）

**现象**：同一局出现三次，方向互相矛盾。

| 时间 | 引擎/系统行 | 陈的说法 | 问题 |
|---|---|---|---|
| 01:12 / 01:21 | 「我方战力比 **4.50**，局面稳定」 | 「敌方火力是我们的 **4.5倍**」 | **方向反了**：从"我方 4.5 倍优势"变成"敌方 4.5 倍压制" |
| 03:04 / 03:05 | 「战力比 **21%**」 | 「敌我战力比 **4.0**」 | 大致是倒数（1/0.21≈4.76），但数字对不上 |
| 07:38 | 「战力比 **29%**」 | 「我方火力比为 **0.71**」 | 两个数都不等，也不互为倒数 |

**核过的产出面**（三处都叫"战力比/ratio"，但形态与方向各不相同）：

- `reportSignals.ts:483` → `战力比 {ratio*100}%`（百分数）
- `commanderPresence.ts:356` → `战力比{ratio.toFixed(2)}`（两位小数，mood 行）
- FRONT_JUDGMENT 行 → `ratio=0.33`（裸 token）

**新定位（本次的增量）**：FRONT_JUDGMENT 节头**解释了 survival 和 eta，唯独没说
ratio 是哪边除哪边**——

> `survival=committed HP vs visible enemy DPS, eta=straight-line terrain estimate`

方向没写，模型就只能猜；一局之内猜出了两个相反的方向。01:12 那次尤其贵：引擎给的
是"我方 4.5 倍、局面稳定"，陈告诉长官"我们撑 18 秒、敌人是我们 4.5 倍"，
长官据此批了一次增援。

**修法（成本很低，但要裁）**：节头给 ratio 补一句方向说明，与已有的
`survival=…` / `eta=…` 完全同类——**是格式/定义指称，不是新说话规则**。
C2 那个"四定义归一"的大活仍归第 9/10 级，本条只是把**已经在印的这个数**说清楚它
是什么。若判为超出本级范围，则原样记账，不动。

---

### ★ H4 —— 编制分队被当面叫成「临时编队」，玩家编的队名没了（**新账**）

**现象**（幕四 4a）：玩家先框选兵营旁一坨兵点「编队」，**特意去「编制」页确认
Aiden 这支队在**，然后问「我军兵营附近有没有空闲部队？」。陈答的是
`我军机场附近未编组群[临时编队G32]`。玩家怀疑第一次是编队没反应过来，
**又确认一次、又问一次**，陈仍答同一个未编组群（号变成 G39）。

**引擎侧已逐层排除**（实跑复现，不是读代码推断）：

1. **成员没有漏进未编组群**：照 `GameCanvas.handleCreateSquad` 的等价流程建队后，
   `buildBattleBoard` 的群行、`buildReinforceOptions` 的未编组候选里
   **一个编队成员都没有**。`inAnySquad` 过滤（frontEscalationPayload）与
   `squadLabels` 剔除（battleBoard:103-108）都工作正常。
2. **编队本身在信封里，而且带着长官用的那个词**。在兵营旁建队后实跑 digest：
   ```
   ---SQUADS--- (loc= is the only verified current position; …)
   Chen(combat):
     Aiden(T1,leader) parent:chen 5units(2×main_tank,2×light_tank,1×artillery)
       @(405,64) morale=1.0 mission=idle task=无任务 hp=100% loc=我军兵营附近
   ```
   `loc=我军兵营附近` —— **和长官问的「兵营附近」逐字一致**；`mission=idle`
   `task=无任务` —— 明写着它闲着。
3. **频道没过滤掉它**：`buildDigestForChannel`（digestHelper.ts:17-34）对 combat
   直接返回全量 `buildDigest`，不按 owner 筛。陈拿到的就是上面那一行。
4. `state.nextSquadNum` 会自增（`autoSquadId` 内部 mutate），不存在编号撞车。

⇒ **归类（谁算未编组）是对的。**

#### ★ 但措辞是错的，而且错的那半是刀2 捅的

用户补充确认后重新定性：**陈指的那批人就是 Aiden，他只是一直管它叫「空闲部队」。**
顺着这条线复现，抓到了：

构造一支**编制分队** Aiden(I1)（3 人，线外，闲着），让它成为某条战线的
best_help，实跑 `buildFrontJudgmentLines`：

```
1. 北部战线: survival≈15s ratio=1.00
   best_help=none(线外1股/3units 闲着，最近 Aiden(I1)[临时编队G1] eta≈69s > survival 15s，都赶不到)
```

一支**有名有号的编制分队**，信封同时给它贴了三个标签：

| 标签 | 出处 | 谁捅的 |
|---|---|---|
| **`[临时编队G1]`** | `handleOf` → `forceHandleTag`（commanderPresence:153-158） | ★ **第 8 级 刀2（R6 裁定）** |
| `1股` | `describeNoHelp`：`线外${idle.length}股` | 既有（v4 §8 期） |
| `闲着` | 同上 | 既有（v4 §8 期） |

**「临时编队」这半是本级新加的。** 刀2 之前这一行是
`best_help=Aiden(I1)(3units …) handle=G2`——号是行尾一个独立 token，**没有任何一个字
说 Aiden 是临时的**。刀2 按 R6 把 `[临时编队G#]` 贴到了部队名紧邻处，于是一支
编制分队被当面称作"临时编队"。

**R6 的理由在这个场景下不成立。** R6 原文：「升级候选可以是编制分队的一个**子集**
…冻结的那批本来就不是编制分队，自描述反而更准」。对**子集**这话是对的；
但这次候选是**整支分队**（3/3 全员），把它叫「临时编队」就是纯粹说错——
而且**正好抹掉了玩家编队时得到的那样东西：一个名字**。

⇒ **所以陈说「空闲部队」不是他在编话，是嘴照账本念，账本写错了。**
这与 G 刀立的那条形状完全一致：**「嘴已经诚实了，谎在账本」**。

#### 修法建议（要动 R6，须裁）

**判定式，不是措辞替换**：铸号时看冻结名单是不是**某支活着的编队的全员**——

- **是全员** ⇒ 印它自己的身份：`Aiden(I1)`，把手用它自己的 `I1`（本来就是合法
  fromSquad），**不加「临时编队」**；
- **不是全员**（子集 / 空间群 / 跨队拼盘）⇒ 照 R6 原样印 `[临时编队G#]`。

一并建议复核（既有账，不是本级捅的，但同屏加重了误读）：`describeNoHelp` 的
「N股…闲着」把编制分队也数成「股」。分队有编制、有名字，不该论"股"。

**成本**：`handleOf` / `mintSpokenForce` 一处判定 + 断言（全员编队不带临时编队前缀 /
子集仍带 / 空间群仍带 / 摘刀负对照）。与 H1 是同一片代码，**建议与 H1 一并修**。

**为什么建议修而不是记账**：它直接吃掉「编队」这个功能的可见价值——
长官花力气编了队、给了名字，参谋当面把它降级成"一股临时的闲兵"。demo 里这一下很伤。

---

## 4. 一笔观察

**陈用"兵营附近"描述一个"机场附近"的群** —— 04:45 与 05:17 两次都说
「我军兵营附近有**我军机场附近**未编组群」。我军兵营 (410,75)、我军机场 (450,130)，
**相距约 68 格**。label 是引擎给的，"兵营附近"那半句是陈自己加的——他把长官的
问法套在了一个并不在那儿的群上。n=2，够立观察账，不建议本级动。

---

## 5. 请裁

1. **H1 修不修、是否本级修**（我的意见：修，它是刀2 捅的，且在 demo 里表现为
   "命令没反应"）。修法如 §3-H1，含三条断言与摘刀负对照。
2. **H3 的节头一句**：补 ratio 方向说明算不算"只改格式/定义指称"（我认为算，
   与已在节头里的 `survival=` / `eta=` 同类），还是判为超范围、原样记账。
3. **H4：R6 要不要收窄成判定式**（全员编队印自己的名与号，只有子集/空间群才叫
   「临时编队」）。我的意见：修，且与 H1 同一片代码一并修。**这是本级新捅的**——
   刀2 之前没有任何一个字说 Aiden 是临时的。玩家观感代价很实：
   **编了队、起了名，参谋当面把它降级成"一股临时的闲兵"**。
   顺带请裁：`describeNoHelp` 的「N股…闲着」把编制分队也论"股"（既有账，
   不是本级捅的，但同屏加重误读），要不要一并收。
4. H2 维持排队即可，但建议把"执行代价"写进 B3 的账面（它不再只是难看）。

—— Opus 5 整理，2026-08-08。手测数据来自用户实机四幕；诊断中的每条机制均已在
`2d37e20` 上逐行核过或实跑，未据台架推断。
