# spoken 层 · 步 6 决胜手测单（2026-08-09 立，2026-08-10 刀 C 后重写）

> **这一份决定 V1 能不能合 main。**
> 现场：worktree `~/MyProjects/AI Commander-voice-input` @ 刀 C 收尾 commit，
> 工作区干净、全家硬线 22/22 全绿、main 零字节。
>
> 刀 C（句首被吃）**已经修完并证完**，本单不再包含它——两臂对照结果在
> `VOICE_KNIFE_C_HEAD_BLINDSPOT_PROPOSAL_20260810.md` §11。
> 你现在测的是**耳朵这一层**：陈说给你听的那一句，好不好听、够不够快、对不对。

---

## §0 你要开的那一个页面

**本刀臂 → http://localhost:3023**

打开就能玩。**先点一下 🔊**（默认 🔇，不开什么都听不见）。

> 这一对服务（网页 3023 → 后端 3022）起在**我的名下**，所以你说的每一句、
> 陈听成什么、设备开销多少毫秒，**全都自动落进我读得到的日志**。
> §2 那份听错清单你一个字都不用记。

**延迟基线臂**（现状 Web Speech，用来跟本刀比快慢）：**要做 §1 时先跟我说一声**，
我现起——同一个文件夹最多只能同时挂 5 个服务，位置不够，不能一直开着。

---

## §1 延迟 A/B（唯一需要你动手贴一次的地方）

分层之后"第一声"的意思变了，所以分两个数看：

| 量 | 本刀臂 | 基线臂 | 线 |
|---|---|---|---|
| **t1 松手→第一声** | 本地应答（「收到。」这类） | 陈的正文起念 | 本刀臂**应该更早** |
| **t2 松手→实质内容** | spoken 起念 | 同上（基线只有一声） | **中位劣化 ≤ +0.7s** |

**t2 超线 ⇒ 按 §X-2 裁定：V1 不合并、退回现状。这条别客气。**

把这段贴进 DevTools → Console（两个臂各贴一次），之后每句话自己打印：

```js
(() => {
  window.__lat = []; window.__n = 0; let t0 = null;
  const mark = (kind, ms, text) => { window.__lat.push({ n: window.__n, kind, ms, text }); console.log((kind === 'play' ? '🔊出声 ' : '📡请求 ') + '+' + ms + 'ms  ' + text); };
  document.addEventListener('pointerup', (e) => {
    const b = e.target && e.target.closest && e.target.closest('button');
    if (b && /按住说话|松开结束/.test(b.title || '')) { t0 = performance.now(); window.__n++; console.log('—— 第 ' + window.__n + ' 句，松手 ——'); }
  }, true);
  const of = window.fetch;
  window.fetch = async function (i, init) {
    const url = typeof i === 'string' ? i : (i && i.url) || String(i);
    if (url.includes('/api/tts') && t0 != null) {
      let t = ''; try { t = JSON.parse(init.body).text; } catch (e) {}
      window.__lastTtsText = t; mark('req', Math.round(performance.now() - t0), t);
    }
    return of.call(this, i, init);
  };
  const op = HTMLAudioElement.prototype.play;
  HTMLAudioElement.prototype.play = function () { if (t0 != null) mark('play', Math.round(performance.now() - t0), window.__lastTtsText || ''); return op.apply(this, arguments); };
  if (window.speechSynthesis) { const os = speechSynthesis.speak.bind(speechSynthesis); speechSynthesis.speak = (u) => { if (t0 != null) mark('play', Math.round(performance.now() - t0), (u && u.text) || ''); return os(u); }; }
  console.log('计时器已装。说完 10 句后敲 copy(JSON.stringify(window.__lat)) 拷给我。');
  return 'ready';
})()
```

**只看 `kind:"play"` 那几行**（`req` 是请求发出的时刻，差一个 Edge 往返约 0.65s）。
说完敲 `copy(JSON.stringify(window.__lat))` 贴给我。

> 参考值（我用假麦跑通探针时量到的，**不是判据**）：本地应答 1.67s、spoken 3.45s。
> ★提案 §4 画的「说完 0.3s 出第一声」**乐观了**——"本地"是不等模型，不是不等 TTS。

---

## §2 听错清单（**你什么都不用记**）

自然玩就行，想说什么说什么。你说的每一句、陈听成什么，我从服务端日志逐条对账。

想帮忙的话，多说几句带这些名字的：
**战狼点 / 南线前哨 / 中央前哨 / 烽火台 / 驼峰山脊 / 阿拉曼镇 / Aiden / G8 番号 / 一句纯确认「可以」**

> 已知会盯的一笔：「战狼点」→「狼店」（刀 C 手测中出现过 1 次，句中非句首，
> 是转写本身错，不是采集）。这次看还在不在。

---

## §3 spoken 手感（**这一节才是本单的主菜**）

你原话是「要么大段全读跟 Emily 似的当播报员，要么出现 Aiden 这种，要么逼逼半天，
要么重复我说的话，要么就短短一句」。逐条对：

| 症状 | 该变成什么 | 你听到的 |
|---|---|---|
| 大段全读 | 耳朵里只剩一两句人话 | |
| 逼逼半天 | 同上 | |
| 重复我说的话 | 耳朵里不该再出现你自己的话 | |
| 短短一句（只剩「照办」4 个字） | 该有一句真内容 | |
| **Aiden 这种** | **仍会出现，见 §5-1** | |

**三件要你特别留意的：**

1. **两声的节奏**：松手 →（很快）一声「收到。」→（两三秒）一句人话。
   舒不舒服？会不会觉得"应了一声又等半天"还不如直接等？
2. **★承诺早于理解**：本地应答那一声是从现有池子里随机抽的，里面有「这就办。」
   「动手。」这类**承诺**词——而它出声时模型还没听懂。万一那一轮陈其实要反问
   「您刚才说的是？」，你会先听到一句"这就办"再听到反问。**撞上了记一笔。**
3. **执行回执不再单独出声**：屏上照旧有「照办，长官。 增援中央」，耳朵里不会再
   听第三遍——因为 spoken 已经说过了。**听着是清爽还是缺了个确认？**
   这是我定的（R2 二选一），你说不对就改回去。

---

## §4 四幕语音重走（第 8 级那四幕，全用嘴说）

- **幕一 · 开局环视**：「当前战线什么情况」→ 三条战线报数与屏上是否一致
- **幕二 · 中央战线危机 → 增援**：等危机弹出来 → 用嘴回「调东北那队去增援」
  → 看**实际开动的是不是那一队**（会动兵的判据，别只听台词）
- **幕三 · 撤退**：「北部战线的部队暂时先撤退到中央前哨」→ 看撤的是不是北线那批、
  到了以后**会不会又掉头开回原岗**（旧账 `87ea941` 修过，顺带回归）
- **幕四 · 前哨危机 + 标记点调兵**：等前哨危机 →「我军兵营附近有没有空闲部队」
  → 再用嘴把**标记名**说回去（「战狼点附近的部队，去支援南线前哨」）→ 看真有兵开过去

出现"说的和做的不一样"，**记时间点**，我按时间去捞日志。

---

## §5 两笔观察项（刀 C 之后新加，**复发才立案**）

刀 C 之前这两笔挂着不动，理由是家法「**输入是脏的时候不审嘴**」——句首都被吃了，
再去改陈的说话合同，改的是一个没犯错的人。**现在输入干净了，可以看它们还在不在。**

| # | 盯什么 | 怎么算复发 |
|---|---|---|
| ① **拒办口吻** | 陈说出「XX 已失守，无法再增援 YY」这类**替你判死**的话 | **战线失守 ≠ 派不了兵**。陈可以说代价（"过去要 40 秒、那边就空了"），但"值不值得"是你的决定。他说"无法"就算复发 |
| ② **屏上复读** | 陈把你刚说的话原样印在屏上当回答 | 耳朵这一半分层后已解决；**屏上这一半**还在不在（台架实测 6/21）。看到就记一笔 |

两笔都**只在干净输入下复发才立案**——这次要是不复发，说明它们本来就是脏输入的
下游症状，直接销账。

---

## §6 我知道的、还没解决的（免得你重复报）

1. **「Aiden 这种」仍会出现**——判据已按 Fable 裁定改口径：**信封里有的专名
   （`I1(Aiden)`）念出来不算错，信封里没有的洋文才算**。根因是信封没给部队中文
   代称，真解是引擎侧另一刀（已进 LEDGER）。**这一格请你判它有多难受**——
   难受就排下一刀。
2. **屏上仍可能重复你说的话**（见 §5-②）：分层后耳朵听不见了，屏上那行还在。
3. **★台架的小信封里 spoken 和正文几乎没分化**（长度比 0.96~1.11）——真信封下
   分化度只有这次手测能判。**如果你听到的还是"正文照读"，那这一层就是白上了**，
   这是本单最要紧的一个观察。

---

## §7 跑完给我什么

1. §1 两臂各一份 `copy(JSON.stringify(window.__lat))`
2. §3 那张表 + 三件留意事项的感受
3. §5 两笔有没有复发
4. 四幕里任何"说的和做的不一样"的时间点
5. 一句话：**这一层该不该留、V1 该不该合**

听错清单（§2）不用你交——我从日志读。

---

**Opus 5 · 2026-08-10 重写 · 刀 C 已收口，等这一份定 V1 去留**
