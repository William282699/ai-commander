# 第 8 级提案：信封与地名精度 + 校准记录仪（2026-08-06, Fable 5 起草）

> 自足档案，开工不依赖对话上下文。实施者（Opus 5 新窗口）从 §0 读起。
> 本级回到**引擎层**（上一级 G 刀是纯 prompt；这级正好相反：prompt 一行不动）。
> 五把小刀全部来自用户活体撞过的账，诊断齐全、零新数据依赖。

## 0. 现场

- main = `d5eb32e`（= origin/main，第 7 级收口后），**开新 worktree**（家法：main cwd 零实施）
- 一刀一 commit：typecheck 4 包 + 全部台架 + negctl 真红 + 相应快照；不带 lockfile、不 add .github/
- 每刀先盘点（file:line 落到实处，若实际结构与本档描述不符→停下来报告，不许猜）
- 判据家法全套：会动兵的数 assignedUnitIds+核坐标；负对照必做；fixture 走生产路径

## 刀1 危机票据带设施名（撞过实账：北线前哨挨打、援兵去战线别处）

**病**：危机链从战报到票据只说"战线"；前哨的名字传丢。玩家在前哨危机语境下说
「派他们去」，票据只带 `targetFrontId` → 走梯子 → 落到战线上现存友军簇，前哨没人管。
**修**：危机事件若锚在某个设施（前哨遇袭/占领冲突），铸票时把设施 id 一并冻进票据；
`ticketDestinationVerdict` 分支 3 注入顺序改为：**票据设施名 > 票据战线**（"精确目的地
压过梯子"的优先级 `resolveTarget` 里已在，缺的只是把名字传到位）。设施已丢（转敌）时
不注入设施名照旧走战线——夺回是 attack 语义，另说。
**验收**：生产路径造"前哨危机 + 无目的地跟进单"→ 落点 == 设施坐标（数 assignedUnitIds
+核坐标）；负对照：纯战线危机（无设施锚）行为与现状逐字节同；设施已转敌 → 不注入。

## 刀2 番号印到部队名紧邻处（撞过实账：「北部战线 Aiden G2」贴错人 9-30/131）

**病**：`handle=G#` 印在整行末尾，行主语是战线，模型把号绑给行首名字。G 刀把号念出来后
歧义变假话。
**修**：所有铸号面（态势板行/板子群行）把号印在**部队名紧邻处**，行尾 `handle=` 形式废除。
**★用户裁定（2026-08-06）：印成自描述形式「临时编队G#」**（如 `X附近未编组群[临时编队G2]`，
具体格式设计时定）。原则＝玩家不需要记号——每次提到自带"这是临时编队"的说明，
陈照账本念嘴上天然带上，不加任何说话规则。第 9 级做同批复用号时沿用此称呼。
SPEECH_RULE_SITES / CANDIDATE_FACES 两张登记表同步。
**验收**：信封行格式断言（号与部队名相邻、行尾无 handle=）；不铸号路径字节不变（纯度
断言沿用）；贴错率的活体改善在用户后续对局中由盲读记录（不设本刀阻塞线）。

## 刀3 战线矩形消重叠（撞过实账：「中央战线无战事却吃紧」）

**病**：`central_desert` bbox [120,80,370,140] 把 `ruweisat_zone` [230,85,275,115] 整个
套住——中央战线与山脊战线地盘重叠，同一场仗两边认领，战报与玩家目视对不上。
**修**：改 `mapData.ts` 的 bbox 数字消除重叠（哪块地归哪条战线由地图语义定：雷达区归
山脊，中央矩形相应收边）。**全图审计所有 zone 两两对**，不只这一对。
**新增不变量台架**：扫全图（或全部 bbox 对）断言**任何一点最多属于一条战线**，从此
重叠即炸红。
**⚠️ 连带面最广的一刀**：bbox 变了 = 单位/设施的战线归属变了 = 战报、派兵、崩溃钟的
输入都会挪。必须全台架过 + 逐一核对快照 diff，凡行为变化在 commit message 里显式列出
（合同变更纪律，照 T1j 先例）。若审计发现**有意**的重叠（如中立带），停下来报告。

## 刀4 tag 进就近地标（撞过实账：闲兵停在标记点旁却只能叫"东北方向群"）

**病**：`nearestPlaceWithin`（frontEscalationPayload.ts）只扫设施和战线中心，不认玩家
插的 tag。
**修**：tag 加进扫描（玩家自己的公开标记，零雾风险）；命名如「标记点1附近未编组群」。
**验收**：tag 半径内的群名带标记点；半径外不变（负对照）；无 tag 局字节不变。

## 刀5 校准记录仪（为第 9 级"账本诚实化"攒数据；纯仪表，零行为改动）

**病**（第 9 级要治的，本刀只记账）：崩溃钟报 1-6 秒实撑 30 秒+、ETA 偏大 3 倍、
"战力比 4.8 却 4 秒被突破"——校准需要"报的数 vs 实际结果"成对样本，现在 n=2-3。
**修**：引擎侧记录仪——凡**说出口**的 survival 秒数 / ETA / 战力比（升级面、态势板面、
proactive 面）落一行 diagnostics（值+战线+时刻）；结果侧记录战线实际失守/守住时刻与
援兵实际到达时刻。**信封字节零变化**（记录在 digest 构建之外），行为零变化。
**验收**：信封 sha 前后逐字节同（硬线）；泵帧一局产出配对记录；记录字段够第 9 级
直接算偏差倍数。

## 排队预告（第 9 级起，本级不碰）

执行链补漏：**打赢自走回**（攻击版 stale-anchor walk-home，帧级证据在
`~/MyProjects/_archive/retreat-uturn-20260729/`，候选修法已写：带单且单目标≠家→废锚
提前）；toFront-only+all 广派侧门（作用域规矩延伸，与 H1 披露衔接）；G38→G42 同批
换号（roster 相同复用号，已裁）；handleThreadApprove 番号（或确认死路拆除）；
crisisResponse 旧卡遗留 frontCenterPos 殉葬。
账本诚实化（第 10 级候选，吃刀5 的数据）：崩溃钟校准 + 战力比四定义归一。
战略表态→引擎 preflight（吃用户对局频率/手感数据）。G 刀二期（马克斯/Emily/全体
频道/存量例句，吃 ALL 频道对局证据）。

## 给用户的对局数据收集清单（桶二；第 8 级合并后、玩的时候顺手记）

刀5 记录仪装上后，崩溃钟/ETA/战力比的校准数据**自动攒**，不用手记。需要人眼的只有五样：
1. **商量被抢跑**：你在聊战略他却开单——截图（供"战略表态→preflight"定阈值与频率）；
2. **高影响确认闸弹出时烦不烦**：记一句当时感受（同上，定手感边界）；
3. **首问画树/答非所问**：问判断他不表态、问A答B——截图攒 n（现各只有 1-2 例）；
4. **全体频道与马克斯/Emily**：偶尔故意去聊几句，看到编话截图（G 刀二期的排期证据）；
5. **抽兵披露句**：大规模派兵时那句"其中N个是从…抽走的"，烦了就说（H1 手感账）。

## 收口路径

五刀各自绿 → 用户手测（重点：前哨危机跟进单落前哨、号贴对人、中央战线战报与目视
一致、标记点旁的群叫得出名、玩起来无任何新异样）→ 合 main + tag
`envelope-precision-v1-done` + ROADMAP 收口段 + push。

—— Fable 5 起草 2026-08-06。证据出处：ROADMAP 第 6/7 级收口段、
DIALOGUE_G_KNIFE_AB_REPORT_20260806.md、本日手测截图账（北线前哨/中央战线/贴错号）。
